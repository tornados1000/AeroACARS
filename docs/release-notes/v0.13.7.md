## 🇩🇪 Deutsch

**v0.13.7 — Polish-Release. Keine neuen Features, aber 10 angesammelte Kleinkram-Punkte abgeräumt: Security-Bumps, i18n-Lücken, Dead-Code raus, Cold-Start spürbar schneller.**

### Was Du davon hast

- **Schnellerer App-Start** — Landung-Tab + Release-Notes-Modal werden jetzt lazy nachgeladen. Main-Bundle: 743 KB → 634 KB (gzip 202 KB). Spürbar beim ersten Öffnen.
- **PIREP server-side entfernt → Banner** — Wenn phpVMS einen laufenden PIREP wegwirft (Inaktivitäts-Timeout oder Admin-Eingriff), kriegst Du jetzt einen ℹ-Banner statt nur einen Activity-Log-Eintrag. Lokaler Flug-State wird sauber aufgeräumt.
- **IntegrityBanner spricht jetzt komplett DE/EN/IT** — vorher fielen 6 Keys auf Schlüssel-Namen zurück.
- **Resume-Hard-Stop-Dialog jetzt auch auf Italienisch** — die 7 fehlenden `resume.*`-Strings sind nachgezogen.
- **Doppelte-Haken-Fix** (Issue #43) — formal geschlossen, war bereits gefixt.

### Unter der Haube

- **RustSec-Bumps**: rumqttc 0.24 → 0.25, rustls 0.22 → 0.23, sentry 0.34 → 0.42. Closet die rustls-webpki-CVE-Kette aus dem Sentry-Pfad. Verbleibender Hinweis im rumqttc-Pfad ist für unseren WSS-gegen-Caddy-mit-Let's-Encrypt nicht ausnutzbar (kein CRL-Handling, keine Name-Constraints).
- **Vitest 2 → 4** — alle 73 Frontend-Tests grün.
- **Dead-Code raus**: `SimPanel.tsx` (263 Zeilen, seit Monaten ungenutzt), `activity_log_add` Tauri-Command (Frontend hat ihn nie aufgerufen), 2× Diagnose-`console.log` aus `BidsList.tsx`.

### Was Du tun musst

Nichts. Auto-Updater rollt v0.13.7 aus.

---

## 🇬🇧 English

**v0.13.7 — polish release. No new features, but 10 accumulated paper-cuts cleaned up: security bumps, i18n gaps, dead code out, noticeably faster cold start.**

### What you get

- **Faster app start** — Landing tab + release-notes modal now lazy-loaded. Main bundle: 743 KB → 634 KB (gzip 202 KB). Noticeable on first open.
- **PIREP removed server-side → banner** — When phpVMS drops a running PIREP (inactivity timeout or admin action), you now get an ℹ banner instead of just an activity-log entry. Local flight state is cleared cleanly.
- **IntegrityBanner now fully DE/EN/IT** — 6 keys previously fell back to key names.
- **Resume hard-stop dialog now in Italian too** — the 7 missing `resume.*` strings added.
- **Double-checkmark fix** (Issue #43) — formally closed, was already fixed.

### Under the hood

- **RustSec bumps**: rumqttc 0.24 → 0.25, rustls 0.22 → 0.23, sentry 0.34 → 0.42. Closes the rustls-webpki CVE chain on the sentry path. The remaining advisory on the rumqttc path is not exploitable for our WSS-against-Caddy-with-Let's-Encrypt setup (no CRL handling, no name constraints).
- **Vitest 2 → 4** — all 73 frontend tests green.
- **Dead code out**: `SimPanel.tsx` (263 lines, unused for months), `activity_log_add` Tauri command (frontend never called it), 2× diagnostic `console.log` from `BidsList.tsx`.

### What you need to do

Nothing. Auto-updater rolls out v0.13.7.
