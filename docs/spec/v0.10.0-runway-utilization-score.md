# v0.10.0 — Runway-Utilization-Score (Bahn-Auslastung neu)

**Status:** DRAFT R5 — addresses QS-R4 findings (2 P1, 1 P2 — Spec-Drift in Test-Section)
**Autor:** v0.9.x-Sessions-Owner
**QS-Reviewer:** Thomas Kant (VA-Owner)
**Trigger:** Adrians Discord-Beschwerde 2026-05-18 (EK406 OMDB→YMML A388). Plus F19 (8-Monate-alter Sync-Bug).

---

**Errata v0.20.x (Bahnauslastung-QS, 2026-08-04):** Thomas' Beschwerde zu
seinem eigenen Flug (EDDH→LOWS, 53,8 % effektive Auslastung → nur
„ok_stop", 55 PT, trotz objektiv guter Landung) zeigte, dass die Bänder
zu strikt waren — 53,8 % auf einer 2850-m-Bahn ist keine schlecht
geflogene Landung, sondern eine komfortable. Die Bänder unten (30/50/70/
90 %) und die Float-Toleranz (15 % LDA, siehe v0.12.0-Spec) sind damit
VERALDET — aktuelle Werte: Banding 40/60/80/95 %, Toleranz 20 % LDA.
Siehe `sub_rollout.rs`'s Konstanten (`EXCELLENT_MAX_PCT` etc.) für den
aktuellen Stand; `score_algorithm_version` Some(5). Diese Spec bleibt
als historischer Kontext für die GRUNDFORMEL (LDA-basiert statt Meter-
absolut) gültig — nur die konkreten Zahlen sind überholt.

---

## Changelog dieser Spec

- **R0:** initialer Draft (verworfen — Formel + SSoT-Punkt falsch)
- **R1:** Thomas-QS — 2 P0 + 5 P1 + 2 P2
- **R2:** alle R1-P0/P1 addressed
- **R3:** R2-Befunde eingearbeitet (Missing-TD-Skip, Overrun-vor-Allowance, etc.)
- **R4:** R3-Befunde eingearbeitet:
  - P1-1: `SubScoreEntry`-Struct jetzt 1:1 mit echtem Rust-Code (verifiziert in `landing-scoring/src/lib.rs:66-96`)
  - P1-2: `SubScoreEntry::skipped("rollout", "landing.sub.rollout", reason)` — 3 Args, nicht 2
  - P1-3: `pre_displaced_threshold`-Cap überschreibt jetzt `rationale_key` auf `pre_displaced_capped` (vorher: original Rationale + Score-Cap = UI sah „Viel Reserve" bei 55 PTS → unehrlich)
  - P2-1: `insta` raus, Mini-Golden-JSON-File rein (kein neuer Dep nötig)
  - P2-2: Repo-Pfade präzisiert: Pilot-Client in AeroACARS-Repo, Webapp in **aeroacars-live**-Repo (separates Repo)
- **R5 (jetzt):** R4-Spec-Drifts in der Test-Section behoben:
  - P1-1: `pre_displaced_caps_at_55_pts`-Test prüfte noch alte R3-Annahme („Rationale bleibt original = excellent_margin"). Jetzt: `assert_eq!(rationale_key, Some("landing.rat.pre_displaced_capped"))` — passt zu R4-Logik (LE3).
  - P1-2: `wire_schema_snapshot_matches_golden`-Test rief noch `insta::assert_snapshot!`, obwohl R4 LE8 `insta` korrekt ausgewählt und auf Mini-Golden-JSON umgestellt hatte. Test jetzt 1:1 wie LE8 (`std::fs::read_to_string` + `assert_eq!`). Verhindert, dass jemand später `insta` aus Versehen reimplementiert.
  - P2: Aufwand-Tabelle nannte `warning` als Wire-Erweiterung — falsch, `warning` existiert schon (verifiziert in `landing-scoring/src/lib.rs:92-95`); bekommt nur einen neuen Wert (`pre_displaced_threshold`). Zeile präzisiert.

---

## Ziel

Den Sub-Score „Bahn-Auslastung" so umbauen, dass er **eine ehrliche, pilot-verständliche Aussage über die genutzte Landing-Distance trifft** — Single Source of Truth im Rust-Crate, alle UIs rendern nur, Wire-Format explizit versioniert.

---

## Pflicht-Vorgaben

### Datenquellen (alle bereits im Touchdown-Payload, verifiziert für EK406)

| Feld | Type | Quelle | Verwendung in dieser Spec | Skip-Verhalten bei `None` |
|---|---|---|---|---|
| `td_distance_from_threshold_m` | `f64` | Forensik | Numerator-Bestandteil | **SKIP** (`missing_td_distance`) |
| `rollout_distance_m` | `f32` | Forensik | Numerator-Bestandteil | **SKIP** (`missing_rollout_distance`) |
| `landing_float_distance_m` | `f32` | Forensik | Nur als Display-Info, nicht im Score | OK (Info-Zeile entfällt) |
| `runway_length_m` | `f32` | Navigraph | Denominator-Bestandteil | **SKIP** (`missing_length`) |
| `runway_displaced_threshold_ft` | `i32` | Navigraph | LDA-Berechnung | OK → 0 ft angenommen |
| `pre_displaced_threshold` | `bool` | Forensik | Cap-Gate (LE3) | OK → false angenommen |
| `runway_geometry_trusted` | `bool` | Forensik | **MUSS `Some(true)` sein** | **SKIP** (`untrusted_geometry` für `Some(false)` UND `None`) |
| `airport_source` | `String` | Recorder | Muss `"runway_match"` sein | **SKIP** (`off_airport_landing`) |
| `aircraft_icao` | `Option<String>` | Bid + Sim-Fallback | Heavy-Allowance-Modifier | OK → 0 Allowance |

**Wire-Wide-Format:** alle Felder schon im heutigen `TouchdownPayload`. **Aber:** `SubScoreEntry`-Wire-Struktur wird in dieser Spec **explizit erweitert** (siehe LE12).

---

## Leitentscheidungen

### LE1 — Numerator: TD-Punkt-bis-Stop

```
distance_used_m = td_distance_from_threshold_m + rollout_distance_m
```

**Sowohl `td_distance_from_threshold_m` als auch `rollout_distance_m` sind hard-required.** Wenn eins fehlt → Skip (siehe LE6).

**R2-P0-1-Fix:** R2 hatte `unwrap_or(0.0)` — das hätte missing data versteckt und falsche 100 PTS produziert. Entfernt. **Kein Fallback auf rollout-only.**

`landing_float_distance_m` bleibt als reine Info-Zeile in der UI, nicht im Score (TD-Distanz enthält Float schon implizit über die Forensik-Definition).

### LE2 — Denominator: Landing Distance Available (LDA)

```
displaced_m = runway_displaced_threshold_ft.unwrap_or(0) as f32 * 0.3048
LDA_m = runway_length_m - displaced_m
```

Wenn `LDA_m <= 0` → Skip (`invalid_lda`). Datenfehler-Schutz.

### LE3 — Sonderfall `pre_displaced_threshold = true` → **Score-Cap auf 55 PTS**

**R2-P1-4-Fix:** R2 ließ den Score ungekappt mit reiner Warning-Pill. Thomas-Vorschlag: Cap auf 55 PTS (= „ok_stop"-Niveau max).

```rust
if input.pre_displaced_threshold.unwrap_or(false) {
    points = points.min(55);
    warning = Some("Vor Displaced-Threshold aufgesetzt — Anflugprofil prüfen");
}
```

Begründung: Pilot setzt VOR der nutzbaren Schwelle auf (= regelwidrig, Wing-Tip-Vortex- + Lärmschutz-Verstoß). Würde der Bremsweg „excellent" sein, wäre der Eindruck „alles super" — falsch. Mit Cap 55 sieht Pilot: „solides Stopping, aber: KEIN Excellent möglich wegen Aufsetz-Punkt".

`distance_used_m` Negativ-Schutz (R2 LE3 bleibt): TD vor Schwelle = negative `td_distance_from_threshold_m`, summiert auf einen Wert < `rollout_distance_m`. Wir clampen:

```rust
let raw_used = td_dist + rollout;
let distance_used = raw_used.max(rollout); // niemals < rollout
```

### LE4 — Score-Bänder, exakt definiert (R2-P2-1 Fix: Float-Banding)

```rust
// 1. Raw-Ratio als Float
let raw_ratio_pct: f32 = (distance_used / LDA_m) * 100.0;

// 2. Overrun-Check ZUERST auf RAW (vor Allowance!) — R2-P0-2-Fix
if raw_ratio_pct > 100.0 {
    return (0, "overrun_risk", Band::Bad);
}

// 3. Heavy-Allowance auf Float
let allowance_pp: f32 = if is_heavy(aircraft_icao) { 5.0 } else { 0.0 };
let effective_pct: f32 = raw_ratio_pct - allowance_pp;

// 4. Banding auf Float — keine Rundung vorher!
let (points, rationale, band) = match effective_pct {
    e if e < 30.0  => (100, "excellent_margin", Band::Good),
    e if e < 50.0  => ( 80, "good_stop",        Band::Good),
    e if e < 70.0  => ( 55, "ok_stop",          Band::Ok),
    e if e < 90.0  => ( 25, "long_rollout",     Band::Bad),
    _              => (  5, "marginal_runway",  Band::Bad),  // 90 <= effective <= 100
};

// 5. Rundung NUR fürs Display
let display_pct = raw_ratio_pct.round() as i32;
```

**Konsequenz:**
- `29.5%` Raw + Heavy-Allowance → 24.5% effective → `< 30` → `excellent_margin` ✓ (R2-P2-1-Fix: keine Pre-Rundung mehr)
- `103%` Raw → trotz Heavy-Allowance → `overrun_risk` (raw > 100, vor Allowance gechecked) ✓ (R2-P0-2-Fix)
- `100.0%` exakt → fällt in 4. Branch (`marginal_runway`, 5 PTS) weil `100 > 100` false und nicht in den anderen Branches matched

**Display-Format** zeigt das gerundete `display_pct` (= UI-Konsistenz), aber Score wird auf Float entschieden.

### LE5 — Heavy-Allowance, **als Reduktion vor Banding**

(R2 LE5 unverändert, lediglich Formel-Reihenfolge präzisiert in LE4)

```
allowance_pp = 5.0  if is_heavy else 0.0
effective_pct = raw_ratio_pct - allowance_pp
```

Heavy-Set bleibt identisch zur heutigen `landing-scoring::category_for_icao`-Liste (Wide-Bodies + B747 + MD11 + IL96 + Antonov). Light/Medium ohne Allowance.

**Q-R2-2 Antwort:** Allowance bleibt bei **5 pp**, nicht 10. 10 wäre fast ein ganzes Band → zu gnädig.

### LE6 — Skip-Gate erweitert (R2-P0-1, P1-2 Fixes)

```rust
fn skip_reason(input: &RolloutInput) -> Option<&'static str> {
    if input.rollout_distance_m.is_none() {
        return Some("missing_rollout_distance");  // NEU R3
    }
    if input.td_distance_from_threshold_m.is_none() {
        return Some("missing_td_distance");       // NEU R3
    }
    if input.runway_length_m.is_none() {
        return Some("missing_length");
    }
    // R2-P1-2-Fix: None ist NICHT trusted
    if input.runway_geometry_trusted != Some(true) {
        return Some("untrusted_geometry");
    }
    if input.airport_source.as_deref() != Some("runway_match") {
        return Some("off_airport_landing");
    }
    // LE2 LDA-Sanity
    let displaced_m = input.runway_displaced_threshold_ft.unwrap_or(0) as f32 * 0.3048;
    let lda = input.runway_length_m.unwrap() - displaced_m;
    if lda <= 0.0 {
        return Some("invalid_lda");
    }
    None
}
```

5 Skip-Reasons im Wire-Format dokumentiert. Kein Meter-Backup-Fallback — lieber „kein Score" als irreführend (R2-Antwort 6).

### LE7 — SSoT: eine Compute-Stelle, zwei Renderer

(R2 LE7 unverändert, kurz wiederholt für Vollständigkeit)

```
Pilot-Client (Rust) → computed sub_scores → MQTT-Payload
                                              ↓
                                         Recorder (speichert)
                                              ↓
                              ┌───────────────┴───────────────┐
                              ▼                               ▼
                      Pilot-Client-React              Webapp
                      RENDERT NUR                     RENDERT NUR
                      (kein computeSubScores)         (kein computeSubScores)
```

`computeSubScores()` aus beiden TS-Files **entfernt** für `ux_version >= 1`-PIREPs. Legacy-Pfad für `ux_version < 1` darf bleiben (Q-R2-6: alte Pre-v0.7.1-PIREPs), wird aber **niemals** auf v0.10+ Records angewendet (Algorithm-Version-Gate, siehe LE11).

### LE8 — Cross-Sync per Schema-Snapshot-Test

Für v0.10.0 reicht ein **Snapshot-Test** auf dem `SubScoreEntry`-Wire-Format (Rust → JSON). Schema-Generator wird **NICHT** als CI-Pflicht versprochen — der landet in v0.11.0-Roadmap als optionale Verbesserung.

**R4-P2-Fix:** `insta` ist **nicht** im aktuellen Dep-Tree des Workspaces. Für v0.10.0 nutzen wir eine **Mini-Golden-JSON-Datei** + normaler `assert_eq!`:

```rust
// landing-scoring/tests/wire_schema_v2.rs
#[test]
fn subscoreentry_v2_wire_schema_unchanged() {
    let sample = sub_rollout_v2(&golden_input_ek406());
    let actual = serde_json::to_string_pretty(&sample).unwrap();
    let expected = std::fs::read_to_string(
        concat!(env!("CARGO_MANIFEST_DIR"), "/tests/fixtures/subscoreentry_v2_ek406.json")
    ).expect("golden fixture missing");
    assert_eq!(actual.trim(), expected.trim(),
               "Wire-Schema drifted vs golden fixture — beabsichtigt? Dann fixture aktualisieren.");
}
```

Update-Workflow bei beabsichtigter Schema-Änderung:
1. Test laufen lassen, sieht Diff
2. `cargo run --bin update-golden` (oder manuell) schreibt neuen JSON in fixture
3. Reviewer prüft Diff im Code-Review
4. v0.11.0-Optional: echter Schema-Generator (= TS-Types werden aus Rust generiert)

**`insta` kann später als `dev-dependency` ergänzt werden** wenn wir mehr Snapshot-Tests bekommen. Für v0.10.0 wäre's overkill.

### LE9 — UI-Transparenz

(R2 LE9 unverändert, gleiche Card-Layouts)

```
┌─ BAHN-AUSLASTUNG  i  ──────────────────── 80 PTS ─┐
│  1100 m / 3657 m  ·  30 %                         │
│  Solider Bremsweg                                 │
│  ▸ davon ~520 m Float vor Aufsetzen               │
│  ▸ Bahn: YMML 16, LDA 3657 m                      │
└───────────────────────────────────────────────────┘
```

Bei Skipped → „nicht bewertet" + Reason.
Bei `pre_displaced_threshold=true` → Score gecappt + Warning-Pill „⚠ Vor Displaced-Threshold aufgesetzt".

### LE10 — Migration: forward-only

(R2 LE10 unverändert)

- Alte PIREPs behalten ihre gespeicherten `sub_scores`
- Neue PIREPs ab v0.10.0 verwenden den neuen Algorithmus
- Kein Re-Score-Tool
- UI-Unterscheidung über `score_algorithm_version` (siehe LE11)

### LE11 — `score_algorithm_version` als hartes Wire-Field (R2-P1-3 Fix)

**R2-P1-3-Fix:** R2 sagte „über `ux_version` oder `score_algorithm_version`" — zu weich. Hart entschieden:

```rust
pub struct TouchdownPayload {
    // ... bestehende Felder ...

    /// v0.10.0 NEU: Algorithmus-Version des `sub_scores`-Arrays.
    /// - None / Some(1): pre-v0.10 Algorithmus (= heutiger meter-only-Score)
    /// - Some(2): v0.10 Algorithmus (= Runway-Utilization-Score dieser Spec)
    ///
    /// `ux_version` bleibt UI-Cutoff (= ob sub_scores überhaupt im Payload sind);
    /// `score_algorithm_version` ist Algorithmus-Cutoff (= wie sie berechnet wurden).
    #[serde(skip_serializing_if = "Option::is_none")]
    pub score_algorithm_version: Option<u8>,
}
```

UI-Rendering:
- `score_algorithm_version = Some(2)` → neuer Card-Layout (LE9)
- `score_algorithm_version = None/Some(1)` → alter Card-Layout (Backward-Compat, Pilot bekommt KEIN „falscher Score"-Indikator, weil die alten Werte nach altem Algo zumindest in sich konsistent waren)

Es gibt **keinen automatischen Re-Score** beim UI-Open — der gespeicherte Score bleibt unangetastet (LE10).

### LE12 — Wire-Format-Erweiterungen explizit (R2-P1-1 + R3-P1-1 Fix)

**R3-P1-Fix:** R3 spiegelte den `SubScoreEntry`-Struct falsch. **R4 jetzt 1:1 verifiziert** gegen `client/src-tauri/crates/landing-scoring/src/lib.rs:66-96`:

**SubScoreEntry-Wire-Struktur v1 (= heutiger Stand, **NICHT** verändern):**

```rust
#[derive(Serialize, Deserialize)]
pub struct SubScoreEntry {
    /// Stabile Schluessel z.B. "rollout"
    pub key: String,
    /// 0-100, gerundet
    pub score: u8,
    /// Alias fuer score — bestehende UI nutzt .points
    pub points: u8,
    /// "good" | "ok" | "bad" | "skipped"
    pub band: String,
    /// i18n-Key z.B. "landing.sub.rollout"
    pub label_key: String,
    /// formatiert: "583 m", neu in v0.10.0: "1100 m / 3657 m · 30 %"
    #[serde(skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// i18n-Key z.B. "landing.rat.excellent_margin"
    #[serde(skip_serializing_if = "Option::is_none")]
    pub rationale_key: Option<String>,
    /// i18n-Key z.B. "landing.tip.long_rollout"
    #[serde(skip_serializing_if = "Option::is_none")]
    pub tip_key: Option<String>,
    /// true wenn Sub-Score nicht bewertet wurde
    pub skipped: bool,
    /// Skip-Reason z.B. "missing_length"
    #[serde(skip_serializing_if = "Option::is_none")]
    pub reason: Option<String>,
    /// Warning-Hinweis z.B. "pre_displaced_threshold"  ← existiert SCHON in v1
    #[serde(skip_serializing_if = "Option::is_none")]
    pub warning: Option<String>,
}
```

**v0.10.0-Erweiterung (= EINZIGES NEUES Wire-Field am `SubScoreEntry`):**

```rust
    /// NEU v0.10.0: Zusatz-Display-Zeilen für UI
    /// (z.B. „davon ~520 m Float vor Aufsetzen", „Bahn: YMML 16, LDA 3657 m").
    /// Renderer alter Versionen ignorieren das Feld schweigend → forward-compat.
    #[serde(skip_serializing_if = "Vec::is_empty", default)]
    pub extra: Vec<String>,
```

**Wire-Format-Garantie:** Renderer alter Versionen die `extra` nicht kennen, müssen es schweigend ignorieren. Renderer v0.10+ die ein Payload ohne `extra` bekommen, müssen `Vec::new()` als Default annehmen.

**Naming-Klarheit:**
- `reason` (im Code) = `skip_reason` (in der Spec-Diskussion). Code-Name bleibt. i18n-Key-Pattern: `landing.skip.<reason_value>`.
- `score` UND `points` sind doppelt im Wire-Format (`points` ist Alias). UI nutzt `points`, neue Spec-Texte verwenden auch `points`.
- `band` ist `String`, nicht `Option<Band>`. Enum-Werte: `"good"`, `"ok"`, `"bad"`, `"skipped"`.
- `value: Option<String>` (skipped-Entries haben `value=None`).
- `label_key: String` (required). Für rollout ist's konstant `"landing.sub.rollout"`.
- `tip_key: Option<String>` existiert bereits. Coach-Tipps bleiben über separates i18n-Pattern.

**NICHT vorhanden (= R3-Fehler):**
- ~~`rationale_label`~~ existiert nicht im aktuellen Struct, wird auch in v0.10.0 nicht eingeführt. UI macht den Label-Lookup aus `rationale_key` selbst (mit `t(rationale_key)`).

---

## Datenmodell

### Wire-Format-Änderungen (R4-präzise)

| Wo | Feld | Type | Status |
|---|---|---|---|
| `TouchdownPayload` | `score_algorithm_version` | `Option<u8>` | **NEU** in v0.10.0 |
| `SubScoreEntry` | `extra` | `Vec<String>` (default empty) | **NEU** in v0.10.0 (einziges neues Feld am Struct) |
| `SubScoreEntry.value` | Format-Änderung | `Option<String>` | von `"583 m"` → `"1100 m / 3657 m · 30 %"` (gleicher Type, neuer Inhalt) |
| `SubScoreEntry.reason` | Werte erweitert | `Option<String>` | 5 neue Werte: `missing_rollout_distance`, `missing_td_distance`, `untrusted_geometry`, `off_airport_landing`, `invalid_lda` |
| `SubScoreEntry.warning` | Werte erweitert | `Option<String>` | NEU Wert: `pre_displaced_threshold` (Feld existiert bereits) |
| `SubScoreEntry.rationale_key` | Werte erweitert | `Option<String>` | 6 neue Werte: `landing.rat.excellent_margin`, `landing.rat.ok_stop`, `landing.rat.overrun_risk`, `landing.rat.pre_displaced_capped`, plus `landing.skip.<reason>`-Lookups für skipped |

**Schema-Snapshot-Test** (LE8) prüft: Rust-`serde_json::to_string()` eines Sample-SubScoreEntry matched ein Golden-JSON-File. Bricht bei Schema-Drift. (R4 Toolchain: siehe LE13.)

---

## Algorithmus (Rust-Implementierung, R3-Final)

```rust
pub struct RolloutInput<'a> {
    pub td_distance_from_threshold_m: Option<f64>,
    pub rollout_distance_m: Option<f32>,
    pub landing_float_distance_m: Option<f32>,
    pub runway_length_m: Option<f32>,
    pub runway_displaced_threshold_ft: Option<i32>,
    pub pre_displaced_threshold: Option<bool>,
    pub runway_geometry_trusted: Option<bool>,
    pub airport_source: Option<&'a str>,
    pub runway_match_icao: Option<&'a str>,
    pub runway_match_ident: Option<&'a str>,
    pub aircraft_icao: Option<&'a str>,
}

pub fn sub_rollout_v2(input: &RolloutInput) -> SubScoreEntry {
    // ── Skip-Gate (LE6) ──────────────────────────────────────────
    // R4-P1-Fix: SubScoreEntry::skipped hat 3 Args, nicht 2:
    //   pub fn skipped(key: &str, label_key: &str, reason: &str) -> Self
    if let Some(reason) = skip_reason(input) {
        return SubScoreEntry::skipped("rollout", "landing.sub.rollout", reason);
    }

    // Unwraps safe weil Skip-Gate alle None-Fälle gefiltert hat
    let td_dist  = input.td_distance_from_threshold_m.unwrap() as f32;
    let rollout  = input.rollout_distance_m.unwrap();
    let runway_m = input.runway_length_m.unwrap();
    let displaced_m = input.runway_displaced_threshold_ft.unwrap_or(0) as f32 * 0.3048;
    let lda_m = runway_m - displaced_m;

    // ── Numerator + Negativ-Schutz (LE1 + LE3 Halb-1) ────────────
    let raw_used = td_dist + rollout;
    let distance_used = raw_used.max(rollout); // niemals < rollout

    // ── Raw-Ratio als Float ──────────────────────────────────────
    let raw_ratio_pct: f32 = (distance_used / lda_m) * 100.0;

    // ── Overrun-Check ZUERST auf RAW (LE4 Step 2, R2-P0-2) ───────
    let (points, rationale, band) = if raw_ratio_pct > 100.0 {
        (0u8, "overrun_risk", Band::Bad)
    } else {
        // Heavy-Allowance auf Float (R2-P2-1: keine Pre-Rundung)
        let allowance_pp: f32 = if is_heavy(input.aircraft_icao) { 5.0 } else { 0.0 };
        let effective_pct: f32 = raw_ratio_pct - allowance_pp;

        match effective_pct {
            e if e < 30.0 => (100, "excellent_margin", Band::Good),
            e if e < 50.0 => ( 80, "good_stop",        Band::Good),
            e if e < 70.0 => ( 55, "ok_stop",          Band::Ok),
            e if e < 90.0 => ( 25, "long_rollout",     Band::Bad),
            _             => (  5, "marginal_runway",  Band::Bad),
        }
    };

    // ── pre_displaced-Cap (LE3 Halb-2, R2-P1-4) ──────────────────
    // R4-P1-Fix: bei Cap wird Rationale-Key auf "pre_displaced_capped"
    // ueberschrieben — sonst sieht UI "Viel Bahn-Reserve" bei 55 PTS,
    // ist unehrlich. Antwort R3-Q3: Override.
    let pre_displaced = input.pre_displaced_threshold.unwrap_or(false);
    let (capped_points, final_rationale, final_band) = if pre_displaced {
        let pts = points.min(55);
        // Band entsprechend der gecappten Punkte neu setzen
        let band = if pts >= 75 { Band::Good }
                   else if pts >= 45 { Band::Ok }
                   else { Band::Bad };
        (pts, "pre_displaced_capped", band)
    } else {
        (points, rationale, band)
    };

    let warning = if pre_displaced {
        Some("pre_displaced_threshold".to_string())  // i18n-Key fuer Warning-Pill
    } else { None };

    // ── Display-Strings (LE9) ────────────────────────────────────
    let display_pct = raw_ratio_pct.round() as i32;
    let value = format!("{} m / {} m  ·  {} %",
                        distance_used.round() as i32,
                        lda_m.round() as i32,
                        display_pct);

    let mut extra: Vec<String> = Vec::new();
    if let Some(float) = input.landing_float_distance_m {
        extra.push(format!("davon ~{} m Float vor Aufsetzen", float.round() as i32));
    }
    if let (Some(icao), Some(ident)) = (input.runway_match_icao, input.runway_match_ident) {
        extra.push(format!("Bahn: {} {}, LDA {} m", icao, ident, lda_m.round() as i32));
    }

    // ── Resultat via scored-Constructor (R4-P1-Fix) ──────────────
    // SubScoreEntry::scored(key, label_key, score, value, rationale, band)
    // - value ist String (nicht Option), wird im Constructor zu Some(...) gewrappt
    // - rationale ist &str, wird zu rationale_key="landing.rat.<rationale>"
    SubScoreEntry::scored(
        "rollout",
        "landing.sub.rollout",
        capped_points,
        value,
        final_rationale,
        final_band,
    )
    .with_extra(extra)        // NEU v0.10.0
    .with_warning(warning)    // existierendes Builder-Pattern auf v1
}
```

TypeScript-Render-Code in beiden TS-Files: **kein Compute**, nur Render aus `payload.sub_scores[].{value, points, rationale_key, band, extra, warning, skipped, reason}`.

---

## i18n (DE/EN/IT)

### Rationale-Keys (Score-Bänder)

| Key | DE | EN |
|---|---|---|
| `excellent_margin` | „Viel Bahn-Reserve" | „Plenty of margin" |
| `good_stop` | „Solider Bremsweg" | „Good stop" |
| `ok_stop` | „OK — sportlich" | „OK, sporty" |
| `long_rollout` | „Lang ausgerollt" | „Long rollout" |
| `marginal_runway` | „⚠ Knapp — fast Overrun" | „⚠ Marginal — close to overrun" |
| `overrun_risk` (NEU) | „⚠ Über Bahnende — Overrun-Risiko" | „⚠ Beyond runway end — overrun risk" |
| `pre_displaced_capped` (NEU, R4-P1-3) | „Bremsweg ok, aber zu früh aufgesetzt" | „Stopping ok, but touched down too early" |

### Skip-Reason-Keys

| Key | DE | EN |
|---|---|---|
| `landing.skip.missing_length` | „Bahnlänge nicht verfügbar" | „Runway length unavailable" |
| `landing.skip.missing_rollout_distance` | „Rollout-Distanz nicht erfasst" | „Rollout distance not captured" |
| `landing.skip.missing_td_distance` | „TD-Distanz zur Schwelle nicht erfasst" | „TD distance to threshold not captured" |
| `landing.skip.untrusted_geometry` | „Bahn-Geometrie nicht vertrauenswürdig" | „Runway geometry not trusted" |
| `landing.skip.off_airport_landing` | „Off-Airport-Landung" | „Off-airport landing" |
| `landing.skip.invalid_lda` | „LDA-Daten inkonsistent" | „LDA data inconsistent" |

### Warning-Keys

| Key | DE | EN |
|---|---|---|
| `landing.warn.pre_displaced_threshold` | „⚠ Vor Displaced-Threshold aufgesetzt — Anflugprofil prüfen" | „⚠ Touched down before displaced threshold — review approach" |

---

## Tests

### Rust-Crate-Tests (`landing-scoring/tests/sub_rollout_v2.rs`)

```rust
use landing_scoring::{sub_rollout_v2, RolloutInput, Band, SubScoreEntry};

// Helper für Standard-Input
fn ok_input(td: f64, rollout: f32, runway: f32, displaced_ft: i32, icao: &str) -> RolloutInput {
    RolloutInput {
        td_distance_from_threshold_m: Some(td),
        rollout_distance_m: Some(rollout),
        landing_float_distance_m: Some((td as f32).max(0.0)),
        runway_length_m: Some(runway),
        runway_displaced_threshold_ft: Some(displaced_ft),
        pre_displaced_threshold: Some(false),
        runway_geometry_trusted: Some(true),
        airport_source: Some("runway_match"),
        runway_match_icao: Some("XXXX"),
        runway_match_ident: Some("00"),
        aircraft_icao: Some(icao),
    }
}

#[test]
fn ek406_a380_real_case_excellent() {
    // EK406 reale Werte (verifiziert in der Recorder-DB für Touchdown id=225)
    let r = sub_rollout_v2(&ok_input(516.93, 583.55, 3657.0, 0, "A388"));
    // distance_used = (516.93 + 583.55).max(583.55) = 1100.48 m
    // raw_ratio = 1100.48 / 3657 = 30.09 %
    // Heavy-Allowance -5 → effective 25.09 %
    // 25.09 < 30 → excellent_margin
    assert_eq!(r.points, 100);
    assert_eq!(r.rationale_key.as_deref(), Some("landing.rat.excellent_margin"));
    assert!(r.value.contains("30 %"));   // display-rounded
    assert!(!r.skipped);
    assert!(r.warning.is_none());
}

#[test]
fn a380_short_runway_overrun_risk() {
    // Raw 108 % → overrun_risk (vor Allowance gechecked, R2-P0-2)
    let r = sub_rollout_v2(&ok_input(500.0, 2200.0, 2500.0, 0, "A388"));
    // (500+2200)/2500 = 108 % → overrun_risk
    assert_eq!(r.points, 0);
    assert_eq!(r.rationale_key.as_deref(), Some("landing.rat.overrun_risk"));
}

#[test]
fn eddf_25c_displaced_threshold_ok_stop() {
    // 4000 m physisch, 1968 ft displaced (≈600 m), LDA ≈ 3400 m
    // TD 800 m past threshold + 1500 m Rollout = 2300 m used
    // Raw ratio = 2300/3400 = 67.6 %; A320 = Medium, keine Allowance
    let r = sub_rollout_v2(&ok_input(800.0, 1500.0, 4000.0, 1968, "A320"));
    // 67.6 < 70 → ok_stop
    assert_eq!(r.points, 55);
    assert_eq!(r.rationale_key.as_deref(), Some("landing.rat.ok_stop"));
}

#[test]
fn pre_displaced_caps_at_55_pts() {
    // R2-P1-4: pre_displaced cap auf 55
    let mut input = ok_input(516.93, 583.55, 3657.0, 0, "A388");
    input.pre_displaced_threshold = Some(true);
    let r = sub_rollout_v2(&input);
    // Sonst wäre excellent_margin (100); mit Cap → 55
    assert_eq!(r.points, 55);
    assert_eq!(r.warning.as_deref(), Some("pre_displaced_threshold"));
    // R4-Fix: Rationale-Override auf pre_displaced_capped (NICHT excellent_margin),
    // sonst zeigt der Pilot "Viel Bahn-Reserve" obwohl 55 PTS = unehrlich.
    assert_eq!(
        r.rationale_key.as_deref(),
        Some("landing.rat.pre_displaced_capped")
    );
}

#[test]
fn no_pre_rounding_at_band_boundary() {
    // R2-P2-1: 29.5% raw, Light (keine Allowance), Float-Banding
    // Wenn vorher gerundet wäre: 30 → good_stop (80 PTS)
    // Mit Float-Banding: 29.5 < 30.0 → excellent_margin (100 PTS)
    let input = ok_input(295.0, 0.0, 1000.0, 0, "C172");
    let r = sub_rollout_v2(&input);
    assert_eq!(r.points, 100);
    assert_eq!(r.rationale_key.as_deref(), Some("landing.rat.excellent_margin"));
}

#[test]
fn skip_missing_td_distance() {
    let mut input = ok_input(0.0, 583.55, 3657.0, 0, "A388");
    input.td_distance_from_threshold_m = None;
    let r = sub_rollout_v2(&input);
    assert!(r.skipped);
    assert_eq!(r.reason.as_deref(), Some("missing_td_distance"));
}

#[test]
fn skip_missing_rollout() {
    let mut input = ok_input(516.93, 0.0, 3657.0, 0, "A388");
    input.rollout_distance_m = None;
    let r = sub_rollout_v2(&input);
    assert!(r.skipped);
    assert_eq!(r.reason.as_deref(), Some("missing_rollout_distance"));
}

#[test]
fn skip_runway_geometry_trusted_none() {
    // R2-P1-2: None ist NICHT trusted
    let mut input = ok_input(516.93, 583.55, 3657.0, 0, "A388");
    input.runway_geometry_trusted = None;
    let r = sub_rollout_v2(&input);
    assert!(r.skipped);
    assert_eq!(r.reason.as_deref(), Some("untrusted_geometry"));
}

#[test]
fn skip_runway_geometry_trusted_false() {
    let mut input = ok_input(516.93, 583.55, 3657.0, 0, "A388");
    input.runway_geometry_trusted = Some(false);
    let r = sub_rollout_v2(&input);
    assert!(r.skipped);
    assert_eq!(r.reason.as_deref(), Some("untrusted_geometry"));
}

#[test]
fn skip_off_airport_landing() {
    let mut input = ok_input(516.93, 583.55, 3657.0, 0, "A388");
    input.airport_source = Some("nearest_25nm");
    let r = sub_rollout_v2(&input);
    assert!(r.skipped);
    assert_eq!(r.reason.as_deref(), Some("off_airport_landing"));
}

#[test]
fn heavy_allowance_5pp_at_band_boundary() {
    // 35 % Raw + Heavy-Allowance → 30 % effective → boundary
    // Light wäre bei 35 % → good_stop (80 PTS)
    // Heavy bei 35 % → effective 30 → wir sind GENAU AN der Grenze
    // 30 < 30 false → fällt in `e if e < 50` → good_stop (80 PTS)
    // Heavy mit 34 % → effective 29 < 30 → excellent_margin (100 PTS)
    let r_heavy_35 = sub_rollout_v2(&ok_input(350.0, 0.0, 1000.0, 0, "A388"));
    assert_eq!(r_heavy_35.points, 80, "Heavy bei 35% raw → 30% effective → good_stop");
    let r_heavy_34 = sub_rollout_v2(&ok_input(340.0, 0.0, 1000.0, 0, "A388"));
    assert_eq!(r_heavy_34.points, 100, "Heavy bei 34% raw → 29% effective → excellent");
}

#[test]
fn medium_no_allowance() {
    // A320 50 % raw → 50 % effective → ok_stop (55 PTS)
    let r = sub_rollout_v2(&ok_input(500.0, 0.0, 1000.0, 0, "A320"));
    assert_eq!(r.points, 55);
}

#[test]
fn cessna_grass_strip_long_rollout() {
    // 350m rollout / 500m → 70% → long_rollout
    let r = sub_rollout_v2(&ok_input(0.0, 350.0, 500.0, 0, "C172"));
    assert_eq!(r.points, 25);
    assert_eq!(r.rationale_key.as_deref(), Some("landing.rat.long_rollout"));
}

#[test]
fn negative_td_distance_clamped_to_rollout_only() {
    // Pre-displaced + neg TD-Distance
    let mut input = ok_input(-50.0, 800.0, 1000.0, 0, "A320");
    input.pre_displaced_threshold = Some(true);
    // raw_used = -50 + 800 = 750; max(800) = 800 → ratio = 80 %
    // Light/Medium ohne Allowance → 80 % → long_rollout (25 PTS)
    // pre_displaced cap auf 55 → bleibt 25 (Cap nur min(55))
    let r = sub_rollout_v2(&input);
    assert_eq!(r.points, 25);
    assert!(r.warning.is_some());
}

#[test]
fn wire_schema_matches_golden_fixture() {
    // R4 LE8: Mini-Golden-JSON statt insta (insta NICHT im Workspace-Dep-Tree).
    // Bei beabsichtigter Schema-Änderung: Test laufen, Diff sehen, fixture-File
    // aktualisieren, Reviewer prüft Diff im Code-Review.
    let sample = sub_rollout_v2(&ok_input(516.93, 583.55, 3657.0, 0, "A388"));
    let actual = serde_json::to_string_pretty(&sample).unwrap();
    let expected = std::fs::read_to_string(concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/fixtures/subscoreentry_v2_ek406.json"
    ))
    .expect("golden fixture missing — siehe LE8");
    assert_eq!(
        actual.trim(),
        expected.trim(),
        "Wire-Schema drifted vs golden fixture — beabsichtigt? Dann fixture aktualisieren."
    );
}
```

### TypeScript-Render-Tests (vitest)

Keine Compute-Tests mehr. Stattdessen:
```typescript
it("renders new value-format with m/m/% triple", () => { /* render value="1100 m / 3657 m · 30 %" */ });
it("renders skipped card with i18n reason", () => { /* … */ });
it("renders warning pill for pre_displaced", () => { /* … */ });
it("renders extra lines below rationale", () => { /* … */ });
it("renders overrun_risk in bad band", () => { /* … */ });
it("legacy computeSubScores still works for ux_version=0 PIREPs", () => {
    // R2-Antwort 6: alte Pre-v0.7.1-PIREPs dürfen Legacy-Pfad
});
```

---

## UI-Aufwand pro Surface (R4-P2-Fix: präzise Repo-Pfade)

| Repo | Datei | Änderung |
|---|---|---|
| **AeroACARS** (Pilot-Client) | `client/src-tauri/crates/landing-scoring/src/sub_rollout.rs` | Neue `sub_rollout_v2()`-Function (alte `sub_rollout` bleibt für Backward-Compat-Tests). Heavy/Medium/Light-ICAO-Sets aus alter Function übernommen. |
| **AeroACARS** | `client/src-tauri/crates/landing-scoring/src/lib.rs` | `SubScoreEntry`-Struct um `extra: Vec<String>` erweitern + Builder-Method `with_extra(Vec<String>)`. `RolloutInput`-Struct um neue Felder erweitern. |
| **AeroACARS** | `client/src-tauri/crates/aeroacars-mqtt/src/lib.rs` | `TouchdownPayload` um `score_algorithm_version: Option<u8>` erweitern |
| **AeroACARS** | `client/src-tauri/src/lib.rs` | `score_algorithm_version: Some(2)` setzen im PIREP/Touchdown-Build-Pfad, `RolloutInput` mit neuen Feldern befüllen |
| **AeroACARS** | `client/src/components/LandingPanel.tsx` | Card-Layout-Erweiterung (extra-Lines + warning-Pill), `computeSubScores`-Aufruf für `ux_version >= 1` entfernen |
| **AeroACARS** | `client/src/lib/landingScoring.ts` | `computeSubScores`-Function für `ux_version >= 1`-Pfad **entfernen**. Legacy-Pfad für `ux_version < 1` bleibt. (Antwort R3-Q4: kein v0.11-Removal-Marker.) |
| **AeroACARS** | `client/src/locales/{de,en,it}/common.json` | 13 neue Keys (6 Skip-Reasons + 1 Warning + 6 Rationales) |
| **AeroACARS** | `client/src-tauri/crates/landing-scoring/tests/sub_rollout_v2.rs` | Neue Tests (siehe Test-Section) |
| **AeroACARS** | `client/src-tauri/crates/landing-scoring/tests/fixtures/subscoreentry_v2_ek406.json` | Golden-JSON-File für Schema-Snapshot-Test |
| **aeroacars-live** (Webapp) | `webapp/src/components/LandingAnalysis.tsx` | gleiche Card-Layout-Erweiterung |
| **aeroacars-live** | `webapp/src/components/landingScoring.ts` | `computeSubScores`-Function für `ux_version >= 1`-Pfad entfernen |
| **aeroacars-live** | `recorder/src/db.ts` | Optional: neue Migration für `score_algorithm_version`-Column? Falls Recorder die Wire-Field-Roundtrip schon transparent ueber JSON-Blob abdeckt, **NICHT nötig**. Code-Verifikation pending. |

**Recorder:** Speichert das Touchdown-Payload als-is. `score_algorithm_version` ist im JSON-Blob → kein Schema-Change nötig. UI-Endpoints reichen das Feld durch.

**Keine Änderung in:**
- `webapp/src/components/_LandingQualityCard.tsx` (Liste-Card ohne Sub-Score-Detail)
- `webapp/src/tabs/PirepFeed.tsx` (rendert Aggregate, nicht Sub-Scores)
- Recorder-API-Surface (`server.ts`) — neue Felder gehen automatisch im JSON durch

---

## Aufwand-Schätzung

| Task | Schätzung |
|---|---|
| Spec QS bis ACCEPTED | 0-2 weitere Runden á 30 min |
| Rust-Crate sub_rollout_v2 + 14 Tests + Snapshot | 4 h |
| Wire-Schema-Erweiterung (`score_algorithm_version` + `extra`; `warning` existiert schon, bekommt nur neuen Wert `pre_displaced_threshold`) | 1 h |
| TypeScript-Render-Code beide Surfaces (compute weg, render erweitern) | 3 h |
| i18n DE/EN/IT (13 neue Keys) | 30 min |
| UI-Card-Erweiterung | 1 h |
| QS-Build + manuelle Verifikation | 1 h |
| CHANGELOG + Release-Notes | 30 min |
| **Gesamt** | **~11-13 h Code + Spec-QS** |

---

## Antworten R3-Open-Questions

R3 hatte 5 offene Fragen, Thomas-R3-Antworten alle eingearbeitet:

1. ✅ `reason` (= Code-Name), nicht `skip_reason`
2. ✅ `warning` existiert bereits im Wire-Format (verifiziert in `landing-scoring/src/lib.rs:92-95`); R4 sagt nicht mehr „NEU"
3. ✅ Rationale-Override auf `pre_displaced_capped` bei Cap (ehrlicher als „excellent_margin + 55 PTS")
4. ✅ Legacy-`computeSubScores` für `ux_version < 1` bleibt drinnen ohne v0.11-Removal-Marker
5. ✅ `insta` raus, Mini-Golden-JSON-File reicht

---

## Antworten R4-Open-Questions (Thomas-R4)

1. ✅ `with_extra(Vec<String>)`-Builder okay. Falls `with_warning(...)` schon existiert → benutzen; sonst analog ergänzen oder direkt mutieren (Implementer-Entscheidung beim Implementieren).
2. ✅ Recorder-DB-Migration: vor Code-Touch kurz im Recorder verifizieren. Wenn JSON-Blob transparent → keine Migration.
3. ✅ Wording „Bremsweg ok, aber zu früh aufgesetzt" passt — kurz, klar, nicht belehrend.

---

## Status

**R5 wartet auf Thomas-Review.** R4-Spec-Drifts in der Test-Section behoben; fachliche Score-Logik unverändert seit R3.
- Bei „SPEC ACCEPTED" → Code-Implementation startet (siehe Aufwand-Tabelle)
- Bei Restpunkten → R6 (sollte minimal sein)

**Kein Code-Touch bis SPEC ACCEPTED.**
