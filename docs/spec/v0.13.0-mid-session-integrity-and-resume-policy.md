# v0.13.0 — Mid-Session-Integrity-Validator & Resume-Policy

**Status:** SPEC R13 — wartet auf QS-Runde 14 oder direkt ACCEPTED (Thomas Kant).
**Autor:** v0.13.0-Session-Owner.
**QS-Reviewer:** Thomas Kant (VA-Owner GSG).
**Trigger:** Forensik des PIREP **WZZ2271** (24.05.2026, LHBP→EHEH, A321 HA-LVA, X-Plane) hat strukturelle Lücken im aktuellen Integrity-Modell offengelegt. Der Flug bekam ein „Greaser 100/100" trotz vier Sim-State-Brüchen, einer Teleport-Reposition zum Zielflughafen mitten im Descent, impossibler Fuel- und Weight-Bilanz und Touchdown bei `GS = 0` mit gesetzter Parkbremse. Drei systemische Probleme wurden identifiziert:

1. **Detektions-Lücke:** v0.12.1 LE14 ist als reine UX-Maßnahme am App-Restart-Resume designt. Mid-Session-Glitches bei laufender App und Glitches an den Edges von Telemetrie-Lücken fallen komplett durch.
2. **Downstream-Lücke:** Phase-FSM und Landing-Scorer konsumieren Snapshots ungebremst — auch suspecte. Ein Touchdown-Sample mit GS = 0 und Parking-Brake true erreicht ungefiltert den Scorer.
3. **Recovery-Lücke:** Resume ist binär (auto-confirm oder einmal-klicken). Es gibt keine Differenzierung zwischen „App-Crash nach 30 s" und „Pilot kommt nach 4 h zurück mit drift Sim-State". Beides läuft auf denselben 10-s-Banner hinaus.

Diese Spec adressiert alle drei Lücken in einem konsistenten Modell mit **sieben Streams** (A–G) plus Sub-Stream C-Sub (Ingestion-Serialisierung), einer **drei-stufigen Severity-Taxonomie** und einer **Cross-Surface-Matrix** die Recorder, Client und Web-Monitor synchron hält. Plus phpVMS-Integrity-Gate-Module (Stream G) für die Wahrheits-Synchronisation.

---

## Changelog dieser Spec

- **R0:** initialer Draft. Sechs Streams: A Physik-Validator · B Gap-Handler & Data-Quality · C Downstream-Schutz · D Persistence & Surfacing · E (entfällt — Backfill verworfen per Entscheidung A3) · F Resume-Policy. Entscheidungen A1–A11 aus der Vorbereitungs-Diskussion eingearbeitet (siehe Abschnitt „Übernommene Entscheidungen" unten).
- **R1:** QS-R0-Findings von Thomas eingearbeitet:
  - **P1 — Severity-Aggregation** (`base_severity` vs. `effective_severity`) als globaler Mechanismus eingeführt; Combo-Eskalationen explizit (siehe „Globales: Severity-Modell").
  - **P1 — Stream A Validator-Modi** (Continuous-Mode + Gap-Edge-Mode) als normative Sektion eingefügt — Stream-A-Checks laufen jetzt explizit auch an Gap-Edges, nicht nur Sample-zu-Sample.
  - **P1 — Normative Recorder-Ingestion-Pipeline** (Sektion C.1) als verbindliche 9-Schritt-Sequenz spezifiziert — Reihenfolge Validator → Quarantäne → FSM → Scorer → Finalizer ist Test-Target.
  - **P1 — Score-Trust gehärtet (LE15):** zusätzliche Hard-Triggers (`indicated_airspeed_kt < 30`, `quarantine_status != clean` am Touchdown-Sample, CRITICAL-Flag im [TD−60s; TD+30s]-Fenster, `landing_score_trusted=false` als Alias).
  - **P1 — Neue LE16b: BlocksOn/TaxiIn/Arrived-Transitions dürfen nicht aus quarantänisiertem Sample entstehen.** Fängt den WZZ2271-Hauptfehler (BlocksOn aus Teleport-Sample) direkt ab.
  - **P2 — Backfill (Stream E) revidiert:** A3-Entscheidung in QS revidiert; Backfill kommt rein, aber strikt als Dry-Run-Job mit Admin-Approval, keine stillen Statusänderungen. LE27 + LE28.
  - **P2 — Clean-Shutdown-Bonus eingegrenzt:** Bonus wirkt nur auf Gap-Duration (×6) und Position-Drift (×2), NICHT auf Altitude, Fuel, Air/Ground-Match oder aircraft_icao. Stream-A-Physik-Checks sind explizit unabhängig vom Bonus.
  - **P2 — Aircraft-Identity-Klassifikation verfeinert (LE12):** explizite `AIRCRAFT_FAMILIES`-Tabelle, In-Familie-Wechsel = INFO, Cross-Family = ANOMALY, Cross-Manufacturer/Size = CRITICAL.
  - **P2 — Cross-Surface-Matrix:** 5. Spalte „Admin-Action" hinzugefügt (acknowledge/accept-with-warning/reject/dismiss-FP/attach-note).
  - **P3 — Aufwand-Schätzung neu kalibriert mit Phase-Split:** Initialer Phase-Split-Vorschlag (~71 h + ~44 h = 120 h). *(Phase-Split wurde in späterer Runde verworfen — alles in zusammenhängendem v0.13.0-Release, kein Folge-Release vorgesehen.)*
  - **Neu — Referenz-Akzeptanz-Sektion „WZZ2271":** explizite E2E-Erwartungen für die ACCEPTED-Schwelle.
- **R2:** QS-R1-Findings von Thomas eingearbeitet:
  - **P1 — Live-Severity-Aggregation:** Aggregations-Pass läuft jetzt session-live nach jeder Flag-Ingestion (nicht erst beim PIREP-Finalize). FSM/Scorer/MQTT/Client arbeiten mit `effective_severity` ab dem Moment der Eskalation, nicht erst rückwirkend.
  - **P1 — Gap-Edge-Plausibilitäts-Envelope:** A.1 ersetzt punkt-genaue „Expected"-Werte durch Plausibilitäts-Envelopes (reachable_range, max_VS_envelope, fuel_burn_range, **phase_continuity**). WZZ2271-Insight: 31 NM in 351 s ist innerhalb des Position-Envelope, aber `airborne descent → on_ground parked + fuel reset` verletzt die Phase-Continuity hart.
  - **P1 — Safe Session Termination (LE16c):** neuer LE explizit für den Fall „Arrival-Transition wird wiederholt geblockt". Session kann nach M Reject-Versuchen oder N Minuten ohne Progress als partial-PIREP gefiled werden (Landing/BlocksOn ungescored, requires_review). Verhindert Orphan-Sessions.
  - **P2 — Backfill `reproducibility_pin`:** Apply-Run prüft zusätzlich zu TTL: validator_version, ruleset_hash, dataset_hash, dataset_count, operator, dry_run_summary_checksum. Verhindert Apply auf veränderter Daten- oder Code-Basis.
  - **P2 — Aircraft-Families data-driven:** Mapping ausgelagert in versioniertes JSON (`aircraft_families.json` mit `version: 1`). Unknown-ICAO ist immer Data-Quality (`AIRCRAFT_TYPE_UNKNOWN`, ANOMALY), nie CRITICAL. CRITICAL nur bei Family/Manufacturer-Shift + Operational-Konflikt (airborne, oder Weight/Fuel-Profilbruch).
  - **P2 — Clean-Shutdown-Bonus strikt UX-only:** Bonus wirkt ausschließlich auf Stream-F Tier-Klassifikation (Client-Resume-Dialog). Recorder-Integrity-Validierung (Stream A/B) ignoriert `clean_shutdown_reason` komplett — Recorder bewertet jeden Flug identisch, egal ob Update oder App-Crash.
  - **P2 — Admin-Audit-State-Maschine (LE21b):** explizite State-Machine `open → acknowledged → {accepted_with_warning, rejected, dismissed_false_positive}` mit Pflichtfeldern (actor, ts, reason für reject/dismiss) und vollständigem Audit-Log. Macht Review-Queue revisionssicher.
  - **P3 — Negative Controls** in der WZZ2271-Akzeptanz-Sektion: zwei zusätzliche Pflicht-Tests (legitimer App-Restart mit Pause ohne Teleport, legitimes Boarding/Refuel mit Fuel-Anstieg). Verhindert Overfitting auf WZZ2271.
  - **Scope-Klarstellung:** kein Phase-Split. v0.13.0 wird als zusammenhängendes Release umgesetzt — Resume-Policy, Review-Workflow und Backfill sind alle in scope für v0.13.0. Phase-1/2-Split in der Aufwand-Schätzung entfernt, ersetzt durch einheitliche ~110–130 h-Gesamt-Schätzung.
- **R3:** QS-R2-Findings von Thomas eingearbeitet:
  - **P1 — Recompute-Mechanismus bei Live-Eskalation:** R2's „keine Retro-Wirkung" war falsche Vereinfachung. R3 macht es korrekt: nachträgliche Eskalation markiert das **Affected-Segment** (von first-flag bis current) als `requires_recompute`, ein Background-Pass läuft re-validation über die FSM und re-evaluiert Scorer-Entscheidungen. Sonst bleiben kontaminierte Zustände stehen.
  - **P1 — Per-Flight-Ingestion-Serialisierung:** neuer Abschnitt „Ingestion-Sequencing" mit normativer per-`session_id`-Queue, Idempotency-Key, monotone sequence/ts checks, Buffering bei out-of-order arrival.
  - **P1 — Post-Resume-Status-Pill im Client:** wenn SEAMLESS-Resume durchläuft, der Recorder aber Stream-A/B-Flags wirft → Client zeigt persistente Status-Pill „Flug fortgesetzt — Datenlücke wird geprüft". Verhindert UX-Widerspruch zwischen Client-Komfort und Recorder-Realität.
  - **P1 — LE16c Partial-PIREP Statistik-Sperre:** partial-/safe-terminated PIREPs sind aus ALLEN Aggregaten (Bestenliste, Route-Stats, Fuel-Stats, Completion, Pilot-Stats) ausgeschlossen, bis Admin entscheidet. Sonst zählt ein kaputter Flug halb mit.
  - **P2 — Negative Controls Wording:** „NICHT geflagt" → präzise: „keine review-impacting flags" (kein effective-CRITICAL, kein requires_review, kein score_trust untrusted, keine Quarantäne). INFO/ANOMALY-base ist OK.
  - **P2 — Admin-State-Machine `reopen`:** jedes terminale State kann via `reopen`-Transition zurück zu `open` (mit Pflicht-Reason + Audit-Trail). Korrigiert Fehl-Entscheidungen ohne SQL-Eingriff.
  - **P2 — Ruleset-/Aircraft-Families-Version pro Flag:** jeder `integrity_flag` persistiert `ruleset_version` + `aircraft_families_version`. Spätere Mapping-/Schwellen-Updates ändern historische Interpretation nicht.
  - **P2 — Backfill-Apply Drift-Sperre verschärft:** `reproducibility_pin` enthält jetzt vollständige `pirep_ids[]`-Liste (kanonisch sortiert + gehashed) + per-source-JSONL-Content-Hash. Apply rejected bei jedem Drift in der Zielmenge, nicht nur bei Count.
  - **P2 — Phase-Continuity Go-Around/Missed-Approach/Bounce:** Stream-A LE2 Phase-Continuity-Tabelle erlaubt explizit Final→Climb, Approach→Climb, Landing→TakeoffRoll/Climb — aber **nur unter Operating-State-Check** (engines_running, !parking_brake, GS > 30 kt). Verhindert false-positive bei legit Bounces, blockiert Teleport-zum-Boden-und-zurück.
  - **P3 — Aufwand revidiert nach oben:** R2 ~122 h war zu sportlich gegeben Recompute-Mechanismus, Serialisierung, Reopen, Ruleset-Versioning, zusätzliche Fixtures. R3 ehrliche Neuschätzung ~150 h.
- **R4:** QS-R3-Findings (auf GitHub-Version mit Zeilenreferenzen) von Thomas eingearbeitet:
  - **P1 — Aggregations-Widerspruch beseitigt:** alte Lifecycle-Stelle ("einmal pro PIREP beim Finalize-Pass") + C.1-Pipeline-Step-12 widersprachen R3's Live-Aggregation. R4 konsolidiert: Aggregation läuft live in jedem Snapshot-Tick (vor FSM/Scorer), Finalize macht nur noch finale Konsolidierung + Persistenz von computed fields. Inkonsistenz war Folge der R1→R2→R3-Inkremente.
  - **P1 — Datenmodell für Ingestion + Recompute nachgezogen:** neue Tabellen `flight_session_checkpoints`, `recompute_log`, `ingestion_replay_cache` ergänzt; zusätzliche Spalten `last_acknowledged_sequence_number`, `last_recompute_at` auf `flight_sessions`. Sequence-Number-Feld auf Snapshot-Schema dokumentiert.
  - **P1 — Partial-PIREP-Filter review-state-aware:** R3-Filter `pireps.status != 'partial'` schloss auch nach `accepted_with_warning` aus. R4 korrigiert: Filter ist `(status != 'partial' OR review_state == 'accepted_with_warning')`, damit Admin-Entscheidung "akzeptiert" tatsächlich greift.
  - **P2 — Status-Header + A3 nachgezogen:** Header `SPEC R0` → `SPEC R4`. A3-Entscheidung markiert als revidiert in R1 (Backfill ist drin, Stream E).
  - **P2 — API `target_state` enum + `reopen`:** LE21b State-Maschine erlaubte `reopen` als Transition, API-Body listete sie nicht. R4 ergänzt im `target_state`-Enum.
  - **P2 — Cross-Surface-Matrix Aircraft-Identity konsistent:** Matrix listete MANUFACTURER_SHIFT + SIZE_CLASS_SHIFT als CRITICAL, LE12 R2 hatte beide auf ANOMALY-base herabgestuft (mit Operational-Conflict-Eskalation). R4 sync't Matrix.
  - **P2 — v0.13.1-/„später"-Reste entfernt:** REPOSITION-Cancel-Pfad (Stream-D-Erweiterung später) jetzt explizit in v0.13.0; Q1–Q10 von „v0.13.1/Backlog"-Formulierungen befreit (alles innerhalb v0.13.0 oder klar als Out-of-Scope-Edge-Case).
- **R5:** **Wahrheits-Lücke geschlossen.** Thomas-Befund: Die Review-Queue auf dem VPS war unverbunden mit phpVMS — VA-Admin sah dort „WZZ2271 ist kaputt", aber auf der GSG-Website (`german-sky-group.eu`) lief der PIREP weiter mit Greaser 100/100 in der Bestenliste. Zwei Wahrheiten, beide lebten getrennt.
  - **phpVMS-API-Limitation verifiziert** im phpvms/dev-Source: `PirepController::prefile()` und `::file()` nehmen `$user = Auth::user()` direkt — **keine Service-Account-Impersonation möglich**. `PirepController::checkReadOnly()` blockt jeden Update nach ACCEPTED (bereits in unserem Code-Kommentar `lib.rs:10957` vom 2026-05-04 dokumentiert).
  - **Neuer Stream G: phpVMS-Truth-Sync via VPS-Filing** mit Variante 1 (Pilot-API-Key auf VPS). Client filed PIREPs **nicht mehr direkt** an phpVMS, sondern über den VPS-Recorder als Gatekeeper. VPS authentifiziert sich als der jeweilige Pilot (mit dessen verschlüsselt gespeichertem phpVMS-Key) und filed erst nach Validierung. PIREPs mit review-impacting Flags landen in held_queue, gehen erst nach Admin-Entscheidung an phpVMS (oder werden bei `rejected` gar nicht gefiled).
  - **9 neue LEs (LE29–LE37):** Client-Refactor, MQTT pirep_draft, VPS PirepFilingService, Encrypted-Key-Storage, Retry-Queue, Client-UX, Local-Draft-Fallback, Migration-Pfad für alte Clients.
  - **3 neue DB-Tabellen:** `pilot_phpvms_keys` (encrypted), `pirep_held_queue`, `pirep_filing_audit`.
  - **Sicherheits-Sektion:** Threat-Model + Mitigationen für VPS-side Pilot-Key-Storage (AES-256-GCM at rest, in-memory-only decryption, audit-log, Pilot-Revoke-Path).
  - **Aufwand-Schätzung:** R4 ~162 h + Stream G ~40 h = **~200 h gesamt** (range 190–220 h, 8–10 Wochen).
- **R6:** **Stream G fundamental pivotiert** auf DisposableBasic-Pattern (phpVMS-Module-Listener statt VPS-Filing). Thomas-Befund + Recherche an `FatihKoz/DisposableBasic` zeigten: phpVMS hat eingebaute Event-Hooks (`PirepFiled`, `PirepPrefiled`, ...) die Modules nutzen können um PIREP-State direkt zu manipulieren — `$pirep->state = REJECTED; $pirep->save()` umgeht den `checkReadOnly()`-API-Gate. R5's VPS-Filing-Architektur ist damit überengineered.
  - **3 phpVMS-Core-Fakten verifiziert** im phpvms/dev-Source: (1) `PirepFiled` feuert BEFORE auto_approve_acars (`PirepService.php` Line 326 vs Line 341), (2) PirepState-Enum hat PENDING=1, ACCEPTED=2, REJECTED=6, (3) Event-Payload ist `public Pirep $pirep` — direkter Model-Zugriff.
  - **Stream G komplett umgeschrieben:** eigenes phpVMS-Module `AeroACARSIntegrityGate` mit `PirepFiled`-Listener, der per HTTP an VPS-Recorder ein Integrity-Verdict holt und direkt am Eloquent-Model den State setzt. Kein VPS-Filing-Service, kein Pilot-Key-Storage auf VPS, kein Client-Refactor, keine Migration.
  - **Sicherheits-Surface drastisch reduziert:** nur ein Shared-Secret zwischen phpVMS-Module und VPS-Recorder (statt aller Pilot-API-Keys auf VPS). Pilot-Keys bleiben am Client wo sie hingehören.
  - **2 DB-Tabellen entfernt, 1 vereinfacht:** `pilot_phpvms_keys` weg, `pirep_held_queue` weg (phpVMS hält PENDING-State selbst), `pirep_filing_audit` → `pirep_review_metadata` (nur Review-State-Tracking + Module ↔ VPS Audit).
  - **MQTT-Topics für PIREP-Filing entfernt:** keine `pirep_draft`/`pirep_status`-Topics mehr nötig. Synchrone HTTP-Calls Module ↔ VPS sind ausreichend.
  - **Migration-Pfad LE37 entfällt:** alte v0.12.x-Clients funktionieren automatisch mit, weil das Filing-Verhalten am Client unverändert bleibt.
  - **Aufwand-Schätzung neu:** ~30 h für Stream G (Module + VPS-Endpoints) statt 38 h R5-VPS-Filing. Gesamt v0.13.0 **~194 h (range 180–210 h)**.
- **R7:** QS-R6-Findings (Thomas auf GitHub-Version) — 7 Findings, alle valide, alle eingearbeitet:
  - **P1 — PENDING-Bypass behoben:** R6 setzte für held-Status `PirepState::PENDING`, aber phpVMS' `submit()`-Code refresh()'d das Model UND führt danach Auto-Approve aus → `PENDING + rank.auto_approve_acars=true` wird sofort wieder `ACCEPTED`. **R7 nutzt `PirepState::REJECTED` als Hold-State** (überlebt den Post-Event-Code-Block, ist nicht auto-approve-eligible). Distinct via Comment-Pattern + neues `PirepFieldValue.slug='aeroacars_integrity_state'` mit Werten `'held'` | `'rejected_final'` für UI-Unterscheidung. Pilot-Notification beim Hold wird via PirepStateChange-Listener-Suppression unterdrückt (Implementation Code-QS-Task).
  - **P1 — VPS-404-Bypass behoben:** Für v0.13.0+-Clients ist 404 jetzt fail-closed (REJECTED-held + Comment „No telemetry received — admin verify"). Legacy-v0.12.x-Allowlist nur via expliziter Pilot-Liste (separate DB-Tabelle `legacy_pilot_allowlist`). Default: kein PIREP ohne Telemetrie wird auto-akzeptiert.
  - **P1 — Unbekannter Throwable behoben:** Listener catcht jetzt `\Throwable` (nicht nur `\Exception`), Default ist fail-closed REJECTED-held mit Comment „Integrity Gate internal error — admin verify". Audit-Log capturt Exception-Details für Diagnose.
  - **P2 — Sync-Timeout reduziert:** statt 3 Attempts × 5s + Backoff (= ~16,5s) jetzt **ein** synchroner Versuch mit 3s-Timeout, bei Fail → fail-closed REJECTED-held + async Retry-Worker. Pilot-perceived Filing-Latenz max ~3s.
  - **P2 — Shared-Secret um HMAC ergänzt:** statt Plain-Secret-Compare jetzt HMAC-SHA256 über `body + timestamp + nonce`, Timestamp-TTL 5 min, Nonce-Replay-Cache auf beiden Seiten. Standard-Webhook-Security.
  - **P2 — v0.12.x-Kompatibilität entschieden:** Hard-Cut auf v0.13.0+. Snapshot-Schema-Pflicht (`session_id`, `sequence_number`) gilt für ALLE Clients. Alte v0.12.x-Clients können Telemetrie nicht mehr senden → 404 von VPS → fail-closed REJECTED-held → Pilot muss auf v0.13.0+ updaten. Migrations-Plan: 4 Wochen Vorab-Kommunikation via Discord/Email/in-app-Update-Banner. `legacy_pilot_allowlist` nur als Notfall-Override für Einzelfälle.
  - **P2 — `dismissed_fp` separater Pfad:** statt Same-Path wie `accepted_with_warning` jetzt eigener Code-Pfad mit eigenem Comment-Template (`dismissed_as_false_positive` vs `accepted_with_warning`) und separatem Audit-Eintrag.
  - **Aufwand:** ~6 h Mehraufwand für HMAC-Implementierung, Hold-State-Mechanik, Suppression-Listener. Gesamt **~200 h (range 185–215 h)**.
- **R8:** QS-R7-Findings (Thomas, GitHub) — 7 Findings, Kern-Erkenntnis: **R7's REJECTED-als-Hold-Architektur war broken**. Zu viele Folgefehler durch unkontrolliertes phpVMS-`reject()`-Triggering und fehlende Reverse-Path-Logik.
  - **P1 — Architektur-Pivot:** weg von REJECTED-als-Hold, hin zu **PENDING-als-Hold + GSG-Side-Voraussetzung `auto_approve_acars=FALSE` für alle Ranks**. Mit deaktiviertem Auto-Approve hält PENDING tatsächlich (phpVMS' submit-Code respektiert es). Module nutzt `PirepService::accept()`/`::reject()` proper für Lifecycle-Events.
  - **Konsequenz P1:** keine ungewollten `reject()`-Side-Effects mehr (Hours/Pay/Notifications werden nicht reversed bei Hold). Clean PIREPs gehen durch proper `accept()`-Flow inklusive Notifications + Stats. Final-Reject geht durch proper `reject()`-Flow. Held PIREPs bleiben sauber in PENDING ohne UX-Disaster.
  - **Module-Installation neue Pflicht:** Modul verifiziert beim Boot dass `auto_approve_acars=FALSE` auf allen Ranks. Wenn nicht: Health-Check failure-Notification an VA-Admin + Module disabled bis Config fixed. Schutz vor accidentaler Re-Aktivierung.
  - **P1 — Webhook-Lift-Hold korrekt:** statt R7's `if ($pirep->state === PENDING) → ACCEPTED`-Check jetzt `$pirepSvc->accept($pirep)` (proper Lifecycle), für reopen `$pirepSvc->reject($pirep)` wenn vorher ACCEPTED + integrity_state='accepted_with_warning' (Re-Hold zurück in PENDING).
  - **P1 — Reverse-Path über PirepService:** alle State-Wechsel im Webhook gehen über `PirepService::accept()`/`::reject()` statt direkt-`$pirep->save()`. Garantiert dass Hours, Pay, Stats, Aircraft-State, Rank-Progression korrekt aktualisiert werden.
  - **P2 — Notification-Suppression entfällt komplett:** weil keine ungewollten reject()-Aufrufe mehr → kein false Reject-Mail → kein Suppression-Listener nötig. Eine ganze problematische Mechanik weg.
  - **P2 — Stream G Tests komplett umgeschrieben:** alle 20 Tests auf neue PENDING-Semantik. WZZ2271-Test prüft `state=PENDING + integrity_state='held'`, nicht REJECTED.
  - **P2 — Stale R6/R7 Architekturtexte durchgesucht und bereinigt:** Diagramm in G.2 zeigt jetzt korrekt PENDING-Flow. Webhook-Code-Beispiele alle aktualisiert. v0.12.x-Hard-Cut-Aussage konsistent durchgezogen.
  - **P2 — `reopen` semantisch klar:** setzt state → PENDING (via accept() wenn vorher ACCEPTED, sonst direkt) + `integrity_state='held'`. Konsistent mit normaler Hold-Semantik.
  - **Aufwand:** −2 h (Suppression-Listener entfällt) +3 h (Config-Verify-Service + Health-Check) +2 h (Stream G Test-Refactor) = **+3 h netto, Gesamt ~205 h (range 190–220 h)**.
- **R9:** QS-R8-Findings (Thomas, GitHub) — 4 Findings, alle eingearbeitet. R9 ist eher Politur als Pivot — Architektur von R8 ist richtig, aber Listener/Webhook-Lifecycle-Boundary war noch schlampig und Misconfig-Verhalten war fail-open.
  - **P1 — Listener-vs-Webhook-Lifecycle-Split (normativ):** R8 ließ den Listener `PirepService::accept()`/`->reject()` rufen, was im Submit-Flow-Kontext einen **Double-Trigger** verursachte (phpVMS macht das anschließend nochmal). R9 macht die Boundary verbindlich:
    - Listener-Kontext: NUR `$pirep->state = X; $pirep->save();` — kein PirepService-Call. phpVMS' Submit-Flow ruft `accept()`/`reject()` automatisch ein einziges Mal je nach State.
    - Webhook-Kontext: PirepService::accept()/->reject() explizit aufrufen — kein umgebender Submit-Flow.
    - Code-Audit-Regel: `PirepStateManipulator` injected nicht mehr `PirepService`. Wer's addiert, kriegt Code-Review-Reject.
  - **P1 — Misconfig fail-closed statt fail-open:** R8's „Module deaktiviert sich bei Drift" war ein **Bypass-Vektor** (PIREPs fielen durch auf phpVMS-Default-Verhalten = Auto-Approve wenn aktiv). R9 macht das umgekehrt:
    - Installation fail-closed: Module wird gar nicht erst aktiviert wenn Config-Check fail
    - Runtime-Drift: Module wechselt in **Emergency-Mode** (`applyConfigDriftFallback`), jeder PIREP wird auf PENDING-held gesetzt mit `integrity_state='held_config_drift'`
    - Sichtbarkeit: 5 parallele Kanäle (phpVMS-Admin-Banner, Discord, E-Mail, VPS-Webapp-Banner, Pilot-Comments) — kein silent failure möglich
    - Recovery: VA-Owner fixt Config + verify-Command, dann Switch zurück in Normal-Mode
  - **P2 — Reopen-from-ACCEPTED in v0.13.0 verboten:** R8's „erst reject(), dann PENDING setzen" war zu side-effect-anfällig (würde Reject-Notification + Audit-Trail feuern). R9 returnt Webhook 400 bei reopen-from-ACCEPTED. VA-Admin nutzt phpVMS-Admin-UI direkt für seltene Cases. *(R9 hatte hier ursprünglich auf eine spätere Reverse-Pipeline verwiesen — in R10 endgültig verworfen, kein Folge-Release vorgesehen.)*
  - **P2 — Stale R7-REJECTED-Hold-Texte bereinigt:** mehrere Stellen waren noch auf R7-Architektur (REJECTED-Hold). R9 hat alle gefunden und auf R8/R9-PENDING-Hold umgestellt (LE30 Fail-Closed-Policy, G.4 Security-Tabelle, Changelog R7-Historie-Einträge markiert als historisch).
  - **Aufwand:** +0 h netto (Code-Aufwand für Listener-Refactor wird durch eingesparten PirepService-Inject und reopen-from-ACCEPTED-Removal kompensiert). Gesamt **~205 h (range 190–220 h)**.
- **R10:** QS-R9-Findings (Thomas, GitHub) — 6 Findings (3× P1, 3× P2), alle in v0.13.0-Scope gelöst, **kein Auslagern nach v0.13.1 oder Backlog mehr** (Scope-Vorgabe).
  - **P1 — Emergency-Mode-Widerspruch behoben durch Auto-Repair-Pattern:** R9's „Emergency-Mode setzt PIREPs auf PENDING bei Config-Drift" war fundamentaler Logikbug — bei `auto_approve_acars=true` würde PENDING ja wieder zu ACCEPTED. R10 ersetzt Emergency-Mode komplett durch **Auto-Repair**: Module setzt `auto_approve_acars=false` automatisch (Boot + Daily-Cron + Per-PIREP-Defense-in-Depth). Drift wird in Sekunden korrigiert, nicht „bis VA-Owner reagiert". Standard-Pattern (Letsencrypt-Auto-Renew, fail2ban). Vollständig protokolliert + Discord-Notification + Admin-Banner 24h.
  - **P1 — Async-Retry-Worker nutzt PirepService:** R9-Boundary war nur für Listener vs. Webhook gezogen. Worker läuft Stunden später off-flow (kein umgebender Submit-Flow) — gleiche Regel wie Webhook: PirepService explizit aufrufen sonst läuft kein Lifecycle. R10 dokumentiert das explizit + Code-Beispiel.
  - **P1 — Kein Auslagern nach v0.13.1, reopen-from-ACCEPTED HART entfernt:** R9's „v0.13.1 Stream H Backlog" widersprach Scope-Vorgabe. R10: reopen-from-ACCEPTED ist **dauerhaft nicht supported** in AeroACARS — phpVMS hat keinen side-effect-freien Reverse-Pfad aus ACCEPTED, und wir bauen ihn nicht. VA-Admin nutzt für seltene Reverse-Cases das phpVMS-Admin-Panel direkt (phpVMS-Native-Funktionalität). Webhook returns 400 mit klarer Fehler-Message. LE21b State-Machine entsprechend angepasst: reopen nur von REJECTED/dismissed_fp.
  - **P2 — Test-Matrix R10-aktualisiert:** alle Stream-G-Tests von R8/R9-Semantik auf R10 gebracht. Neue Tests für Auto-Repair (Boot+Cron+Per-PIREP), `no_double_lifecycle_trigger`, `async_retry_worker_uses_pirep_service_off_flow`, `module_webhook_decision_reopen_from_accepted_returns_400`, `reason_required_for_rejected_dismissed_reopen`. Entfernt: Tests die R9-`Emergency-Mode`, R8-`Listener-PirepService-Aufrufe`, R8-`reopen-from-ACCEPTED-revert-then-hold` annahmen.
  - **P2 — `reopen` global konsistent:** LE21b Transitions-Tabelle und LE34 Webhook-Switch sagen jetzt beide dasselbe: reopen nur von `rejected`/`dismissed_false_positive`, nicht von `accepted_with_warning`. `reason` ist konsistent REQUIRED für rejected/dismissed/reopen.
  - **P2 — Stale Status/Changelog-Texte bereinigt:** R8-Hauptänderungen-Block sagte noch „alle Lifecycle-Wechsel über PirepService" (war R8-Annahme, in R9 korrigiert) und „Module deaktiviert sich bei Misconfig" (war R8-Annahme, in R9 Emergency-Mode, in R10 Auto-Repair). R10 hat alle Status-Texte auf aktuellen Stand gebracht.
  - **Aufwand:** −2 h (Emergency-Mode entfällt) +3 h (Auto-Repair-Service inkl. Boot/Cron/Per-PIREP-Check) +1 h (Worker-Refactor) = **+2 h netto, Gesamt ~207 h (range 195–225 h)**.
- **R11:** QS-R10-Findings (Thomas, GitHub) — 4 Findings (1× P1, 3× P2), alle eingearbeitet:
  - **P1 — Auto-Repair als ERSTER Schritt im Listener-Code normativ verankert:** R10 hatte Auto-Repair beschrieben (G.2.0), aber LE30-Listener-Code rief es nicht auf. Same-Request-Bypass-Risiko: phpVMS' Submit-Code liest `$pirep->user->rank->auto_approve_acars` aus dem **in-memory cached Eloquent-Relation** — wenn wir nur die DB updaten ohne Relation-Refresh, sieht phpVMS noch den drift `true`-Wert und dreht PENDING auf ACCEPTED. **R11:** LE30 ruft `AutoRepairService::repairForPirep($pirep)` als allererstes auf. Service macht DB-Update + Laravel-Cache-Invalidation + `$pirep->user->setRelation('rank', $rank->fresh())` + `$pirep->load('user.rank')` — garantiert frische Daten für den Rest des Submit-Flows. Plus defensive Re-Check nach Repair: wenn rank IMMER NOCH drift zeigt → emergency Hold (sollte unmöglich sein, aber fail-closed).
  - **P2 — Webhook-Validation: `reason` REQUIRED für `dismissed_fp` und `reopen`:** R10's Validation-Rule war nur `required_if:target_state,rejected`, das widersprach LE21b-Konsistenzregel. R11 korrigiert auf `required_if:target_state,rejected,dismissed_fp,reopen`. Test `module_webhook_reason_required_for_rejected_dismissed_reopen` (war schon in der Test-Matrix).
  - **P2 — Stale Security-Texte (R7/R9-Reste in G.4) bereinigt:** R10's Security-Tabelle hatte noch „REJECTED-Hold" und „Emergency-Mode" als Beschreibung. R11 hat alle 4 betroffenen Zeilen auf R10/R11-Auto-Repair-Pattern + PENDING-Hold umformuliert. Zusätzlich neue Threat-Zeile „Same-Request-Cache-Bypass via Eloquent-Relation" mit R11-Mitigation.
  - **P2 — Backlog-Reste in A5 (Dump-Aircraft) und Stream-A-LE9 (Aircraft-Awareness) entfernt:** statt „im Backlog" jetzt explizit „nicht Teil dieser Produktanforderung — A330/B747/B777 sind nicht in GSG-Fleet, A321-Schwellen sind ausreichend tolerant für kleinere Aircraft". Plus R9-Block-Stelle „Reverse-Pipeline für v0.13.1 (Stream H Backlog)" als R9-Aussage markiert, die in R10 verworfen wurde.
  - **Aufwand:** +1 h netto (AutoRepairService-Definition + Tests). Gesamt **~208 h (range 196–226 h)**.
- **R12:** QS-R11-Findings (Thomas, GitHub) — 5 Findings (1× P1, 3× P2, 1× P3). R11-Auto-Repair-Logik war fast komplett, aber Failure-Pfad war wieder fail-open.
  - **P1 — Auto-Repair-Failure-Fallback wirklich fail-closed:** R11 setzte bei nachgewiesenem Repair-Failure `applyConfigDriftFallback()` mit PENDING. Aber genau in diesem Zustand würde phpVMS-Submit-Flow PENDING wieder auf ACCEPTED drehen (weil rank.auto_approve_acars immer noch true ist). R12: **neue `applyAutoRepairFailureFallback()`-Methode** setzt `PirepState::REJECTED` mit `integrity_state='emergency_repair_failure'`. Einziger State der diesen Code-Pfad fail-closed überlebt. Konsequenz: PIREP wird hart abgelehnt mit Reject-Notification an Pilot + 🚨-Discord-Alarm an VA-Owner. UX-suboptimal aber einzige korrekte fail-closed Option für „should never happen"-Pfad.
  - **P2 — `dismissed_fp → open` Konsistenz-Fix:** R11 erlaubte in LE21b das Reopen aus `dismissed_false_positive`, aber LE34 blockte jeden phpVMS-State=ACCEPTED (was dismissed_fp via `PirepService::accept()` auch ist). R12: LE21b entfernt `dismissed_false_positive → open` aus den erlaubten Transitions. Konsistent mit LE34's Hard-Block für alle ACCEPTED-States. VA-Admin nutzt für seltene Reverse-Cases das phpVMS-Admin-Panel direkt.
  - **P2 — Architektur-Diagramm + Webhook-Summary auf R12-Stand:** R8-stale-Text „Listener: `$pirepSvc->accept($pirep)`" durch R12-korrekten Listener-NUR-State-Flow ersetzt. Webhook-Summary „setzt $pirep->state je nach target_state" durch R12-korrekten PirepService-basierten off-flow-Lifecycle ersetzt. Diagramm zeigt jetzt R11/R12-Auto-Repair-First-Step + Listener-Schritt-Pfade explizit.
  - **P2 — Hard-Cut-Text PENDING-Hold (statt REJECTED-Hold):** v0.12.x-Hard-Cut-Sektion sagte noch „REJECTED-Hold" — R12 auf `PENDING-Hold` mit `integrity_state='held_no_telemetry'` korrigiert.
  - **P3 — Aufwand-Tabelle Stream-G-Zeile auf R12-Wording:** „R8 final" → „R12 final" mit Auto-Repair-Service + Listener-Boundary + Cache-Invalidation + Emergency-Fallback eingerechnet. +4 h ggü. R8-Schätzung, was in R10/R11 nicht reflektiert war. Gesamt jetzt ~216 h ehrlich (statt 208).
  - **Aufwand:** +8 h netto in Stream-G-Zeile (Auto-Repair-Failure-Fallback + Cache-Invalidation-Tests + Spec-Pflege für R7–R12-Runden). Gesamt **~216 h (range 200–235 h)**.
- **R13:** QS-R12-Findings (Thomas, GitHub) — 5 Findings (1× P1, 3× P2, 1× P3). R12-Emergency-Fallback war konzeptionell korrekt, aber AutoRepair-Exception-Pfad lief am Fallback vorbei.
  - **P1 — try/catch um repairForPirep() im Listener (LE30):** R12 rief `$this->autoRepairService->repairForPirep()` ungeschützt. Wenn der Service eine Exception wirft (DB-Lock, Cache-Plugin-Crash, etc.), kam der Code nie bis zum Re-Check + Emergency-Fallback. R13: Listener wickelt repair+verify in `try { ... } catch (\Throwable $t) { applyAutoRepairFailureFallback(); return; }`. Sowohl `AutoRepairVerificationFailedException` (Re-Check zeigt drift) als auch jede andere Repair-Exception landet im selben Emergency-Pfad mit REJECTED-Hold + Discord-Alarm.
  - **P2 — `applyConfigDriftFallback()` aus LE33 entfernt:** Diese Methode setzte PENDING-Hold, was bei Config-Drift unsicher ist (PENDING wird via Auto-Approve zu ACCEPTED). R12 hatte die Methode noch als „weitgehend obsolet" stehengelassen. R13 entfernt sie hart aus dem Code-Block. Plus Audit-Test `module_code_does_not_reference_applyconfigdriftfallback` verhindert Wieder-Einführung.
  - **P2 — Retry-Queue-Schema-Kommentar inkludiert `dismissed_fp`:** `module_webhook_retry_queue.target_state`-Spalten-Kommentar war `'accepted_with_warning | rejected | reopen'` — fehlend `dismissed_fp` das LE34-Webhook ebenfalls akzeptiert. R13: Kommentar erweitert, konsistent mit Webhook-Body-Validation.
  - **P2 — Historische v0.13.1/Backlog-Reste sanitisiert:** mehrere historische Changelog-Stellen (R3 Phase-Split, R9 Reverse-Pipeline) hatten noch „v0.13.1/Backlog"-Wording. R13 umformuliert auf „verworfen, kein Folge-Release vorgesehen" — keine Verwechslung mehr mit aktiven Scope-Aussagen möglich.
  - **P3 — Header „sechs Streams" → „sieben Streams":** Trigger-Block sagte „mit sechs Streams (A–F)" aus R0-Zeit als Stream G noch nicht existierte. R13: korrigiert auf „sieben Streams (A–G) plus Sub-Stream C-Sub".
  - **Aufwand:** +0 h netto (try/catch ist trivial, Code-Removal spart, Audit-Test ergänzt). Gesamt bleibt **~216 h (range 200–235 h)**.

---

## Übernommene Entscheidungen (Pre-Spec)

| ID | Frage | Entscheidung |
|---|---|---|
| A1 | Detektions-Ort | **Recorder + Client (defense-in-depth)** |
| A2 | Auto-Reject oder Flag? | **Auto-Flag + Review-Queue** |
| A3 | Backfill alter PIREPs? | ~~**Nein — nur neue PIREPs ab v0.13.0**~~ → **revidiert in R1: JA, als Stream E mit Dry-Run + Apply (siehe Stream E unten)** |
| A4 | CRITICAL-Verhalten im Flug | **Toast-Notification + Weiterfliegen erlaubt** |
| A5 | Fuel-Rate-Schwelle | **Hartcodiert für Non-Dump-Aircraft** (deckt 100% GSG-Fleet — A320-Familie und kleiner; Dump-Aircraft wie A330/B747/B777 sind nicht Teil dieser Produktanforderung) |
| A6 | Score-Sichtbarkeit bei untrusted | **Score sichtbar + roter Trust-Badge "DATA INTEGRITY: REVIEW"** |
| A7 | PIREP-Freeze-Cutoff | **`pirep_filed`-Event** |
| A8 | SEAMLESS-Banner-Dauer | **5 s mit Countdown + Cancel-Button** |
| A9 | GUIDED-Diff-Anzeige | **Volle Diff-Tabelle mit Ampel je Metrik** |
| A10 | REPOSITION → Review-Queue? | **Immer Review-Queue** |
| A11 | Versions-Bump | **v0.13.0** |

---

## Globales: Severity-Modell (`base_severity` vs. `effective_severity`)

Jedes Integrity-Event hat eine **`base_severity`** (was der Detektor emittiert) und einen abgeleiteten **`effective_severity`** (was nach PIREP-weiter Aggregation gilt). Beide werden persistiert, beide sind via API sichtbar, Webapp zeigt `effective_severity` in Badges, Forensik-View zeigt beide.

### Severity-Stufen (gilt für base und effective)

| Severity | Recorder | Client (Toast) | PIREP-Effekt | Web-Monitor |
|---|---|---|---|---|
| **NOISE** | unter Schwelle, ignoriert | — | — | — |
| **INFO** | im Flight-Log persistiert | optional als Activity-Log-Eintrag | im Forensik-View sichtbar, kein Score-Effekt | sichtbar im Forensik-View, kein Badge |
| **ANOMALY** | im `integrity_flags`-Array | nicht-blockierender Toast (3 s Auto-Hide) | gelbes Warn-Badge im PIREP-View, kein Score-Effekt | gelbes Badge in Listen, sichtbar im Forensik-View |
| **CRITICAL** | im `integrity_flags`-Array + `requires_review = true` | nicht-blockierender Toast (manuell schließen) | rotes Review-Badge, **`score_trust_level` mindestens auf `review`** | rotes Badge in Listen, **Eintrag in Review-Queue**, Forensik-View hebt Event hervor |

### Aggregations-Regeln (R1)

**Default:** `effective_severity = base_severity`.

**Combo-Eskalationen** (laufen **live nach jeder Flag-Ingestion** innerhalb der Snapshot-Transaktion vor allen Downstream-Schritten — siehe Live-Aggregation-Sektion unten und C.1-Pipeline):

| Combo-Bedingung im selben Flug | Effekt |
|---|---|
| `FUEL_TELEMETRY_UNAVAILABLE` (ANOMALY) **+** beliebige `FUEL_RATE_IMPOSSIBLE` **oder** `WEIGHT_DISCONTINUITY` | `FUEL_TELEMETRY_UNAVAILABLE.effective_severity` → CRITICAL (Begründung: ohne Fuel-Telemetrie kann legitimes Verhalten nicht plausibilisiert werden) |
| `TELEMETRY_GAP_LONG` (ANOMALY) **+** beliebiger `GAP_EDGE_STATE_CONFLICT` am selben Gap | `TELEMETRY_GAP_LONG.effective_severity` → CRITICAL |
| `AIRCRAFT_IDENTITY_INCONSISTENT` (ANOMALY) **+** jede `WEIGHT_DISCONTINUITY` | `AIRCRAFT_IDENTITY_INCONSISTENT.effective_severity` → CRITICAL (Indikator für echten Aircraft-Wechsel, nicht Sim-Adapter-Glitch) |
| ≥ 3 verschiedene `ANOMALY`-Typen im selben Flug | PIREP bekommt zusätzlich `requires_review = true`, auch ohne CRITICAL einzeln |
| `RESUME_AFTER_REPOSITION` **+** jeder CRITICAL-Flag nach dem Resume | `RESUME_AFTER_REPOSITION.effective_severity` → CRITICAL |

**Lifecycle (R4 korrigiert — Aggregation ist live, Finalize konsolidiert nur):**

```
1. Detection (Streams A, B, C, F)
   → schreibt base_severity + flag_payload ins integrity_flags-Array
   → INNERHALB DERSELBEN SNAPSHOT-TRANSAKTION:

2. Live-Aggregations-Pass (siehe Live-Aggregation-Sektion + C.1-Pipeline)
   → läuft über ALLE bisherigen Flags der Session
   → setzt/aktualisiert effective_severity je Flag
   → setzt/aktualisiert session.effective_severity (rolling)
   → bei Eskalation eines älteren Flags: Recompute-Trigger (Stream C)
   → aktualisiert PIREP-cached score_trust_level wenn schon vorhanden

3. Downstream (FSM, Touchdown-Detector, Landing-Scorer) sieht die
   aktuelle effective_severity — nicht mehr base_severity allein

4. Persist (Snapshot + alle Flag-Änderungen werden committed)

5. PIREP-Finalize-Pass (bei pirep_filed-Event)
   → konsolidiert: finaler Aggregations-Pass als Sanity-Check
   → schreibt finalen score_trust_level, requires_review, score_trust_reasons
   → KEIN „erstes Mal Aggregation" — alle Werte sind schon live berechnet
   → Snapshot-Stream nach pirep_filed → post_flight_events (LE16)

6. Sichtbarkeit
   → API liefert base_severity + effective_severity
   → Webapp zeigt effective in Badges, Forensik zeigt beide + Versions-Bindung
```

**Der entscheidende Punkt (R4):** Aggregation ist **nicht** mehr „einmal beim Finalize" — sie ist **kontinuierlich, vor jedem Downstream-Schritt**. Das verhindert dass FSM/Scorer einen Snapshot mit zu niedriger Severity verarbeiten. Finalize macht nur noch die finale Konsolidierung — keine Severity entsteht erst dort.

**Wichtig — keine Severity bedeutet „Reject":** Auch effective CRITICAL führt nicht zu automatischer PIREP-Ablehnung. Der VA-Admin entscheidet manuell in der Review-Queue. Das ist Konsequenz der A2-Entscheidung und schützt vor False-Positive-Schäden.

**Schwellwerte sind hartcodierte Konstanten** in v0.13.0 (kein Config-System). Tuning erfolgt durch Folge-Releases nach realen Pilot-Daten.

### Ingestion-Sequencing (R3, normativ)

Bevor Live-Aggregation überhaupt sinnvoll laufen kann, muss die Ingestion **deterministisch geordnet und idempotent** sein. R3 macht das explizit:

**Pflicht-Eigenschaften der Ingestion-Pipeline pro `session_id`:**

| Eigenschaft | Wie |
|---|---|
| **Serialisierung** | Pro `session_id` läuft genau ein Ingestion-Worker. Realisiert via DB-Lock (`SELECT ... FOR UPDATE` auf `flight_sessions`-Zeile) oder per-session in-memory-Mutex im Recorder. Parallele Snapshot-Batches für dieselbe Session werden serialisiert. |
| **Idempotency** | Jeder Snapshot trägt einen idempotency-key `(session_id, sequence_number)`. Sequence-Number kommt aus dem Client. Recorder hält eine Replay-Cache (LRU, letzte 1.000 keys pro Session). Doppelter Empfang = no-op. |
| **Monotonic-Sequence-Check** | `sequence_number` muss streng monoton steigen pro Session. Out-of-order: Snapshot wird in einen Per-Session-Buffer gelegt, max. 60 s gehalten. Wenn Lücke nicht geschlossen wird: alle gepufferten Snapshots werden verworfen + `INGESTION_OUT_OF_ORDER_DROPPED`-Flag (ANOMALY) gesetzt. |
| **Monotonic-Timestamp-Check** | `snapshot.ts` muss monoton steigen pro Session. Zeitsprung rückwärts > 1 s → `INGESTION_CLOCK_REGRESSION`-Flag (ANOMALY) + Snapshot wird normal verarbeitet (Toleranz: NTP-Jitter). > 10 s rückwärts → CRITICAL + Snapshot wird quarantäniert. |
| **MQTT-Replay-Safety** | MQTT-Subscriber identifiziert sich mit Client-Instance-ID. Bei Reconnect: Server liefert `last_acknowledged_sequence_number`, Client startet ab da. Verhindert doppelten Replay über MQTT-Retain. |

**Konsequenz:** Egal welche Form von Parallelität, Retry oder Replay auftritt — die Ingestion-Pipeline sieht jeden Snapshot **genau einmal in der Originalreihenfolge**, und Live-Aggregation hat eine konsistente Sicht auf den Session-State.

### Live-Aggregation (R3 revidiert mit Recompute)

`effective_severity` wird **nicht** erst beim PIREP-Finalize berechnet, sondern **session-live nach jeder Flag-Ingestion innerhalb derselben Snapshot-Transaktion vor allen Downstream-Schritten**. R2's „keine Retro-Wirkung" war falsche Vereinfachung — R3 macht es korrekt mit explizitem Recompute-Mechanismus.

```
on each new snapshot in session (innerhalb DB-Transaction):
    1. write flag(s) with base_severity (Stream-A/B/C/F-Detection)
    2. run Combo-Aggregation-Pass über ALLE bisherigen flags der Session
    3. update flag.effective_severity (kann zwischen base und höher gehen)
    4. update session.effective_severity = max(flag.effective_severity ∀ flag)
    5. compare to previous session.effective_severity:
         IF stayed same OR went up only on new flags:
             → kein Recompute nötig, weiter mit FSM/Scorer (LE13ff)
         IF nachträgliche Eskalation eines ÄLTEREN Flags:
             → triggert RECOMPUTE-Pfad (siehe unten)
    6. push MQTT integrity_event mit neuer effective_severity
    7. update PIREP-cached score_trust_level wenn schon vorhanden
    8. commit transaction
```

#### Recompute-Pfad (R3 normativ)

**Auslöser:** Live-Aggregation eskaliert einen Flag der bereits vor der aktuellen Snapshot-Verarbeitung existierte. Beispiel: Snapshot N hatte `FUEL_TELEMETRY_UNAVAILABLE` (ANOMALY base) am Flug-Start. Snapshot N+200 wirft `FUEL_RATE_IMPOSSIBLE`. Die Combo-Regel eskaliert FTU rückwirkend auf CRITICAL. Snapshots N+1 bis N+199 sind aber bereits durch FSM und teilweise durch Scorer gelaufen.

**Affected-Segment-Berechnung:**

```
affected_segment = [
    first_snapshot_with(escalated_flag),   # = N
    current_snapshot                       # = N+200
]
```

**Recompute-Aktionen** (alle innerhalb derselben Transaktion):

1. **FSM-Replay:** FSM-State wird auf den letzten Checkpoint vor `affected_segment.start` zurückgesetzt. Alle Snapshots im Segment werden mit den neuen effective_severities erneut durch die FSM gefahren (jetzt unter Berücksichtigung der eskalierten Severity → eventuell andere Phase-Transitions, eventuell PHASE_TRANSITION_REJECTED).
2. **Scorer-Invalidation:** Alle Touchdown-Events / Landing-Scores die im `affected_segment` entstanden sind, werden als `requires_rescore = true` markiert. Re-Scoring läuft mit den neuen Quarantine-Zuständen.
3. **PIREP-Cache-Invalidation:** `score_trust_level`, `requires_review`, `landing_score_trusted` werden neu berechnet.
4. **MQTT-Notification:** Spezielles Event `integrity_segment_recomputed` mit Range + Reason an Client (Pilot sieht „Bewertung der letzten X Minuten wurde neu evaluiert").
5. **Audit-Log:** `recompute_log`-Eintrag mit Segment-Range, Auslöser-Flag, From/To-State.

**Checkpointing für Recompute:** Recorder hält pro Session alle 60 s einen FSM-Checkpoint (`FlightStats`-Snapshot in eigener Tabelle). Recompute lädt den nächst-älteren Checkpoint vor `affected_segment.start` und replayed forward.

**Backstop-Schutz:** Recompute läuft maximal 3× pro PIREP. Wenn ein 4. Recompute-Trigger käme (extrem selten, würde z. B. Chain-Eskalation bedeuten) → PIREP wird `requires_review = true` markiert, Recompute übersprungen, FSM-State bleibt im aktuellen Stand. Verhindert Endlos-Loops bei pathologischen Flügen.

**Folgen für Downstream:**
- **FSM** (Stream C LE14) entscheidet auf Basis der aktuellen `session.effective_severity` — atomar innerhalb der Snapshot-Transaktion
- **Landing-Scorer** (LE15 Hard-Triggers) prüft live ob `score_trust_level` schon untrusted ist; Recompute revidiert bei Bedarf
- **Client-Toast** zeigt Severity ab Eskalations-Zeitpunkt; bei Recompute zusätzliches Event „Bewertung aktualisiert"
- **Web-Monitor** Review-Queue füllt sich live während aktiver Flüge; bei Recompute werden cached Einträge aktualisiert

### Scope-Klarstellung (R2, verbindlich)

> **v0.13.0 wird als zusammenhängendes Integritäts-Release umgesetzt; es gibt keine Auslagerung von Resume-Policy, Review-Workflow oder Backfill in eine spätere Version. Eine interne Sequenzierung der Code-Slices ist erlaubt, ändert aber nicht den Release-Scope.**

Alle sechs Streams (A–F) sind Pflicht für das v0.13.0-Release. Implementation darf intern in Slices erfolgen (z. B. „Slice 1 = Recorder-Validator, Slice 2 = Webapp-Surfacing, Slice 3 = Client-Resume-Policy"), aber **die Spec ist erst ACCEPTED wenn alle Streams umsetzungsfähig spezifiziert sind**, und das Release ist erst tagbar wenn alle Streams ausgeliefert sind.

---

## Stream A — Physik-Validator (kontinuierliche + Gap-Edge-Plausibilität)

### A.0 Befund

Der Recorder konsumiert heute jeden eingehenden Snapshot ohne Plausibilitäts-Check. Bei WZZ2271 wurden vier physikalisch unmögliche Sprünge eingespielt (Position-Δ > 30 NM in < 5 s, Altitude-Δ > 30.000 ft in < 5 s, Fuel-Reset auf exakt 7.000 kg). Keiner dieser Sprünge hat ein Flag gesetzt.

Stream A fügt einen **Plausibilitäts-Validator** ein, der in **zwei Modi** operiert (siehe A.1) und Δ-Werte gegen phase-aware Schwellwerte prüft.

### A.1 — Validator-Modi (R1, normativ)

Alle Stream-A-Checks (LE1–LE7) operieren in **zwei Modi**, beide produzieren Flags mit demselben Typ-Namen, aber unterschiedlichem `mode`-Tag im Detail-Payload:

**(a) Continuous-Mode** — Sample-zu-Sample
- Aktiv wenn `Δt(previous, current) ≤ 15 s`
- Schwellwerte siehe LEs (auf 5-s-Basis kalibriert; bei Δt zwischen 5 s und 15 s linear extrapoliert)
- Flag-Detail: `{ mode: "continuous", dt_s: <float> }`

**(b) Gap-Edge-Mode** — Envelope-Plausibilität (R2 revidiert)
- Aktiv wenn `TELEMETRY_GAP_LONG` oder `TELEMETRY_GAP_CRITICAL` detektiert (Stream B LE8)
- Operiert gegen den **letzten gültigen Pre-Gap-Sample** (= letzter Snapshot mit `quarantine_status == "clean"` vor dem Gap; nicht zwingend der unmittelbar vor dem Gap, falls dieser bereits suspect war)
- **R2-Kernwechsel:** keine punkt-genaue Expected-Berechnung mehr. Stattdessen **Plausibilitäts-Envelope** — der Post-Gap-Sample muss innerhalb eines mehrdimensionalen erlaubten Raumes liegen. Der WZZ2271-Fall „31 NM in 351 s = 322 kt rechnerisch noch erreichbar" zeigt warum punkt-Vergleiche schwach sind. Der eigentliche Konflikt ist nicht „falsche Distanz", sondern Phase-Continuity-Bruch.

**Vier Envelope-Dimensionen:**

| Envelope | Berechnung | Verletzung (Flag) |
|---|---|---|
| **(1) Reachable-Range** (Position) | Radius = `max_GS_for_phase(last_phase) × gap_duration_s × 1.5` (mit 50% Sicherheitsmarge für Variabilität); `max_GS_for_phase`-Tabelle: Cruise=600 kt, Climb/Descent=400 kt, Approach=300 kt, TaxiOut=30 kt, Boarding=5 kt | Post-Gap-Position außerhalb Radius → `POSITION_OUTSIDE_REACHABLE_ENVELOPE` (CRITICAL) |
| **(2) Vertical-Envelope** (Altitude) | Range = `[last_alt - max_VS_descent_for_phase × gap_duration_s, last_alt + max_VS_climb_for_phase × gap_duration_s]`; `max_VS_climb/descent` Cruise/Climb/Descent = 6000 fpm, Approach = 2500 fpm, on_ground = 100 fpm | Post-Gap-Altitude außerhalb Range → `ALTITUDE_OUTSIDE_VERTICAL_ENVELOPE` (CRITICAL) |
| **(3) Fuel-Burn-Range** | Range = `[last_fuel - max_burn × gap_duration_s, last_fuel - 0]` (untere Grenze max-Burn, obere Grenze ist nicht-Verbrauchen bei Sim-Pause); `max_burn` Cruise=2,5 kg/s, Climb=3,5 kg/s, sonst 2 kg/s, on_ground = 1 kg/s | Post-Gap-Fuel **unterhalb** Range → `FUEL_OUTSIDE_BURN_RANGE_LOW` (ANOMALY); **oberhalb** Range (Fuel-Anstieg ohne Refuel-Phase) → `FUEL_OUTSIDE_BURN_RANGE_HIGH` (CRITICAL) |
| **(4) Phase-Continuity** | Erlaubte FSM-Transitions über die Lücke, basierend auf last_phase. Cruise → {Cruise, Descent}, Descent → {Descent, Approach, Final}, Final → {Final, Landing, TaxiIn} usw. | Post-Gap-Phase nicht aus erlaubter Set → `PHASE_CONTINUITY_VIOLATION` (CRITICAL) |

Phase-Continuity-Tabelle (R3 erweitert um Go-Around / Missed-Approach / Bounce):

```
ALLOWED_PHASE_TRANSITIONS_OVER_GAP = {
    "Boarding":      ["Boarding", "Pushback"],
    "Pushback":      ["Pushback", "TaxiOut"],
    "TaxiOut":       ["TaxiOut", "TakeoffRoll", "Takeoff"],
    "TakeoffRoll":   ["TakeoffRoll", "Takeoff", "Climb"],
    "Takeoff":       ["Takeoff", "Climb"],
    "Climb":         ["Climb", "Cruise", "Descent"],          # legit Step-Climb-Reversal
    "Cruise":        ["Cruise", "Descent"],
    "Descent":       ["Descent", "Approach", "Final", "Climb"], # R3: Climb-back legit bei Re-Routing
    "Approach":      ["Approach", "Final", "Landing", "Climb"], # R3: Missed-Approach
    "Final":         ["Final", "Landing", "TaxiIn",
                      "Approach", "Descent", "Climb"],         # R3: Go-Around (Final → Climb)
    "Landing":       ["Landing", "TaxiIn", "TakeoffRoll", "Climb"], # R3: Bounce / Touch-and-Go
    "TaxiIn":        ["TaxiIn", "BlocksOn"],
    "BlocksOn":      ["BlocksOn", "Arrived"],
    "Arrived":       ["Arrived"],
}
```

**Operating-State-Check für „airborne-after-arrival"-Transitions (R3 normativ):**

Die R3-Erweiterungen erlauben Go-Around (Final → Climb), Missed-Approach (Approach → Climb) und Bounce (Landing → TakeoffRoll/Climb). Diese Transitions sind aber nur dann legit wenn das Aircraft tatsächlich operating ist — sonst ist es ein Teleport-Artefakt. Zusätzliche Bedingung:

```python
def is_legit_airborne_after_arrival(target_phase, snapshot):
    """
    Wird zusätzlich zur Phase-Continuity-Tabelle geprüft für
    Final → {Climb, Approach, Descent}, Approach → Climb,
    Landing → {TakeoffRoll, Climb}.
    """
    if target_phase not in {"Climb", "Approach", "Descent", "TakeoffRoll"}:
        return True  # nicht-arrival-related, normal weiter
    
    operating_state_ok = (
        snapshot.engines_running > 0
        AND snapshot.parking_brake == False
        AND snapshot.groundspeed_kt > 30
        AND snapshot.indicated_airspeed_kt > 30
    )
    
    if not operating_state_ok:
        return False  # nicht legit — wahrscheinlich Teleport-Artefakt
    return True
```

Wenn `is_legit_airborne_after_arrival` False zurückgibt: Phase-Transition wird BLOCKIERT, `PHASE_CONTINUITY_VIOLATION` (CRITICAL) wird gesetzt — selbst wenn die Transition in der Tabelle steht. Der Operating-State-Check ist das harte Diskriminans zwischen legitimer Pilot-Aktion (Bounce, Go-Around) und Sim-Glitch (Teleport-zum-Boden-und-zurück).

**Spezialregel — explizit verboten (unverändert):**

```
from any AIRBORNE phase → directly to {on_ground in BlocksOn/Arrived}
without traversing {Landing → TaxiIn}
→ diese Combo löst zusätzlich AIR_TO_GROUND_TELEPORT aus
```

**Beispiel-Fälle:**

| Szenario | Transition | Operating-State | Verdikt |
|---|---|---|---|
| Pilot macht Touch-and-Go | Landing → TakeoffRoll | Engines on, brake off, GS=120 kt | ✅ legit, kein Flag |
| Pilot macht Go-Around bei 100 ft | Final → Climb | Engines on, brake off, IAS=140 kt | ✅ legit, kein Flag |
| Sim respawnt am Gate, dann airborne | Landing → TakeoffRoll | Engines off, brake on, GS=0 | ❌ CRITICAL — Teleport |
| WZZ2271-Style: Cruise → BlocksOn | Cruise → BlocksOn | irrelevant | ❌ CRITICAL — AIR_TO_GROUND_TELEPORT |

**WZZ2271-Konsequenz:**
- Gap-Edge 06:40 (351 s): last_phase = Descent, last_alt = 13.463 ft, last_fuel = 2.899 kg, last_on_ground = false
- Post-Gap: lat=51.454 (~31 NM weiter), alt = -359 ft, fuel = 7.000 kg, on_ground = true
- **(1) Reachable:** 31 NM in 351 s = 322 kt, innerhalb `max_GS_descent × 1.5 = 600 kt`-Radius → **OK** (Envelope-Check allein würde *nicht* flaggen)
- **(2) Vertical-Envelope:** Δ = -13.822 ft in 351 s = -2.366 fpm, innerhalb `max_VS_descent × gap = 6000 × 5.85 min = 35.100 ft`-Range → **OK** (auch Envelope-Check würde nicht flaggen)
- **(3) Fuel-Burn:** Δ = +4.101 kg (Anstieg!) → **`FUEL_OUTSIDE_BURN_RANGE_HIGH` CRITICAL** ✓
- **(4) Phase-Continuity:** Descent → on_ground bei lat=EHEH = effektiv `Descent → BlocksOn` ohne `Landing/TaxiIn`-Transit → **`PHASE_CONTINUITY_VIOLATION` CRITICAL + `AIR_TO_GROUND_TELEPORT` CRITICAL** ✓

Der **Hauptbefund** liegt in Dimensionen (3) + (4), nicht in (1) + (2). Die WZZ2271-Realität wäre damit zuverlässig erfasst, ohne dass eine zu-präzise Position-Erwartung false security suggeriert.

- Flag-Detail je Envelope-Violation: `{ mode: "gap_edge", gap_duration_s: <float>, dimension: "phase_continuity", envelope: {...}, actual: {...} }`

**Reihenfolge im Recorder bei TELEMETRY_GAP-Detection:**

```
1. Stream B detektiert TELEMETRY_GAP_LONG/CRITICAL
2. Stream A Gap-Edge-Mode prüft alle 4 Envelopes parallel
3. Jeder Envelope-Bruch → eigener Flag (können kombiniert auftreten)
4. Live-Aggregation-Pass kann auf effective CRITICAL eskalieren
```

### LE1 — Position-Δ-Check (phase-aware)

Bei jeder Snapshot-Ingestion wird die Großkreis-Distanz zum vorherigen verarbeiteten Snapshot berechnet (`haversine_m`). Zeit-Δ in Sekunden gemessen. Schwelle abhängig von der **aktuellen FSM-Phase** des vorherigen Samples:

| Phase | Position-Δ NOISE (filtert) | Position-Δ ANOMALY (> Schwelle) | Position-Δ CRITICAL (> Schwelle) |
|---|---|---|---|
| Boarding / Pushback / TaxiIn / BlocksOn / Arrived | < 50 m | 50 m – 0,5 NM in < 5 s | > 0,5 NM in < 5 s |
| TaxiOut / Landing | < 50 m | 50 m – 1 NM in < 5 s | > 1 NM in < 5 s |
| TakeoffRoll / Takeoff | < 200 m | 200 m – 2 NM in < 5 s | > 2 NM in < 5 s |
| Climb / Descent / Approach / Final | < 500 m | 500 m – 3 NM in < 5 s | > 3 NM in < 5 s |
| Cruise | < 1.000 m | 1 NM – 5 NM in < 5 s | > 5 NM in < 5 s |

**Begründung der Cruise-Schwelle:** 5 NM in 5 s = 3.600 kt. Schneller als jedes ziviles Flugzeug. Selbst Concorde war bei ~Mach 2 ≈ 1.150 kt — also < 1,6 NM/5 s.

**Quarantäne-Wirkung (Stream C):** CRITICAL-Samples werden als `quarantined = true` getaggt und von Phase-FSM + Scorer ignoriert (siehe LE11).

### LE2 — Altitude-Δ-Check

Reine MSL-Altitude-Δ-Prüfung, phase-unabhängig (zivile Flugzeuge haben überall die selbe Steig-/Sinkrate-Grenze):

| Altitude-Δ in < 5 s | Severity |
|---|---|
| < 50 ft | NOISE |
| 50 – 500 ft | INFO (normaler Verkehrsflug-Climb/Descent) |
| 500 – 1.500 ft | INFO (steiler Climb/Descent — F-15-Style aber in der Praxis OK für Steep-Climbs) |
| 1.500 – 3.000 ft | ANOMALY (> 18.000 fpm — unrealistisch für Verkehrsflugzeug) |
| > 3.000 ft | CRITICAL (> 36.000 fpm — physikalisch ausgeschlossen) |

**WZZ2271-Beleg:** Vier Sprünge mit > 30.000 ft Δ in < 3 s. Alle CRITICAL.

### LE3 — Fuel-Rate-Check (absolut, beide Richtungen)

Heutige Fehler-Annahme: „Fuel sinkt im Flug nur." Falsch. WZZ2271 zeigt symmetrische negative Sprünge von −4.151 kg in 8,4 s. Echter Check ist eine **absolute Rate**:

| `|Δ Fuel|` in < 5 s | Phase | Severity |
|---|---|---|
| < 10 kg | alle | NOISE |
| 10 – 100 kg | alle | INFO |
| 100 – 1.000 kg | airborne (Climb/Cruise/Descent/Approach/Final/Landing) | ANOMALY |
| 100 – 1.000 kg | on-ground (Boarding/Pushback/Taxi/BlocksOn/Arrived) | INFO (kann legit Refueling sein) |
| > 1.000 kg | alle Phasen | CRITICAL |

**Begründung:** A321 max realistischer Burn ~6.500 kg/h ≈ 1,8 kg/s. > 100 kg/5 s = > 20 kg/s ist 11× über Max-Burn — physikalisch ausgeschlossen für Non-Dump-Aircraft. > 1.000 kg/5 s wäre 200 kg/s — selbst Dump-Aircraft (Default-Dump-Rate ~25 kg/s) erreichen das nicht. CRITICAL hier ist unstrittig.

**Aircraft-Type-Awareness (A5):** v0.13.0 hartcodiert, ignoriert Dump-Fähigkeit. Wenn künftig A330/777/747 dazukommen, neuer Stream G in v0.13.x.

### LE4 — Weight-Konsistenz-Check (Cross-Field)

Eines der schärfsten Signale aus WZZ2271: zeitweise sinkt Fuel um 2.729,8 kg, aber `total_weight_kg` **steigt** um 3.570,2 kg. Das ist physikalisch nur erklärbar wenn ein anderes Gewichtsfeld (ZFW oder Payload) gleichzeitig manipuliert wurde.

Validator-Regel:

```
expected_weight = zfw_kg + fuel_total_kg
weight_delta_actual = total_weight_kg - expected_weight
```

| `|weight_delta_actual|` | Phase | Severity |
|---|---|---|
| < 50 kg | alle | NOISE (Floating-Point-Toleranz) |
| 50 – 500 kg | alle | INFO (kleinere Loadsheet-Inkonsistenz) |
| 500 – 2.000 kg | airborne | ANOMALY |
| > 2.000 kg | alle | CRITICAL |

Zusätzlich **Richtungs-Konsistenz-Check**:

```
if airborne_phase AND |Δ fuel| > 100 kg AND |Δ weight| > 100 kg:
    if sign(Δ fuel) != sign(Δ weight):
        → ANOMALY  (Fuel runter aber Weight rauf → unmöglich ohne Payload-Wechsel)
```

### LE5 — Ground-Elevation-Check

Bei WZZ2271 meldet der Snapshot mehrfach `on_ground = true` mit `altitude_msl_ft ≈ −359 ft` direkt am EHEH. EHEH liegt auf 79 ft MSL. Δ = 438 ft — physikalisch unmöglich (das Flugzeug wäre 134 m unter dem Asphalt).

Validator nutzt die bestehende Airport-Navdata aus `/api/va-airports`:

```
if on_ground AND lat/lon nahe bekanntem Airport (< 5 NM):
    airport_elev = lookup(nearest_airport).elevation_ft
    msl_delta = abs(altitude_msl_ft - airport_elev)
```

| `msl_delta` | Severity |
|---|---|
| < 50 ft | NOISE (Sim-Terrain-Genauigkeit) |
| 50 – 100 ft | INFO |
| 100 – 500 ft | ANOMALY |
| > 500 ft | CRITICAL |

**Fall „Airport nicht in Navdata":** Validator skippt (keine False-Positives bei unbekannten Bush-Strips).

### LE6 — on_ground/Phase-Transition-Check

Beobachtung WZZ2271: `on_ground = true`-Snapshot taucht plötzlich auf, ohne dass die FSM durch Landing-Phase gelaufen ist.

Regel:

```
if previous_phase ∈ {Climb, Cruise, Descent, Approach, Final} 
   AND current_snapshot.on_ground == true
   AND no Landing-Phase-Transition zwischen previous und current:
    → CRITICAL — Air-to-Ground-Teleport
```

Dies fängt den Hauptfall aus WZZ2271 (Teleport zum Ziel im Descent) auch dann ab, wenn die anderen Heuristiken (Position-Δ über Gap) noch versagen.

### LE7 — SIM_STATE_RESET_SIGNATURE-Erkennung

Spezialfall der häufigsten Crash-Reload-Signatur: Fuel snapt auf einen **exakten Default-Wert für das Aircraft-Modell**:

```
KNOWN_RESET_VALUES = {
    "A320": 6000,
    "A321": 7000,
    "A21N": 7000,    // A321neo
    "A20N": 6000,    // A320neo
    "B738": 5500,
    "B739": 6500,
    "B77W": 14000,
    "B748": 18000,
    "DH8D": 1900,
    // ... wird in der Codebase erweitert
}

if |Δ fuel| > 1.000 kg in < 5 s:
    new_fuel = current_snapshot.fuel_total_kg
    if any KNOWN_RESET_VALUES[aircraft_icao] satisfies |new_fuel - reset_value| < 50 kg:
        → SIM_STATE_RESET_SIGNATURE (CRITICAL) — zusätzlich zu FUEL_RATE_IMPOSSIBLE
```

Dieser Flag ist **diagnostisch wertvoller** als FUEL_RATE_IMPOSSIBLE allein — er sagt dem VA-Admin: „das war wahrscheinlich ein Sim-Crash-Reload, nicht ein Slew-Cheat" (Slew setzt Fuel nicht zurück).

### Stream A — Tests

| Test | Prüft |
|---|---|
| `position_delta_cruise_5nm_triggers_critical` | Cruise-Snapshot mit > 5 NM in 5 s → CRITICAL |
| `position_delta_taxi_50m_jitter_filtered` | TaxiOut mit < 50 m → NOISE, kein Flag |
| `altitude_delta_30000ft_in_3s_triggers_critical` | WZZ2271-Reproduktion → CRITICAL |
| `fuel_rate_dump_check_negative_jump_critical` | −4.100 kg in 8 s → CRITICAL |
| `fuel_rate_normal_burn_filtered` | Cruise mit 0,7 kg/s über 30 min → keine Flags |
| `weight_inconsistency_fuel_down_weight_up_anomaly` | Δ fuel = −2.700 kg + Δ weight = +3.500 kg → ANOMALY |
| `ground_elevation_eheh_minus_359_critical` | on_ground bei EHEH mit msl = −359 ft → CRITICAL (438 ft mismatch) |
| `air_to_ground_teleport_without_landing_phase_critical` | Cruise → on_ground ohne Landing-Phase → CRITICAL |
| `sim_state_reset_signature_a321_7000kg_detected` | Fuel-Sprung zu exakt 7.000 ±50 kg in A321 → SIM_STATE_RESET_SIGNATURE |
| `wzz2271_full_replay_produces_expected_flags` | E2E: gesamter WZZ2271-JSONL-Stream → mindestens 4× CRITICAL Position-Teleport, 4× FUEL_RATE_IMPOSSIBLE, 4× SIM_STATE_RESET_SIGNATURE, 1× GROUND_ELEVATION_MISMATCH, 1× AIR_TO_GROUND_TELEPORT |

---

## Stream B — Gap-Handler & Data-Quality

### B.0 Befund

WZZ2271 zeigt vier lange Telemetrie-Lücken (326 s, 621 s, 351 s, 301 s). **Die kritischen Zustandsbrüche liegen ausnahmslos an den Edges dieser Lücken.** Eine reine Sample-zu-Sample-Heuristik (Stream A) erkennt das teilweise — aber nur teilweise, weil der „Sprung" formal an dem **einen** Snapshot stattfindet, der nach einer 300+ s-Pause kommt. Stream B macht Telemetrie-Gaps zu **eigenständigen Events** und prüft Edge-Konflikte mit anderer Logik als kontinuierliche Streams.

Zusätzlich: WZZ2271-Snapshots haben über alle 2.612 Samples `fuel_used_kg = 0.0` und `fuel_flow_kg_per_h = null`. Das ist nicht „kein Glitch", sondern fehlende Sim-Adapter-Implementation. Wird als Data-Quality-Flag erfasst.

### LE8 — TELEMETRY_GAP-Detektion

Bei jeder Snapshot-Ingestion: Δ-Zeit zum vorherigen Snapshot.

| Gap-Dauer | Severity | Flag |
|---|---|---|
| < 15 s | NOISE | (Tick-Jitter, normal) |
| 15 – 60 s | INFO | `TELEMETRY_GAP_SHORT` |
| 60 – 300 s | ANOMALY | `TELEMETRY_GAP_LONG` |
| > 300 s | CRITICAL | `TELEMETRY_GAP_CRITICAL` |

**Phase-Awareness:** Gap-Schwellen verdoppeln sich in den Phasen Boarding/Pushback (Pilot pausiert oft am Gate), und **werden nicht erhoben** wenn `simulator.paused == true` zum Gap-Beginn (legit Sim-Pause).

Gap-Flag enthält Detail-Payload: `{ gap_start_ts, gap_end_ts, duration_s, previous_phase, next_phase }`.

### LE9 — GAP_EDGE_STATE_CONFLICT

**Wenn** ein Gap > 60 s erkannt wurde, läuft eine zusätzliche Edge-Konsistenz-Prüfung am ersten Snapshot **nach** dem Gap. Schwellen sind **strenger als Stream A**, weil die Annahme „Δ pro Sekunde" über lange Gaps nicht sinnvoll ist:

| Bedingung am Gap-Edge | Severity |
|---|---|
| `previous_phase` airborne **und** `current_snapshot.on_ground` | CRITICAL |
| `aircraft_icao` ändert sich | CRITICAL |
| Position-Δ > 50 NM | CRITICAL (Teleport, unabhängig von Gap-Dauer) |
| Position-Δ > erwartete Reisedistanz (Phase × Gap-Dauer × typische GS) × 2 | ANOMALY |
| Fuel-Δ > expected_burn(Gap-Dauer) ± 500 kg | ANOMALY |
| Fuel-Δ < −expected_burn × 3 | CRITICAL (Refuel im Flug → ausgeschlossen) |
| Fuel-Δ > +100 kg airborne | CRITICAL |

`expected_burn(seconds) = phase_burn_rate × seconds`. Burn-Rates pro Phase, hartcodiert auf A321-Profil (Standard-GSG-Aircraft; abweichende Aircraft-Typen wie Embraer/CRJ sind nicht Teil dieser Produktanforderung — die A321-Schwellen sind ausreichend tolerant, dass kleinere Aircraft nicht fälschlich geflagt werden):

```
phase_burn_rate_kg_per_h = {
    Cruise: 2500, Climb: 4500, Descent: 1500,
    Approach: 2000, Final: 2500, Landing: 1000,
    TaxiOut: 800, TaxiIn: 800, Boarding: 0, Pushback: 0, BlocksOn: 0
}
```

### LE10 — Post-Resume-Snapshot-Timeout

Eng verwandt mit Stream F (Resume-Policy), aber technisch Teil des Gap-Handlers:

```
on `flight_resumed`-Event:
    start timer (max_wait = 60 s)
    if no snapshot arrives within max_wait:
        → ABORT-Pfad triggern (Stream F)
        → RESUME_TIMEOUT_ABORTED-Flag
    if first snapshot arrives:
        → übergebe an Stream-F-Tier-Klassifikation
```

WZZ2271-Konnex: das `flight_resumed`-Event war bei 06:04:53, nächster Snapshot erst 06:09:24 — das wären 4 min 31 s Wartezeit. Mit der 60-s-Schwelle hätte v0.13.0 hier auf ABORT geschaltet.

### LE11 — FUEL_TELEMETRY_UNAVAILABLE

Beim Flight-Start sammelt der Recorder die ersten 10 Snapshots und prüft:

```
if all snapshots.fuel_flow_kg_per_h == null
   AND all snapshots.fuel_used_kg == 0.0:
    → FUEL_TELEMETRY_UNAVAILABLE (ANOMALY)
```

Allein: ANOMALY (führt zu „OFP COMPLIANCE not scored" wie heute, plus expliziter Flag-Eintrag).

**In Kombination mit `FUEL_RATE_IMPOSSIBLE` oder `WEIGHT_DISCONTINUITY`:** Severity-Eskalation auf CRITICAL, weil die fehlenden Telemetrie-Daten verhindern dass legit Fuel-Bewegungen plausibilisiert werden können.

### LE12 — AIRCRAFT_IDENTITY_INCONSISTENT (R1 verfeinert)

WZZ2271-Befund: `aircraft_registration` wechselt zwischen `C-GTLU` und `HA-LVA`, `aircraft_icao` zwischen `A21N` und `A321`, manche Felder werden zeitweise `null`. R0 hatte hier zu grobe Klassifikation — R1 macht explizite Family-Groupings.

**Aircraft-Family-Tabelle (R2: versioniert + data-driven):**

Mapping liegt in einer separaten versionierten JSON-Datei `/opt/aeroacars-live/recorder/data/aircraft_families.json`, nicht in Code:

```json
{
  "version": 1,
  "generated_at": "2026-05-24T10:00:00Z",
  "families": {
    "A320_FAMILY": { "manufacturer": "Airbus", "size_class": "narrow_body", "icao": ["A318", "A319", "A320", "A321", "A19N", "A20N", "A21N"] },
    "A330_FAMILY": { "manufacturer": "Airbus", "size_class": "wide_body", "icao": ["A332", "A333", "A338", "A339"] },
    "A350_FAMILY": { "manufacturer": "Airbus", "size_class": "wide_body", "icao": ["A359", "A35K"] },
    "A380_FAMILY": { "manufacturer": "Airbus", "size_class": "super_jumbo", "icao": ["A388"] },
    "B737_FAMILY": { "manufacturer": "Boeing", "size_class": "narrow_body", "icao": ["B736", "B737", "B738", "B739", "B73G", "B7M7", "B7M8", "B7M9"] },
    "B747_FAMILY": { "manufacturer": "Boeing", "size_class": "super_jumbo", "icao": ["B741", "B742", "B743", "B744", "B748"] },
    "B757_FAMILY": { "manufacturer": "Boeing", "size_class": "narrow_body", "icao": ["B752", "B753"] },
    "B767_FAMILY": { "manufacturer": "Boeing", "size_class": "wide_body", "icao": ["B762", "B763", "B764"] },
    "B777_FAMILY": { "manufacturer": "Boeing", "size_class": "wide_body", "icao": ["B772", "B773", "B77L", "B77W"] },
    "B787_FAMILY": { "manufacturer": "Boeing", "size_class": "wide_body", "icao": ["B788", "B789", "B78X"] },
    "CRJ_FAMILY":  { "manufacturer": "Bombardier", "size_class": "regional", "icao": ["CRJ2", "CRJ7", "CRJ9", "CRJX"] },
    "EMB_FAMILY":  { "manufacturer": "Embraer", "size_class": "regional", "icao": ["E170", "E175", "E190", "E195", "E290", "E295"] },
    "DH8_FAMILY":  { "manufacturer": "DeHavilland", "size_class": "regional", "icao": ["DH8A", "DH8B", "DH8C", "DH8D"] }
  }
}
```

**Update-Pfad:** Admin kann via `POST /api/admin/aircraft_families` neue Version hochladen (mit erhöhter `version`). Recorder lädt beim Boot die höchste verfügbare Version. Frühere Versionen bleiben für PIREP-Replays erhalten.

**`AIRCRAFT_TYPE_UNKNOWN` (R2 neu):** ICAO ist nicht in *keiner* Familie der aktiven Version → `AIRCRAFT_TYPE_UNKNOWN` Flag (Data-Quality, ANOMALY). Niemals CRITICAL allein — auch wenn der ICAO über den Flug stabil bleibt.

**Konsensuelles Identity-Profile pro Flight:**

```
on each snapshot:
    if aircraft_icao != null:
        identity.icao_observations[snapshot.aircraft_icao] += 1
    if aircraft_registration != null:
        identity.reg_observations[snapshot.aircraft_registration] += 1

at flight_ended:
    icao_set    = set(identity.icao_observations.keys())
    family_set  = set(family_of(icao) for icao in icao_set)
    mfr_set     = set(manufacturer_of(family) for family in family_set)
    null_ratio  = null_icao_samples / total_samples
```

**Klassifikations-Regel (R2 verfeinert — CRITICAL nur mit Operational-Konflikt):**

| Beobachtung | Flag | base_severity |
|---|---|---|
| Nur ein ICAO über alle Samples, ist in einer Familie | — | (kein Flag) |
| Nur ein ICAO, aber Familie unbekannt | `AIRCRAFT_TYPE_UNKNOWN` | ANOMALY (Data-Quality, R2) |
| `null_ratio < 0.05` (vereinzelte Sim-Adapter-Lücken) | — | NOISE, gefiltert |
| `null_ratio ≥ 0.50` | `AIRCRAFT_IDENTITY_NULL_DOMINANT` | ANOMALY (Data-Quality) |
| ICAO wechselt innerhalb derselben Familie (z. B. `A321` ↔ `A21N`) | `AIRCRAFT_IDENTITY_VARIANT_SHIFT` | INFO (Sim-Adapter-Glitch) |
| ICAO wechselt zwischen Familien desselben Herstellers (`A320` → `A330`) | `AIRCRAFT_IDENTITY_FAMILY_SHIFT` | ANOMALY |
| ICAO wechselt zwischen Herstellern (`A321` → `B738`) | `AIRCRAFT_IDENTITY_MANUFACTURER_SHIFT` | ANOMALY (R2 herabgestuft von CRITICAL) |
| ICAO wechselt zwischen Größenklassen | `AIRCRAFT_IDENTITY_SIZE_CLASS_SHIFT` | ANOMALY (R2 herabgestuft von CRITICAL) |
| Registration wechselt innerhalb derselben ICAO-Familie | `AIRCRAFT_REGISTRATION_INCONSISTENT` | INFO |

**CRITICAL-Eskalation (Operational-Conflict-Voraussetzung, R2 neu):**

ICAO-Wechsel allein wird in v0.13.0 nicht mehr als CRITICAL klassifiziert — das wäre zu False-Positive-anfällig (Sim-Adapter-Bugs sind häufig). CRITICAL nur wenn ein Identity-Shift mit einem **Operational-Konflikt** zusammenfällt:

```
def escalate_aircraft_identity_severity(identity_flag, session):
    if identity_flag.type not in [
        AIRCRAFT_IDENTITY_FAMILY_SHIFT,
        AIRCRAFT_IDENTITY_MANUFACTURER_SHIFT,
        AIRCRAFT_IDENTITY_SIZE_CLASS_SHIFT
    ]:
        return  # keine Eskalations-Kandidaten
    
    # Operational-Conflict-Bedingungen:
    operational_conflict = (
        identity_flag.shift_occurred_during_phase in AIRBORNE_PHASES
        OR session.has_flag(WEIGHT_DISCONTINUITY)
        OR session.has_flag(FUEL_RATE_IMPOSSIBLE)
        OR session.has_flag(SIM_STATE_RESET_SIGNATURE)
    )
    
    if operational_conflict:
        identity_flag.effective_severity = "critical"
```

Begründung: ein A321 → B738-Wechsel **am Gate** in Boarding-Phase ist mit hoher Wahrscheinlichkeit ein Sim-Adapter-Restart-Artefakt (Pilot hat Aircraft im Sim getauscht, AeroACARS hat die alte Session noch offen). Derselbe Wechsel **airborne im Cruise** ist physikalisch ausgeschlossen und CRITICAL. Operational-Conflict-Voraussetzung trennt beide Fälle.

**Aggregation (R2):** `AIRCRAFT_IDENTITY_VARIANT_SHIFT` (INFO) + jede `WEIGHT_DISCONTINUITY` → eskaliert auf ANOMALY (Hinweis dass es kein reiner Adapter-Glitch ist).

**WZZ2271-Klassifikation:**
- ICAO-Beobachtungen: `A21N` (dominant), `A321` (Variante in derselben Familie) → `AIRCRAFT_IDENTITY_VARIANT_SHIFT` (INFO)
- Registration: `HA-LVA` (dominant), `C-GTLU` (anderer Wert, gleiche ICAO-Familie) → `AIRCRAFT_REGISTRATION_INCONSISTENT` (INFO)
- Kombination mit den vorhandenen `WEIGHT_DISCONTINUITY`-Flags → Aggregation eskaliert ICAO-Flag auf ANOMALY

### Stream B — Tests

| Test | Prüft |
|---|---|
| `telemetry_gap_15s_filtered_as_noise` | 15 s-Lücke → kein Flag |
| `telemetry_gap_400s_triggers_critical` | 400 s-Lücke → TELEMETRY_GAP_CRITICAL |
| `gap_edge_air_to_ground_critical` | airborne vor Gap, on_ground nach Gap → GAP_EDGE_STATE_CONFLICT (CRITICAL) |
| `gap_edge_fuel_increased_500kg_airborne_critical` | airborne vor Gap, Fuel +500 kg nach Gap → CRITICAL |
| `gap_edge_aircraft_type_changed_critical` | A321 vor Gap, B738 nach Gap → CRITICAL |
| `post_resume_no_snapshot_60s_aborts` | flight_resumed-Event + kein Snapshot in 60 s → ABORT-Pfad |
| `fuel_telemetry_unavailable_alone_anomaly` | fuel_flow null + fuel_used 0 über 10 Samples → ANOMALY |
| `fuel_telemetry_unavailable_combined_critical` | FUEL_TELEMETRY_UNAVAILABLE + FUEL_RATE_IMPOSSIBLE → eskaliert auf CRITICAL |
| `aircraft_identity_a321_a21n_mix_anomaly` | beide Varianten in Samples → ANOMALY |
| `aircraft_identity_a321_b738_mix_critical` | echte Typen-Wechsel → CRITICAL |
| `wzz2271_four_gaps_all_critical` | E2E gegen WZZ2271-Log → 4× TELEMETRY_GAP_CRITICAL erfasst |

---

## Stream C — Downstream-Schutz (Pipeline, Quarantäne, FSM-Gating, Score-Trust, Post-Freeze)

### C.0 Befund

Heute fließen Snapshots ungebremst in Phase-FSM und Landing-Scorer. WZZ2271-Belege:

- Die FSM hat **Descent → Approach → Final** transitiert auf Basis eines Snapshots mit `altitude_msl_ft = −359`, `on_ground = true`, `groundspeed_kt = 0`, `parking_brake = true`. Das ist nicht „Approach" — das ist „Flugzeug steht abgestellt unterirdisch in Eindhoven".
- Der Landing-Scorer hat einen Touchdown mit `GS = 0`, `IAS = 0–6 kt`, `approach_window_sample_count = 0`, `pre_edge_sample_count = 0` mit 100/100 bewertet.
- **TaxiIn → BlocksOn-Transition** wurde aus dem Teleport-Sample ausgelöst — d. h. das gesamte PIREP-Final-Window ist aus einem korrupten Zustand definiert. Ein post-`pirep_filed`-Freeze allein reicht nicht, weil `pirep_filed` *nach* dem bogus BlocksOn kommt.

Stream C fügt eine **Quarantäne-Schicht** zwischen Validator und Downstream-Konsumenten ein und macht die Verarbeitungs-Reihenfolge normativ.

### C.1 — Normative Recorder-Ingestion-Pipeline (R1)

Diese Reihenfolge ist **BINDEND** für jeden eingehenden Snapshot. Jeder Schritt ist Test-Target — Code-Review prüft dass kein Bypass existiert.

```
1. snapshot.receive()

2. Stream A Continuous-Mode (LE1–LE7) → schreibt integrity_flags

3. Stream B Gap-Check (LE8) → schreibt TELEMETRY_GAP wenn dt > 60 s

4. IF TELEMETRY_GAP_LONG ODER TELEMETRY_GAP_CRITICAL:
       Stream A Gap-Edge-Mode (A.1) gegen letzten clean Pre-Gap-Sample
       → schreibt zusätzliche integrity_flags mit mode='gap_edge'
   END

5. Stream B Gap-Edge-Konsistenz (LE9) → GAP_EDGE_STATE_CONFLICT

6. **Live-Aggregations-Pass (R4 korrekt platziert):**
       - läuft über ALLE bisherigen Flags der Session (alt + neu aus Schritt 2–5)
       - berechnet effective_severity je Flag per Combo-Regeln
       - aktualisiert session.effective_severity (rolling max)
       - bei Eskalation eines älteren Flags: trigger Recompute-Pfad (Stream C)
       - aktualisiert PIREP-cached score_trust_level, requires_review

7. Quarantine-Filter (LE13) → setzt snapshot.quarantine_status
       - berücksichtigt nicht nur snapshot-lokale Flags, sondern auch
         session.effective_severity aus Schritt 6

8. snapshot.persist()  -- ALLE Snapshots, auch quarantäniert

9. IF snapshot.quarantine_status == "critical" ODER session.effective_severity gerade auf critical eskaliert:
       - SKIP Phase-FSM (LE14)
       - SKIP Touchdown-Detector
       - SKIP Landing-Scorer
       - PUSH MQTT integrity_event (Stream D LE19) mit aktueller effective_severity
       - RETURN
   END

10. Phase-FSM.evaluate(snapshot):
        - bei Transition-Versuch zu {Landing, TaxiIn, BlocksOn, Arrived}:
            IF snapshot.quarantine_status != "clean":
                REJECT Transition (LE16b)
                write integrity_flag(PHASE_TRANSITION_REJECTED) — geht zurück durch Aggregation
                RETURN
            END
        - sonst: normale FSM-Logik

11. IF Phase ∈ {Final, Landing}:
        Touchdown-Detector.evaluate(snapshot)

12. IF Touchdown-Event ausgelöst:
        Landing-Scorer.evaluate(snapshot, window)
        — Scorer nutzt schon-eskalierte effective_severity, nicht base_severity

13. PIREP-Finalize-Pass (wird ausgelöst durch pirep_filed-Event):
        - KEIN „erstes Mal Aggregation" — alle effective_severities sind bereits live in Schritt 6 berechnet
        - finaler Sanity-Check-Aggregations-Pass (idempotent, keine Änderungen erwartet)
        - schreibt finalen score_trust_level, requires_review, score_trust_reasons als persistierte computed fields
        - Snapshot-Stream nach pirep_filed → in post_flight_events (LE16)
```

**Wichtig:** Die Sequenz 1–12 läuft pro Snapshot, atomar in einer DB-Transaktion. Sequenz 13 läuft einmal pro PIREP nur zur Konsolidierung — sie ändert keine Flags mehr. Schritte 6, 9, 10 sind die kritischen Gates: Aggregation läuft vor FSM (P1-1-Fix R4), Quarantäne läuft vor FSM/Scorer, BlocksOn-Reject läuft in FSM. Wenn alle drei korrekt implementiert sind, kann ein suspect Sample weder Phase-Transition triggern noch in den Landing-Scorer fließen — und FSM/Scorer sehen immer die aktuelle effective_severity, nicht eine veraltete base_severity.

### LE13 — Quarantäne-Mechanik

Jeder Snapshot bekommt nach Stream-A/B-Validation ein neues Attribut:

```
snapshot.quarantine_status ∈ { "clean", "anomaly", "critical" }
snapshot.integrity_flags : list of { type, severity, detail }
```

Verarbeitungs-Reihenfolge im Recorder:

```
incoming_snapshot
    → Stream-A-Validator    (tags integrity_flags, calculates quarantine_status)
    → Stream-B-Gap-Check    (tags additional flags)
    ↓
    if quarantine_status == "critical":
        → snapshot wird persistiert (für Forensik)
        → snapshot wird NICHT an Phase-FSM weitergereicht
        → snapshot wird NICHT an Landing-Scorer weitergereicht
        → snapshot wird an MQTT-Push-Topic weitergereicht (Client-Toast)
    if quarantine_status == "anomaly":
        → snapshot wird normal verarbeitet
        → integrity_flags werden mit-persistiert
        → Client-Toast (gelb, 3 s Auto-Hide)
    if quarantine_status == "clean":
        → normaler Pfad
```

### LE14 — Phase-FSM-Gating

Phase-FSM ignoriert quarantänierte Snapshots für **State-Transitions**, aber zeigt sie weiterhin im Activity-Log:

```
on each snapshot in fsm:
    if snapshot.quarantine_status == "critical":
        log("phase-fsm skipped quarantined snapshot at <ts>")
        return  # no state evaluation
    else:
        evaluate_state_transition(snapshot)
```

**Konsequenz für WZZ2271:** Der Teleport-Snapshot bei 06:46 (`on_ground = true` aus dem Nichts) hätte CRITICAL-Quarantäne bekommen. Die FSM wäre in **Descent** geblieben statt nach Approach/Final zu wechseln. Der Landing-Scorer wäre nie auf den Touchdown gefeuert worden (kein Final-Phase-Eintritt).

### LE15 — Landing-Score-Trust (R1 verschärft)

Neue computed fields auf jedem PIREP:

```
score_trust_level     ∈ { "trusted", "review", "untrusted" }
landing_score_trusted : boolean (Alias: true ⇔ score_trust_level == "trusted")
```

**Hard-Untrusted-Trigger (R1):** Die folgenden Bedingungen führen IMMER zu `untrusted`, unabhängig von der weiteren Logik. **Wenn auch nur eine zutrifft, ist die Bewertung kompromittiert:**

```
HARD_UNTRUSTED if ANY of:
  - touchdown_snapshot.groundspeed_kt        < 30
  - touchdown_snapshot.indicated_airspeed_kt < 30        # R1 NEU
  - touchdown_snapshot.parking_brake         == true
  - touchdown_snapshot.engines_running       == 0
  - touchdown_snapshot.quarantine_status     != "clean"  # R1 NEU
  - approach_window_sample_count             == 0
  - pre_edge_sample_count                    == 0
  - any flag in pirep.integrity_flags with
       effective_severity == "critical"
       AND flag.ts in [touchdown_ts - 60s, touchdown_ts + 30s]    # R1 EXPLIZIT
```

**Berechnungs-Regel:**

```
if HARD_UNTRUSTED:
    score_trust_level     = "untrusted"
    landing_score_trusted = false

elif any flag with effective_severity == "critical":
    score_trust_level     = "review"
    landing_score_trusted = false

elif any flag with effective_severity == "anomaly":
    score_trust_level     = "review"
    landing_score_trusted = false

else:
    score_trust_level     = "trusted"
    landing_score_trusted = true
```

Zusätzlich `score_trust_reasons : list of human-readable strings` für die UI:

```
[
    "Touchdown bei Groundspeed = 0 kt (Aircraft stand)",
    "Touchdown bei IAS < 30 kt — Aircraft hatte keine Anflug-Geschwindigkeit",
    "Parking-Brake war beim Touchdown gesetzt",
    "Approach-Stability-Window hatte 0 Samples (kein bewertbarer Anflug)",
    "Touchdown-Sample selbst war quarantäniert (integrity_flags vorhanden)",
    "CRITICAL Integrity-Flag im Touchdown-Fenster: FUEL_RATE_IMPOSSIBLE bei TD-12s"
]
```

**WZZ2271-Konsequenz:** alle obigen Hard-Triggers feuern (GS=0, IAS<30, Parking-Brake=true, Touchdown aus quarantäniertem Sample, CRITICAL-Flag im Fenster). `score_trust_level = "untrusted"`, `landing_score_trusted = false`.

**Effekt auf Bestenlisten:** Bestenliste (`SELECT ... FROM pireps WHERE score_trust_level = 'trusted'`) — `untrusted`/`review`-PIREPs sind im PIREP-View des Piloten sichtbar (mit rotem Badge), aber nicht im Public-Leaderboard.

### LE16b — BlocksOn / TaxiIn / Arrived nur aus clean Samples (R1 neu)

**Befund:** Bei WZZ2271 wurde die TaxiIn → BlocksOn-Transition aus dem Teleport-Sample (`on_ground = true`, lat 51.454 = EHEH, AGL = −359 ft) gefeuert. Dadurch entstand der gesamte PIREP-Final-Zustand aus einem korrupten Snapshot. Ein post-`pirep_filed`-Freeze (LE16) allein reicht nicht — `pirep_filed` kommt *nach* dem bogus BlocksOn.

**Regel (normativ):** Phase-Transitions zu **Arrival-Phasen** dürfen NICHT aus einem Snapshot mit `quarantine_status != "clean"` ausgelöst werden.

Geschützte Ziel-Phasen: `Landing`, `TaxiIn`, `BlocksOn`, `Arrived`.

```python
def evaluate_transition(target_phase, snapshot, fsm):
    PROTECTED = {"Landing", "TaxiIn", "BlocksOn", "Arrived"}
    if target_phase in PROTECTED and snapshot.quarantine_status != "clean":
        fsm.log("phase_transition_rejected_from_quarantined_sample")
        fsm.add_integrity_flag({
            "type": "PHASE_TRANSITION_REJECTED",
            "base_severity": "anomaly",
            "phase_target": target_phase,
            "rejecting_quarantine_flags": snapshot.integrity_flags,
            "snapshot_ts": snapshot.ts
        })
        return  # FSM bleibt im vorherigen State
    proceed_with_transition(target_phase, snapshot, fsm)
```

**Konsequenz für WZZ2271:** Phase wäre bei Descent geblieben statt zu Approach/Final/Landing zu wechseln. Touchdown-Detection wäre nie gefeuert worden. PIREP wäre vom Recorder als „abgebrochen / nicht final" gefiled worden (oder gar nicht — siehe Recovery-Pfad unten) — was den Realzustand korrekt abbildet.

**Recovery-Pfad:** Wenn nach einem `PHASE_TRANSITION_REJECTED`-Event clean Samples wieder eintreffen und die Transition-Bedingung erneut erfüllt ist, kann die FSM die Transition normal vollziehen. Das Reject-Event bleibt im PIREP als Flag erhalten und ist Beweisstück für den VA-Admin.

**Aggregations-Konsequenz:** Wenn ein PIREP genau die `PHASE_TRANSITION_REJECTED`-Flag enthält und trotzdem in einer Arrival-Phase landet, eskaliert der Aggregations-Pass das Flag von ANOMALY auf CRITICAL — denn dann ist die FSM trotz initialem Reject später durchgewunken worden, und die Frage „warum" muss vom VA-Admin beantwortet werden.

### LE16c — Safe Session Termination (R2 neu)

**Befund:** LE16b blockt korrekt jeden Arrival-Transition-Versuch aus quarantäniertem Sample. Aber: ohne Recovery-Pfad entsteht ein neuer Orphan-Session-Fall — die Session läuft endlos, FSM bleibt in der Pre-Arrival-Phase, kein PIREP wird gefiled. Stream C braucht einen **sicheren Endpfad**.

**Regel:** Wenn FSM in einer „Stuck"-Situation festsitzt (Arrival-Transition wiederholt geblockt), wird die Session nach Schwellwerten als **partial-PIREP** gefiled. Schwellwerte:

| Bedingung | Trigger Safe-Termination |
|---|---|
| `N_consecutive_rejected_transitions` ≥ **5** | ja |
| `time_since_last_clean_phase_transition` ≥ **15 min** | ja |
| Session-Dauer überschreitet `2 × erwartete_flugzeit + 30 min` (basierend auf great-circle dep→arr) | ja |
| Pilot beendet AeroACARS während stuck-Phase | ja (sofortiger Trigger) |

**Safe-Termination-Pfad:**

```python
def safe_session_termination(session, reason):
    last_clean_sample = session.last_snapshot_with_quarantine_status_clean()
    
    # PIREP wird auf Basis des letzten clean Samples gefiled
    pirep = build_pirep(
        last_clean_phase = last_clean_sample.phase,
        last_clean_state = last_clean_sample,
        termination_reason = reason,  # 'blocked_by_quarantine', 'pilot_aborted_during_stuck', 'timeout'
    )
    
    # KEINE Landing-/BlocksOn-Metriken — kein vertrauenswürdiges Touchdown-Sample
    pirep.landing_metrics = None
    pirep.blocks_on_metrics = None
    pirep.touchdown_data = None
    
    # PIREP-Status
    pirep.status = "partial"
    pirep.requires_review = True
    pirep.score_trust_level = "untrusted"
    pirep.landing_score_trusted = False
    
    # Spezial-Flag
    pirep.integrity_flags.add({
        type: "SESSION_TERMINATED_VIA_SAFE_PATH",
        base_severity: "critical",
        reason: reason,
        last_clean_snapshot_ts: last_clean_sample.ts,
        last_clean_phase: last_clean_sample.phase,
        rejected_transition_count: session.rejected_count
    })
    
    pirep.file()  # geht in DB, Webapp-Review-Queue
```

**Konsequenz:** Session kann immer beendet werden, auch wenn alle Arrival-Samples kaputt sind. VA-Admin sieht klar: „Pilot hat versucht zu landen, Recorder konnte keine vertrauenswürdige Landing-Phase erkennen, hier sind die letzten guten Daten."

**Statistik-Sperre für Partial-PIREPs (R3 normativ):**

Partial-/safe-terminated PIREPs sind **explizit ausgeschlossen aus ALLEN Statistik-Aggregaten**, bis der VA-Admin in der Review-Queue eine Entscheidung trifft. Konkret:

| Aggregat | Verhalten für partial-PIREP |
|---|---|
| **Touchdown-Bestenliste** (Heatmap, Top-X, Greaser-Liste) | ausgeschlossen — kein Touchdown gewertet (auch wenn ein bogus Touchdown-Sample existiert) |
| **Route-Stats** (km geflogen, Flugstunden, Routen-Frequenz) | ausgeschlossen — keine vertrauenswürdige Arrival-Position |
| **Fuel-Stats** (Block-to-Block-Verbrauch, OFP-Compliance) | ausgeschlossen — Fuel-Bilanz wahrscheinlich kompromittiert |
| **Completion-Stats** (PIREPs filed, On-Time-Performance) | ausgeschlossen — Flug nicht regulär beendet |
| **Pilot-Stats** (Total-Stunden, Aircraft-Stats, Airport-Stats) | ausgeschlossen |
| **VA-Stats** (Top-Pilot, Route-Häufigkeit) | ausgeschlossen |
| **Forensik-View** | sichtbar mit klarem „PARTIAL — SAFE-TERMINATED"-Badge |
| **Review-Queue** | sichtbar als requires_review = true |

**Sperre-Implementierung (R4 korrigiert):** Bestehende Stats-Queries bekommen ein zusätzliches WHERE-Filter:

```sql
WHERE (
  status != 'partial'
  OR review_state = 'accepted_with_warning'
)
AND review_state NOT IN ('open', 'acknowledged')   -- nichts unentschiedenes
AND review_decision != 'rejected'                  -- explizit abgelehnte raus
```

**Verhalten je Admin-Entscheidung:**

| review_state | partial-PIREP fließt in Stats? |
|---|---|
| `open` (initialer Zustand nach safe-termination) | NEIN |
| `acknowledged` | NEIN (Admin hat nur gesehen, nicht entschieden) |
| `accepted_with_warning` | **JA** (mit Warn-Badge sichtbar) |
| `rejected` | NEIN (final ausgeschlossen) |
| `dismissed_false_positive` | NEIN (PIREP-Daten werden als nicht-valide gesehen) |

R3-Bug-Fix: alter Filter `status != 'partial' AND ...` hätte `accepted_with_warning`-PIREPs **trotzdem** ausgeschlossen — Admin-Entscheidung war wirkungslos. R4 macht den Filter review-state-aware: `accepted_with_warning` ist explizite Ausnahme zur `status != 'partial'`-Regel.

**Wichtig:** dieser Mechanismus gilt **zusätzlich** zur normalen `score_trust_level`-Filterung. Ein partial-PIREP hat sowohl `status = 'partial'` als auch `score_trust_level = 'untrusted'`. Bei `accepted_with_warning` bleibt der `score_trust_level` auf untrusted (das ist eine technische Eigenschaft der Daten), aber der PIREP fließt in die Stats — das ist Admin-Entscheidung über die Daten-Validität.

**Recovery-Variante:** Bevor Safe-Termination triggert, kann ein clean Sample die FSM noch retten (Recovery-Pfad aus LE16b). Erst wenn das **wiederholt** scheitert, kommt LE16c zum Tragen.

**Edge-Case-Tests (siehe Stream C — Tests):**

- `safe_termination_after_5_rejected_transitions_files_partial` — nach 5×Reject → partial-PIREP
- `safe_termination_uses_last_clean_sample_for_pirep_state` — PIREP-Werte = last clean snapshot, NICHT quarantine-Sample
- `safe_termination_landing_metrics_are_null` — kein landing_rate, kein touchdown_vs, kein score
- `safe_termination_clean_sample_within_threshold_recovers_normally` — wenn vor Reach 5×Reject clean Sample kommt → kein Safe-Path, normale FSM
- `safe_termination_pilot_abort_during_stuck_immediate` — Pilot schließt App → sofort Safe-Path

### LE16 — Post-pirep_filed-Freeze

Alle Snapshots **nach** dem `pirep_filed`-Event landen in einer separaten Quarantäne-Spalte `post_flight_events`:

```
pirep.post_flight_events : JSON array of {
    ts, snapshot_summary, integrity_flags
}
```

Diese Daten fließen NICHT in:
- PIREP-Score
- Phase-FSM-Bewertung
- Touchdown-Forensik-Statistiken
- Bestenlisten

Sie sind sichtbar im Forensik-View für VA-Admin (nützlich um Sim-Verhalten nach „Flug-Ende" zu verstehen — z. B. Spawn-Reset, Sim-Crash beim Aussteigen).

**WZZ2271-Konnex:** `BlocksOn` 07:00:03, danach Anomalie 07:00:11 (14,16 NM Position-Δ, +6.075 ft). Diese hätten **nicht** PIREP-Wertung verändern dürfen — Stream F-Freeze macht das jetzt korrekt.

### Stream C — Tests

| Test | Prüft |
|---|---|
| `ingestion_pipeline_runs_validator_before_fsm` | normative Reihenfolge: Validator-Flags sind gesetzt BEVOR FSM-Logik den Snapshot sieht |
| `ingestion_pipeline_critical_snapshot_skips_fsm_and_scorer` | quarantine_status='critical' → FSM unverändert, Scorer nicht aufgerufen |
| `quarantined_snapshot_skipped_by_fsm` | snapshot.quarantine_status='critical' → FSM bleibt im vorherigen State |
| `quarantined_snapshot_skipped_by_scorer` | CRITICAL-quarantäniertes Touchdown-Sample → Scorer feuert nicht |
| `blocks_on_transition_from_quarantined_sample_rejected` | TaxiIn → BlocksOn-Versuch mit quarantine_status='critical' → REJECT, PHASE_TRANSITION_REJECTED-Flag gesetzt |
| `arrived_transition_from_anomaly_sample_rejected` | jede Arrival-Phase-Transition aus non-clean Sample → REJECT |
| `phase_transition_rejected_then_recovered_keeps_flag` | nach REJECT + clean Samples + späterer normaler Transition → PHASE_TRANSITION_REJECTED bleibt im PIREP |
| `phase_transition_rejected_aggregates_to_critical_if_arrived_anyway` | PIREP endet in Arrival trotz REJECT → Aggregations-Pass eskaliert auf effective CRITICAL |
| `score_trust_untrusted_on_gs_zero_touchdown` | GS = 0 beim Touchdown → score_trust_level = 'untrusted' |
| `score_trust_untrusted_on_ias_below_30_touchdown` | IAS < 30 kt beim Touchdown → 'untrusted' (R1) |
| `score_trust_untrusted_on_parking_brake_touchdown` | parking_brake = true beim Touchdown → 'untrusted' |
| `score_trust_untrusted_on_engines_off_touchdown` | engines_running = 0 beim Touchdown → 'untrusted' |
| `score_trust_untrusted_on_quarantined_touchdown_sample` | touchdown_snapshot.quarantine_status != 'clean' → 'untrusted' (R1) |
| `score_trust_review_on_critical_far_from_touchdown` | CRITICAL-Flag 30 min vor Touchdown → 'review' (nicht 'untrusted') |
| `score_trust_untrusted_on_critical_in_touchdown_window` | CRITICAL-Flag im [TD−60s; TD+30s]-Fenster → 'untrusted' |
| `landing_score_trusted_alias_matches_level` | landing_score_trusted == (score_trust_level == 'trusted') |
| `aggregation_fuel_telemetry_unavail_plus_fuel_rate_escalates_to_critical` | FUEL_TELEMETRY_UNAVAILABLE (ANOMALY base) + FUEL_RATE_IMPOSSIBLE → effective CRITICAL |
| `aggregation_three_distinct_anomalies_sets_requires_review` | 3 unterschiedliche ANOMALY-Typen → requires_review = true ohne CRITICAL einzeln |
| `leaderboard_excludes_untrusted_pireps` | Bestenliste-Query liefert keine untrusted PIREPs |
| `post_pirep_filed_anomaly_in_separate_table` | Snapshot nach pirep_filed-Event → in post_flight_events, NICHT in pirep.integrity_flags |
| `wzz2271_results_in_untrusted_score` | E2E: WZZ2271-Log → score_trust_level = 'untrusted', mehrere score_trust_reasons gefüllt |
| `wzz2271_blocks_on_transition_was_rejected` | E2E: WZZ2271 BlocksOn-Versuch aus Teleport-Sample → REJECT, FSM bleibt in Descent |

---

## Stream D — Persistence & Surfacing

### D.0 Befund

Die bestehende DB hat keine Felder für Integrity-Flags, kein Trust-Level, keine Quarantäne. Das Webapp hat keinen Review-Queue-Tab und keine Badges. Stream D fügt das saubere Datenmodell + UI-Surfacing hinzu — getrennt vom UI-flow, damit Backend und Frontend in eigenen QS-Runden laufen können.

### LE17 — DB-Migration

Neue Spalten auf `pireps`:

```sql
ALTER TABLE pireps ADD COLUMN integrity_flags TEXT DEFAULT '[]';
    -- JSON array of { type, severity, ts, phase, detail }
ALTER TABLE pireps ADD COLUMN score_trust_level TEXT DEFAULT 'trusted';
    -- 'trusted' | 'review' | 'untrusted'
ALTER TABLE pireps ADD COLUMN score_trust_reasons TEXT DEFAULT '[]';
    -- JSON array of strings
ALTER TABLE pireps ADD COLUMN requires_review INTEGER DEFAULT 0;
    -- boolean; computed: any flag.severity == 'critical' OR score_trust_level != 'trusted'
ALTER TABLE pireps ADD COLUMN post_flight_events TEXT DEFAULT '[]';
    -- JSON array of post-pirep_filed snapshots/events
ALTER TABLE pireps ADD COLUMN reviewed_at INTEGER;
    -- unix-ms when VA-Admin reviewed; NULL = pending
ALTER TABLE pireps ADD COLUMN review_decision TEXT;
    -- 'accepted' | 'rejected' | NULL when pending
ALTER TABLE pireps ADD COLUMN review_notes TEXT;
    -- free-text from VA-Admin
ALTER TABLE pireps ADD COLUMN reviewed_by TEXT;
    -- VA-Admin-Identifier
```

Neue Spalten auf `flight_sessions`:

```sql
ALTER TABLE flight_sessions ADD COLUMN integrity_flags TEXT DEFAULT '[]';
ALTER TABLE flight_sessions ADD COLUMN clean_shutdown_reason TEXT;
    -- 'system_restart' | 'update' | 'user_close' | 'crash' | NULL
ALTER TABLE flight_sessions ADD COLUMN resume_history TEXT DEFAULT '[]';
    -- JSON array of { ts, tier ('seamless'|'guided'|'reposition'|'abort'), 
    --                 gap_duration_s, state_diff, accepted_by_pilot }
```

Neuer Index:

```sql
CREATE INDEX idx_pireps_requires_review ON pireps(requires_review, ts DESC);
CREATE INDEX idx_pireps_score_trust ON pireps(score_trust_level, ts DESC);
```

**Backfill (per Entscheidung A3 verworfen):** Migration setzt für alle bestehenden PIREPs `integrity_flags = '[]'`, `score_trust_level = 'trusted'`, `requires_review = 0`. Alt-PIREPs bleiben unangetastet — neuer Validator wirkt nur für ab-jetzt-eingehende Flüge.

### LE18 — Recorder-API-Erweiterungen

Neue Endpoints im Recorder (Pfad-Konvention konsistent mit bestehenden Admin-Endpoints):

```
GET  /api/admin/review-queue
  Query: ?limit=50&offset=0&severity=critical|anomaly|all
  Auth:  requireBearerAdminRead
  Resp:  { total, items: [pirep_summary_with_flags] }

GET  /api/admin/pireps/:id/integrity
  Resp:  { pirep, integrity_flags[], score_trust_reasons[], 
           post_flight_events[], resume_history[] }

POST /api/admin/pireps/:id/review
  Auth:  requireBearerAdminWrite
  Body:  { decision: 'accepted'|'rejected', notes?: string }
  Wirkung: setzt reviewed_at, review_decision, review_notes
           wenn 'rejected' → score_trust_level bleibt, aber PIREP ist explicit raus aus Leaderboard

GET  /api/pireps/:id (existierend)
  → Response wird erweitert um: integrity_flags, score_trust_level, score_trust_reasons
```

Erweiterung von **`GET /api/heatmap/touchdowns`** und **`GET /api/touchdowns`**: Default-Filter `score_trust_level = 'trusted'`. Query-Param `?include_untrusted=1` erlaubt VA-Admin-Sicht aller PIREPs.

### LE19 — MQTT-Topic-Erweiterung (für Client-Realtime-Toast)

Neues Subtopic im bestehenden Topic-Schema:

```
aeroacars/<vaPrefix>/<pilotId>/integrity_event
```

Recorder pusht bei jedem ANOMALY/CRITICAL-Event ein Event-Payload:

```json
{
  "ts": 1779000000000,
  "type": "FUEL_RATE_IMPOSSIBLE",
  "severity": "critical",
  "phase": "Descent",
  "detail": {
    "fuel_delta_kg": -4101,
    "duration_s": 3.3,
    "rate_kg_per_s": 1243
  },
  "user_message_de": "Sim-State-Bruch erkannt: Treibstoff-Sprung -4.101 kg in 3 Sekunden. Der Flug wird zur Überprüfung markiert.",
  "user_message_en": "Sim state break detected: fuel jump -4,101 kg in 3 seconds. Flight will be flagged for review."
}
```

Client (Stream A1-Replikation, siehe oben) abonniert das Topic für den eigenen Piloten — selbst wenn der eigene Validator das nicht erkannt hat (z. B. weil ein Race im Client-State auftrat), zeigt er den Toast.

### LE20 — Webapp Review-Queue-Tab

Neuer Tab `Review` im Admin-Tab-Bar (zwischen `Reports` und `Diagnostics`).

**Tab-Layout:**

```
┌──────────────────────────────────────────────────────────────┐
│ Review Queue · 12 wartet auf Entscheidung                    │
├──────────────────────────────────────────────────────────────┤
│ Filter: [● Critical] [○ Anomaly] [○ Alle] | Sortierung: Neuste │
├──────────────────────────────────────────────────────────────┤
│ ┌─ WZZ2271 · LHBP→EHEH · 24.5.2026 · gsg/2 ─────────────────┐ │
│ │ [SCORE_UNTRUSTED] [4× CRITICAL] [3× ANOMALY]              │ │
│ │ Touchdown: −28 fpm A+ (untrusted)                          │ │
│ │ Hauptbefunde:                                              │ │
│ │  · SIM_STATE_RESET_SIGNATURE (4×)                          │ │
│ │  · AIR_TO_GROUND_TELEPORT (1×, EHEH)                       │ │
│ │  · WEIGHT_DISCONTINUITY (3×)                               │ │
│ │ [Forensik öffnen] [Akzeptieren] [Ablehnen]                 │ │
│ └────────────────────────────────────────────────────────────┘ │
│ ... weitere ...                                               │
└──────────────────────────────────────────────────────────────┘
```

**Forensik-Drill-Down:** Klick auf „Forensik öffnen" → vorhandener PIREP-Detail-View, erweitert um:
- Integrity-Flag-Timeline (synchron mit V/S-Profil-Chart)
- Diff-Anzeige bei `RESUME_AFTER_REPOSITION` (vorher/nachher)
- `post_flight_events`-Block am Ende

### LE21b — Admin-Audit-State-Machine (R2 neu)

**Befund:** R1 Cross-Surface-Matrix hat 5 Admin-Actions definiert, aber keinen State-Fluss. Ohne State-Maschine ist die Review-Queue sichtbar aber nicht revisionssicher — jeder Klick überschreibt den letzten ohne Audit-Trail.

**State-Maschine (R3 mit `reopen`):**

```
                  ┌─→ accepted_with_warning ──┐
                  │                            │
   open ─→ acknowledged ─→ rejected ──────────┤
                  │                            │
                  └─→ dismissed_false_positive ┤
                                               │
                                          (terminal)
                                               │
                                               │  reopen
                                               ↓  (mit reason)
                                              open
```

**`reopen`-Transition (R3 neu):** jedes terminale State kann via `reopen` zurück zu `open`. Pflicht-Reason. Audit-Trail capturt den Reopen-Event. Use-Case: VA-Admin entdeckt nachträglich dass eine Reject-Entscheidung falsch war (z. B. Pilot meldet sich später mit X-Plane-Crash-Beweis) — Admin kann den PIREP wieder öffnen ohne SQL-Hack.

**Pirep-Review-States:**

| State | Bedeutung | Terminal? |
|---|---|---|
| `open` | Default für jedes neue PIREP mit requires_review=true | nein |
| `acknowledged` | Admin hat den PIREP gesehen, keine Entscheidung getroffen | nein |
| `accepted_with_warning` | PIREP-Score gilt, Flag bleibt sichtbar als Hinweis | ja |
| `rejected` | PIREP-Score ausgeschlossen aus Bestenliste/Statistik | ja |
| `dismissed_false_positive` | Flag als FP markiert, PIREP zurück zu `trusted` wenn keine anderen Flags bleiben | ja |

**Transitions-Tabelle:**

| Von | Nach | Erlaubt? | Pflichtfelder |
|---|---|---|---|
| open | acknowledged | ja | actor, ts |
| open | accepted_with_warning | ja | actor, ts, note (optional) |
| open | rejected | ja | actor, ts, **reason (REQUIRED)** |
| open | dismissed_false_positive | ja | actor, ts, **reason (REQUIRED)** |
| acknowledged | accepted_with_warning | ja | actor, ts, note (optional) |
| acknowledged | rejected | ja | actor, ts, **reason (REQUIRED)** |
| acknowledged | dismissed_false_positive | ja | actor, ts, **reason (REQUIRED)** |
| accepted_with_warning | rejected | ja (Re-Entscheidung) | actor, ts, **reason (REQUIRED)** |
| rejected | accepted_with_warning | ja (Re-Entscheidung) | actor, ts, **reason (REQUIRED)** |
| `rejected` → `open` (`reopen`, R10/R11 konsistent mit LE34) | ja | actor, ts, **reason (REQUIRED)** |
| `dismissed_false_positive` → `open` (`reopen`) | **NEIN** (R12 — Konsistenz-Fix: dismissed_fp setzt phpVMS-State auf ACCEPTED via `PirepService::accept()`. LE34 blockt jedes reopen aus phpVMS-State=ACCEPTED. Daher ist auch dismissed_fp → open dauerhaft nicht unterstützt.) | — |
| `accepted_with_warning` → `open` (`reopen`) | **NEIN** (R10: phpVMS hat keinen side-effect-freien Reverse-Pfad aus ACCEPTED; siehe LE34 Webhook 400) | — |
| jeder terminal → andere terminal direkt | NEIN (muss über `open` via `reopen` gehen) | — |
| jeder terminal → acknowledged direkt | NEIN (muss über `open` via `reopen` gehen) | — |

**DB-Schema-Erweiterung:**

```sql
ALTER TABLE pireps ADD COLUMN review_state TEXT DEFAULT 'open';
    -- 'open' | 'acknowledged' | 'accepted_with_warning' | 'rejected' | 'dismissed_false_positive'
ALTER TABLE pireps ADD COLUMN review_state_changed_at INTEGER;
ALTER TABLE pireps ADD COLUMN review_state_changed_by TEXT;

CREATE TABLE pirep_review_audit (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    pirep_id INTEGER NOT NULL,
    from_state TEXT,
    to_state TEXT NOT NULL,
    actor TEXT NOT NULL,
    ts INTEGER NOT NULL,
    reason TEXT,
    note TEXT,
    FOREIGN KEY (pirep_id) REFERENCES pireps(id)
);
CREATE INDEX idx_pirep_review_audit_pirep ON pirep_review_audit(pirep_id, ts DESC);
```

**API-Erweiterung:**

```
POST /api/admin/pireps/:id/review
  Body: {
    target_state: "acknowledged"
                | "accepted_with_warning"
                | "rejected"
                | "dismissed_false_positive"
                | "reopen",                       // R4 — Transition terminal → open
    reason?: string,                              // REQUIRED bei rejected, dismissed_false_positive,
                                                  //   reopen, jeder Re-Entscheidung
    note?: string                                 // optional
  }
  Wirkung:
    - Prüft erlaubte Transition gegen State-Machine (sonst 400 mit
      "transition X→Y not allowed in current state")
    - Prüft Pflichtfelder (sonst 400 mit fehlender Feldname)
    - Bei target_state == "reopen":
        - aktueller State MUSS terminal sein (accepted_with_warning,
          rejected, dismissed_false_positive)
        - setzt review_state = 'open'
        - reason ist REQUIRED
    - Updated pireps.review_state, review_state_changed_at, review_state_changed_by
    - Schreibt pirep_review_audit-Entry mit from_state, to_state, actor, ts, reason, note
    - Updated review_decision-Spalte für Backwards-Compat (siehe LE18)
    - Invalidiert Webapp-Bestenlisten-Cache wenn rejected/dismissed/accepted_with_warning wechselt
```

**Webapp-Wirkung:**
- Review-Queue zeigt State pro PIREP als farbiges Badge
- Open: orange + pulsierend
- Acknowledged: blau
- Accepted-with-warning: grün mit gelbem Warn-Symbol
- Rejected: grau-rot
- Dismissed-FP: grau-blau
- Jeder State-Wechsel zeigt einen kurzen Toast „Status geändert von X zu Y"
- Audit-Trail-View pro PIREP zeigt alle Transitions chronologisch mit Actor + Reason

### LE21 — Webapp Badges + Leaderboard-Filter

In Reports/Touchdowns/PirepFeed-Tabs erweitern:
- Bei `score_trust_level != 'trusted'`: rotes Badge **DATA INTEGRITY: REVIEW** neben dem Score
- Bei `integrity_flags.length > 0` mit allen-INFO: gelbes Badge `i` mit Hover-Tooltip „X kleine Integrity-Hinweise"
- Bei `requires_review = true` und kein `reviewed_at`: pulsierendes orangenes Badge „WARTET AUF REVIEW"
- Bei `review_decision = 'rejected'`: graues Badge „ABGELEHNT (durch VA-Admin)" + Hover mit `review_notes`

Bestenlisten (Stats-Tab, Heatmap-Tab, Touchdowns-Top-X-Listen) filtern Default auf `score_trust_level = 'trusted'`. Toggle „Untrusted einbeziehen" für VA-Admin-Diagnostik.

### Stream D — Tests

| Test | Prüft |
|---|---|
| `db_migration_adds_columns_idempotent` | Migration läuft 2× → keine Fehler |
| `migration_existing_pireps_default_trusted` | Alte PIREPs nach Migration: score_trust_level = 'trusted', integrity_flags = '[]' |
| `review_queue_endpoint_returns_only_requires_review` | GET /api/admin/review-queue liefert nur PIREPs mit requires_review = 1 |
| `review_decision_endpoint_updates_pirep` | POST review-decision → reviewed_at, review_decision gesetzt |
| `mqtt_integrity_event_pushed_on_critical` | Validator erkennt CRITICAL → MQTT-Topic erhält Payload |
| `leaderboard_default_excludes_untrusted` | GET /api/stats/leaderboard → nur trusted-PIREPs |
| `leaderboard_with_include_untrusted_param_shows_all` | ?include_untrusted=1 → alle |
| `review_queue_tab_renders_pireps_correctly` (frontend) | Tab zeigt PIREPs mit allen Flag-Badges |
| `pirep_view_shows_untrusted_badge` (frontend) | PIREP mit score_trust_level='untrusted' → roter Badge sichtbar |

---

## Stream E — Backfill (Dry-Run-Only, R1 revidiert)

### E.0 Befund (Korrektur zur A3-Pre-Spec-Entscheidung)

Pre-Spec-Entscheidung A3 war „kein Backfill — nur neue PIREPs ab v0.13.0". **QS-Runde 1 hat das revidiert** (Thomas-Finding P2): ohne Backfill bleiben unbemerkte historische Glitches in der Bestenliste, inklusive potenzieller Greaser-„Rekorde" die in Wahrheit WZZ2271-Klone sind. Bei aktuell 259 PIREPs in der Historie ist das ein realistisches Risiko.

Backfill kommt rein — **aber strikt als Dry-Run-Job mit explizitem Admin-Approval**. Keine stillen Status-Änderungen. Schritte:

1. Admin triggert Dry-Run via Endpoint
2. Job läuft alle historischen Snapshots durch den Stream-A/B-Validator
3. Erzeugt Markdown- + JSON-Report mit *„würde markiert werden"*-Listen
4. Speichert Report unter `/var/lib/aeroacars-recorder/backfill-report-<ts>.{json,md}`
5. **Keine DB-Änderung im Dry-Run.**
6. Admin prüft Report manuell
7. Wenn OK: Admin triggert Apply-Run mit Reference auf Report-ID
8. Apply-Run setzt die geflaggten Werte, schreibt Audit-Log

### LE27 — Backfill-Job (Dry-Run + Apply-Modus)

Neuer Admin-Endpoint:

```
POST /api/admin/integrity/backfill
  Auth: requireBearerAdminWrite
  Body: {
    mode: "dry_run" | "apply",
    since_ts?: number,                 # optional, default: alle PIREPs
    until_ts?: number,                 # optional
    limit?: number,                    # optional, für Tests
    reference_report_id?: string       # PFLICHT bei mode='apply'
  }
  Resp (dry_run): {
    report_id: string,
    processed: N,
    would_flag: [{ pirep_id, summary, would_be_flags[], would_be_score_trust_level }],
    report_path_md: string,
    report_path_json: string,
    applied: 0
  }
  Resp (apply): {
    report_id: string,
    processed: N,
    applied: N,                        # Anzahl der modifizierten PIREPs
    audit_log_entries: N
  }
```

**Reproducibility-Pin (R3 verschärft — pirep_ids + per-source-Hash):**

Jeder Dry-Run-Report enthält ein `reproducibility_pin`-Objekt mit allen Identitäts-Feldern, die der Apply strikt prüft. R2 hatte `dataset_hash` + `dataset_count` — R3-Befund P2-4: Count allein genügt nicht, zwei Datensätze mit gleicher Count könnten verschiedene Inhalte haben. R3 erweitert:

```json
{
  "reproducibility_pin": {
    "validator_version": "v0.13.0-rc1",
    "ruleset_hash": "sha256:abc123...",
    "aircraft_families_version": 1,
    "aircraft_families_hash": "sha256:fff111...",
    "dataset_count": 259,
    "dataset_hash": "sha256:def456...",
    "pirep_ids_canonical": [             // R3 — kanonisch sortierte vollständige Liste
      "7epBoG5lANKgZ16d",
      "...",                             // alle 259 PIREP-IDs
    ],
    "pirep_ids_hash": "sha256:111aaa...", // R3 — Hash über pirep_ids_canonical
    "per_source_hashes": {                // R3 — Content-Hash je Source-JSONL
      "7epBoG5lANKgZ16d": "sha256:abc...",
      "...": "..."
    },
    "generated_at": "2026-05-24T10:00:00Z",
    "operator": "admin@gsg",
    "dry_run_summary_checksum": "sha256:ghi789..."
  }
}
```

**Apply-Modus-Voraussetzungen (R3 erweitert, alle Punkte müssen erfüllt sein, sonst HTTP 400):**

| Voraussetzung | Prüfung |
|---|---|
| `reference_report_id` zeigt auf existierenden Dry-Run-Report | exists |
| Report-Alter ≤ 24 h | `now - generated_at ≤ 24h` |
| `validator_version` im aktuellen Recorder == Pin | identisch |
| `ruleset_hash` aktuell == Pin | identisch |
| `aircraft_families_version` + `aircraft_families_hash` aktuell == Pin | identisch (R3 explizit) |
| `dataset_count` aktuell == Pin | identisch |
| `dataset_hash` aktuell == Pin | identisch |
| **`pirep_ids_canonical`-Liste aktuell == Pin** (R3) | jeder ID an gleicher Position, gleicher Hash |
| **`per_source_hashes` aktuell == Pin für jeden PIREP** (R3) | jeder source-JSONL hat unverändert Content-Hash |
| Admin-Account hat `requireBearerAdminWrite`-Berechtigung | Auth |
| `operator` darf abweichen | OK |

Wenn ein Drift erkannt wird (Recorder-Update, Schwellwert-Änderung, **neuer PIREP im Dataset**, **alter PIREP entfernt**, **JSONL-Content modifiziert**), Apply wird strikt abgelehnt mit einer Diff-Meldung welche genaue Felder driften. Admin muss neuen Dry-Run starten.

**Konkretes Beispiel der R3-Verschärfung:** zwischen Dry-Run und Apply hat ein Pilot einen neuen PIREP eingereicht. R2 hätte das nicht gemerkt (`dataset_count` würde steigen — aber wenn zufällig ein alter PIREP archiviert wurde, könnte Count gleich bleiben). R3 merkt es sofort: `pirep_ids_canonical` enthält neuen ID, ist != Pin → 400 mit „neuer PIREP X seit Dry-Run hinzugekommen, bitte neuen Dry-Run starten".

**Im Apply-Modus:**
- Setzt `integrity_flags`, `score_trust_level`, `requires_review` auf den geflaggten PIREPs
- Schreibt jede Änderung in `audit_log` mit Vorher-/Nachher-Diff + `applied_by`, `reference_report_id`
- Sendet **keine** MQTT-Events (rückwirkend sinnlos)
- Invalidiert Webapp-Bestenlisten-Caches (Stats-Tab etc.)
- Audit-Log-Eintrag enthält den vollen `reproducibility_pin` (forensisch nachvollziehbar)

### LE28 — Backfill-Report-Format

**Markdown-Report-Struktur** (für VA-Admin-Lesbarkeit):

```markdown
# Backfill-Report — <ISO-Date>
Report-ID: backfill-1779000000-abc123
Validator-Version: v0.13.0-rc1
PIREPs verarbeitet: 259
Zeit-Range: 2026-05-07 -> 2026-05-23

## Summary

| Klassifikation | Anzahl |
|---|---|
| Trusted (kein Flag) | 218 |
| Würde 'review' werden | 28 |
| Würde 'untrusted' werden | 13 |

## Flag-Häufigkeiten

| Flag-Typ | Anzahl PIREPs |
|---|---|
| TELEMETRY_GAP_CRITICAL | 9 |
| FUEL_RATE_IMPOSSIBLE | 7 |
| SIM_STATE_RESET_SIGNATURE | 5 |
| AIR_TO_GROUND_TELEPORT | 3 |
| GROUND_ELEVATION_MISMATCH | 11 |
| ... |

## Top-Affected-PIREPs

### WZZ2271 (2026-05-24)
- Pilot: gsg/2
- Route: LHBP -> EHEH
- Aircraft: A321 HA-LVA
- Score: A+ 100/100 -> würde 'untrusted'
- Flags: TELEMETRY_GAP_CRITICAL (4x), FUEL_RATE_IMPOSSIBLE (4x), ...

### ... weitere ...

## Leaderboard-Impact

- 12 PIREPs würden aus Top-50-Touchdown-Liste fallen
- Davon 3 in Top-10
- Aktuelle Greaser-Bestenliste-Position 1 (`PIREP-ID xyz`, -1 fpm) wäre betroffen

## Empfehlung

Admin sollte PIREPs `XYZ`, `ABC`, `WZZ2271` priorisiert manuell prüfen.

## Apply-Befehl

curl -X POST .../api/admin/integrity/backfill \\
  -H 'Authorization: Bearer ...' \\
  -d '{"mode":"apply","reference_report_id":"backfill-1779000000-abc123"}'
```

**JSON-Report** ist Maschinen-lesbar mit komplettem `would_flag`-Array, für programmatische Diffs zwischen Runs.

### Stream E — Tests

| Test | Prüft |
|---|---|
| `backfill_dry_run_does_not_modify_db` | DB-State vor und nach Dry-Run identisch |
| `backfill_dry_run_produces_report` | Markdown- + JSON-Report unter `/var/lib/aeroacars-recorder/backfill-report-*.{json,md}` vorhanden |
| `backfill_apply_requires_prior_dry_run` | Apply ohne `reference_report_id` → 400 |
| `backfill_apply_rejects_stale_report` | Apply mit Report-ID älter als 24 h → 400 |
| `backfill_apply_rejects_version_mismatch` | Apply wenn Validator-Code seit Dry-Run geändert → 400 |
| `backfill_idempotent_double_apply_no_diff` | Apply mit selbem Report 2× → 2. Run macht keine Änderungen |
| `backfill_audit_log_captures_diffs` | Apply schreibt für jeden modifizierten PIREP einen audit_log-Eintrag mit Vorher/Nachher |
| `backfill_against_wzz2271_in_archive_flags_correctly` | WZZ2271-PIREP im Archiv durchläuft Backfill → würde-untrusted, korrekte Flag-Liste |
| `backfill_does_not_send_mqtt_events` | Apply-Run → keine integrity_event-MQTT-Messages |

---

## Stream F — Resume-Policy

### F.0 Befund

v0.12.1 LE14 ist als reine UX-Maßnahme designt (10-s-Auto-Confirm-Abschaltung wenn Position suspect ist). Das ist zu eng für die realen Resume-Szenarien:

- **App-Crash nach 30 s:** Pilot will einfach weiterfliegen, Sim-State ist intakt — heute zwingt LE14 ihn potenziell zum Klick.
- **Windows-Update mid-flight (45 min Gap):** Sim-State sollte intakt sein, aber 45 min Gap ist außerhalb jeglicher Toleranz — heute behandelt LE14 das genauso wie 30-s-Recovery.
- **Pilot kommt nach 4 h zurück, Sim ist abgestürzt, neu gespawnt am Default-Spawnpoint:** Heute würde LE14 nur "Air/Ground-Mismatch oder 50 NM Drift" sehen — beides erfüllt, also Auto-Confirm aus. Aber: der Pilot kann trotzdem klicken und weiterfliegen, ohne dass irgendwo dokumentiert wird dass eine echte Reposition stattfand.

Stream F ersetzt LE14 durch ein **vier-stufiges Recovery-Tier-System**, das die Toleranzen kontext-aware macht.

### LE22 — Clean-Shutdown-Erkennung

**Windows-Variante:** Tauri-App registriert sich auf `WM_QUERYENDSESSION` (System-Shutdown, Update-Initialisierung). Bei Empfang:

```rust
on WM_QUERYENDSESSION(reason):
    let reason_str = match reason {
        ENDSESSION_LOGOFF => "logoff",
        ENDSESSION_CRITICAL => "system_restart",
        ENDSESSION_CLOSEAPP => "update",  // updater requests app close
        _ => "system_shutdown",
    };
    persisted_flight.clean_shutdown_reason = reason_str;
    persisted_flight.shutdown_ts = now();
    flush_to_disk();
    return TRUE;  // accept shutdown
```

**macOS-Variante:** Listener auf `NSApplicationWillTerminate` + Check ob System gerade `softwareupdate` läuft.

**Pilot-initiierter sauberer Exit:** Pilot klickt "Beenden" im Tray → `clean_shutdown_reason = "user_close"`.

**Crash-Detection:** Beim App-Start prüft Tauri ob beim letzten Exit `clean_shutdown_reason` gesetzt wurde. Wenn nicht — und es gab eine aktive Session — gilt das als **unerkannter Crash** → `clean_shutdown_reason = "crash"`.

Persistenz in `flight_sessions.clean_shutdown_reason`.

### LE23 — Recovery-Tier-Klassifikation

Bei `try_resume_flight` (App-Start mit persistierter aktiver Session):

```
1. Lade persisted_flight aus Storage
2. gap_duration_s = now - persisted_flight.last_snapshot_ts
3. shutdown_reason = persisted_flight.clean_shutdown_reason
4. Warte auf ersten Sim-Snapshot (max 60 s, LE10)
   wenn kein Snapshot → ABORT-Tier
5. Berechne state_match_score:
     position_delta_m  = haversine(last_snap.pos, current_snap.pos) * 1852
     altitude_delta_ft = |last_snap.alt_msl - current_snap.alt_msl|
     fuel_expected     = last_snap.fuel - expected_burn(gap_duration_s, last_snap.phase)
     fuel_delta_kg     = |current_snap.fuel - fuel_expected|
     air_ground_match  = last_snap.on_ground == current_snap.on_ground
     icao_match        = last_snap.aircraft_icao == current_snap.aircraft_icao
     reg_match         = last_snap.aircraft_reg  == current_snap.aircraft_reg

6. Anwendung der Toleranzen (siehe LE24)
7. Tier-Entscheidung: SEAMLESS / GUIDED / REPOSITION / ABORT
```

### LE24 — Recovery-Tier-Toleranzen

**Basis-Toleranzen** (alle Bedingungen müssen erfüllt sein für SEAMLESS bzw. GUIDED):

| Metrik | SEAMLESS | GUIDED | REPOSITION-Trigger | ABORT-Trigger |
|---|---|---|---|---|
| `gap_duration_s` | ≤ 300 (5 min) | ≤ 1.800 (30 min) | > 1.800 | > 21.600 (6 h) |
| `position_delta_m` | ≤ 200 | ≤ 3.700 (2 NM) | > 3.700 | — |
| `altitude_delta_ft` (beide airborne) | ≤ 100 | ≤ 500 | > 500 | — |
| `air_ground_match` | identisch | identisch | Mismatch | — |
| `fuel_delta_kg` (vs. erwartet) | ≤ 50 | ≤ 200 | > 200 oder physikalisch unmöglich | — |
| `icao_match` | identisch | identisch | Mismatch | — |
| `reg_match` | identisch | toleriert | — | — |
| Sim-Snapshot in 60 s | ja | ja | ja | nein |

**Clean-Shutdown-Bonus (R2 — strikt Client-UX-only):**

Bonus wirkt **AUSSCHLIESSLICH** auf die Stream-F Tier-Klassifikation (SEAMLESS/GUIDED/REPOSITION-Wahl im Client-Resume-Dialog). Der Recorder-Integrity-Validator (Stream A/B) **ignoriert `clean_shutdown_reason` komplett** — er bewertet jeden Flug identisch, unabhängig davon ob Windows-Update oder App-Crash die Lücke verursacht hat.

Bonus-Tabelle (nur für Tier-Klassifikation):

| Toleranz | Bonus-Faktor (UX) | Begründung |
|---|---|---|
| `gap_duration_s` | **× 6** | Windows-Update darf 30 min – 3 h dauern, der Resume-Dialog soll dann nicht in REPOSITION-Tier fallen |
| `position_delta_m` | **× 1** (kein Bonus, R2 herabgestuft) | Position-Drift im Recorder identisch bewertet → Bonus würde ungewollt Recorder-Konsistenz untergraben |
| `altitude_delta_ft` | **× 1** | kein Bonus |
| `fuel_delta_kg` | **× 1** | kein Bonus — Physik |
| `air_ground_match` | **× 1** | kein Bonus |
| `icao_match` | **× 1** | kein Bonus |

**Architektonische Trennung (R2 verbindlich):**

```
clean_shutdown_reason wirkt auf:
    Stream F Tier-Klassifikation       → JA (nur gap_duration × 6)
    Stream A Continuous-Mode-Checks    → NEIN (Recorder ignoriert)
    Stream A Gap-Edge-Mode-Envelopes   → NEIN (Recorder ignoriert)
    Stream B Gap-Severity-Schwellen    → NEIN (Recorder ignoriert)
    Stream B Edge-State-Conflict       → NEIN (Recorder ignoriert)
    PIREP-Score-Trust-Berechnung       → NEIN (Recorder ignoriert)
```

Konkretes Beispiel: nach 90 min Update zurück, `clean_shutdown_reason = "update"` →

- **Client-Resume-Dialog (Stream F):** Gap × 6 = 30 min × 6 = bis 3 h erlaubt → wenn restliche State-Checks OK, SEAMLESS-Tier, Pilot kann durchklicken
- **Recorder Integrity (Stream A/B):** Gap = 90 min wird ohne Bonus bewertet → TELEMETRY_GAP_CRITICAL geflagt (> 300 s Schwelle aus LE8), Gap-Edge-Mode läuft mit voller Strenge

Beide Bewertungen koexistieren: Pilot hat eine smooth-UX-Erfahrung (SEAMLESS-Resume), das PIREP bekommt aber trotzdem den TELEMETRY_GAP_CRITICAL-Flag — und wenn der erste Post-Resume-Snapshot z. B. `fuel_total_kg = 7.000` zeigt (von vorher 5.230 kg), feuert Stream A LE3 `FUEL_RATE_IMPOSSIBLE` mit voller CRITICAL-Severity. Das PIREP wird trotz SEAMLESS-Resume-Tier `requires_review = true`. Die zwei Layers sind unabhängig — Client-UX gibt dem Piloten Komfort, Recorder gibt dem VA-Admin die wahre Datenintegrität.

**ABORT-spezifische Bedingungen** (jede einzelne triggert ABORT, kein Bonus möglich):
- Gap > 6 h (egal warum — auch nicht bei Clean-Shutdown)
- Kein Sim-Snapshot in 60 s
- `icao_match == false` (anderes Aircraft im Sim)

### LE25 — Tier-spezifische UI-Flows

**SEAMLESS-Flow (R3 mit Post-Resume-Status-Pill):**

```
1. Recorder sendet bestehenden `flight_resumed`-Event
2. Client zeigt non-blocking Banner:
   "Flug fortgesetzt (Gap: 2 min 14 s). Automatisches Resume in 5 s."
   [Cancel-Button] [Sofort-Resume-Button] [5-s-Countdown]
3. Nach 5 s oder Cancel: flight läuft weiter, Banner schließt
4. PIREP: resume_history.push({tier: 'seamless', ...})
5. Activity-Log: "Resume seamless after 2m14s gap"
6. Client wartet auf Recorder-Acknowledgment (max 10 s) inkl. server-side flags:
     IF Recorder hat während Resume-Validation Flags geworfen (TELEMETRY_GAP_*, 
        Gap-Edge-Envelopes, FUEL_RATE_IMPOSSIBLE, etc.):
         → Client zeigt persistente Status-Pill rechts oben in der Sim-HUD:
           "Flug fortgesetzt — Datenlücke wird geprüft (X Flags)"
           [Klick öffnet Detail-Panel mit Flag-Liste]
     ELSE:
         → keine Pill, alles clean
```

**Warum die Pill (R3-Befund P1-3):** Ohne diese Komponente entsteht ein UX-Widerspruch — der Client zeigt „SEAMLESS, alles ok" während der Recorder das PIREP als `requires_review = true` markiert. Pilot fliegt mit falscher Sicherheit weiter und ist später überrascht wenn das PIREP in der Review-Queue landet. Die Pill macht die Realität sichtbar, ohne den Flug zu unterbrechen.

**Pill-Verhalten:**
- Erscheint sofort nach Recorder-Acknowledgment (typisch innerhalb 1–2 s nach Resume)
- Bleibt sichtbar für die restliche Session
- Klick öffnet Detail-Panel mit Flag-Liste, Severity, Erklärungstext
- Tooltip bei Hover: „Der Recorder hat während deiner Pause Daten-Anomalien erkannt. Der Flug läuft weiter, aber das PIREP wird nach Beendigung vom VA-Admin geprüft."
- Pill-Farbe: gelb wenn nur INFO/ANOMALY-Flags, rot wenn CRITICAL-Flags
- Counter aktualisiert sich live wenn weitere Flags eintreffen

**GUIDED-Flow:**

```
1. Client zeigt blockierendes Modal mit Diff-Tabelle:

   ┌──────────────────────────────────────────────────────────┐
   │ Resume mit Sim-State-Drift                               │
   │ ────────────────────────────────────────────────────────│
   │ Gap-Dauer:    12 min 23 s                                │
   │                                                          │
   │ Metrik    │ Letzter Stand│ Jetzt        │ Status        │
   │ ──────────┼──────────────┼──────────────┼─────────────│
   │ Position  │ 47.43N 19.26E│ 47.44N 19.27E│ 🟢 0.4 NM     │
   │ Altitude  │ FL360        │ FL358        │ 🟢 -230 ft    │
   │ Fuel      │ 5.230 kg     │ 4.890 kg     │ 🟡 260 kg     │
   │           │              │ (erwartet:   │   unter       │
   │           │              │  5.150 kg)   │   Erwartung   │
   │ Aircraft  │ A321 HA-LVA  │ A321 HA-LVA  │ 🟢 identisch  │
   │ Air/Ground│ airborne     │ airborne     │ 🟢            │
   │                                                          │
   │ [Resume mit Drift] [Cancel - Reposition]                │
   └──────────────────────────────────────────────────────────┘

2. Pilot klickt "Resume mit Drift":
   → PIREP: integrity_flags.push({type: 'RESUME_WITH_DRIFT', severity: 'info'})
   → resume_history.push({tier: 'guided', state_diff: {...}, accepted_by_pilot: true})
   → flight läuft weiter
3. Pilot klickt "Cancel - Reposition":
   → switcht zu REPOSITION-Flow
```

**REPOSITION-Flow:**

```
1. Client zeigt vollformatiges Reposition-Modal:

   ┌──────────────────────────────────────────────────────────┐
   │ Reposition erforderlich                                  │
   │ ────────────────────────────────────────────────────────│
   │ Der Sim-State weicht stark vom letzten bekannten Stand   │
   │ ab. Bitte positioniere das Flugzeug manuell:             │
   │                                                          │
   │ ZIELZUSTAND (letzter bekannter Stand):                   │
   │   Position:   47.43°N 19.26°E                            │
   │   Altitude:   FL360 (36.000 ft MSL)                      │
   │   Heading:    042° true                                  │
   │   Geschw.:    480 kt GS / 270 kt IAS                     │
   │   Fuel:       5.230 kg                                   │
   │   Phase:      Cruise (LHBP → EHEH, 412 NM remaining)     │
   │                                                          │
   │ AKTUELL IM SIM:                                          │
   │   Position:   51.45°N 5.38°E    (412 NM Drift)           │
   │   Altitude:   0 ft AGL (on ground)                       │
   │   Aircraft:   A321 HA-LVA                                │
   │                                                          │
   │ Wenn du das Flugzeug positioniert hast, klicke unten.    │
   │ Dieser Flug wird automatisch zur Review markiert.        │
   │                                                          │
   │ [Bereit, Flug fortsetzen]  [Flug abbrechen]              │
   └──────────────────────────────────────────────────────────┘

2. Pilot klickt "Bereit":
   → Client wartet auf nächsten Sim-Snapshot
   → Re-evaluierung (kann erneut REPOSITION triggern, dann Banner bleibt)
   → wenn akzeptabel: flight läuft weiter
   → PIREP: integrity_flags.push({type: 'RESUME_AFTER_REPOSITION', severity: 'anomaly'})
   → resume_history.push({tier: 'reposition', state_diff: {...}, ...})
   → PIREP wird requires_review = true (per A10)
3. Pilot klickt "Flug abbrechen":
   → flight_ended-Event mit reason 'pilot_cancel_at_reposition'
   → PIREP wird als partial-PIREP gefiled (LE16c-Pfad)
     - last_clean_phase + last_clean_state aus letztem Pre-Reposition-Sample
     - keine Landing-/BlocksOn-Metriken
     - status = 'partial', requires_review = true, review_state = 'open'
     - integrity_flag PILOT_CANCELLED_AT_REPOSITION (ANOMALY)
     - landet in Review-Queue, VA-Admin entscheidet wie immer
```

**ABORT-Flow:**

```
1. Client zeigt fatales Modal:
   "Resume nicht möglich. Gap zu lang (8h 24min) / Sim antwortet nicht / 
    Aircraft-Typ hat sich geändert (A321 → B738)."
   
   [Flug verwerfen]  [Als 'partial' filen (kein Score)]
   
2. PIREP: integrity_flags.push({type: 'RESUME_TIMEOUT_ABORTED', ...}) 
          oder entsprechendes ABORT-Reason-Flag
3. Wenn 'partial' filed: score_trust_level = 'untrusted', requires_review = true
4. Wenn 'verworfen': PIREP wird soft-deleted
```

### LE26 — Resume-Flag-Taxonomie

Vollständige Liste der von Stream F produzierten Flags:

| Flag | Severity | Wann gesetzt |
|---|---|---|
| `RESUME_SEAMLESS` | — (kein Flag, nur Activity-Log) | SEAMLESS-Tier durchlaufen ohne Pilot-Cancel |
| `RESUME_WITH_DRIFT` | INFO | GUIDED-Tier durchlaufen mit Drift-Akzeptanz |
| `RESUME_AFTER_REPOSITION` | ANOMALY | REPOSITION-Tier durchlaufen + akzeptiert |
| `RESUME_AFTER_CLEAN_SHUTDOWN` | INFO | Session lief vorher `clean_shutdown_reason ∈ {system_restart, update}` |
| `RESUME_AFTER_CRASH` | INFO | Session-Vorgänger hatte `clean_shutdown_reason = 'crash'` |
| `RESUME_TIMEOUT_ABORTED` | — | ABORT-Tier wegen kein-Snapshot-in-60-s |
| `RESUME_ABORTED_AIRCRAFT_CHANGED` | — | ABORT-Tier wegen `icao_match = false` |
| `RESUME_ABORTED_GAP_TOO_LONG` | — | ABORT-Tier wegen gap > 6 h |
| `RESUME_PILOT_CANCELLED_AT_GUIDED` | INFO | Pilot wählte "Cancel" im GUIDED-Modal |
| `RESUME_PILOT_CANCELLED_AT_REPOSITION` | INFO | Pilot wählte "Flug abbrechen" im REPOSITION-Modal |

### Stream F — Tests

| Test | Prüft |
|---|---|
| `clean_shutdown_windows_writes_reason` (Tauri-side) | WM_QUERYENDSESSION setzt clean_shutdown_reason |
| `crash_detection_no_clean_shutdown_marks_crash` | App-Start ohne vorherigem clean_shutdown → 'crash' im Storage |
| `tier_seamless_5min_perfect_match` | Gap 4 min, alle Werte in Toleranz → SEAMLESS |
| `tier_guided_15min_small_drift` | Gap 15 min, position 1 NM drift, fuel 150 kg drift → GUIDED |
| `tier_reposition_air_ground_mismatch` | airborne vorher, on_ground jetzt → REPOSITION |
| `tier_reposition_gap_60min` | Gap 60 min ohne clean_shutdown → REPOSITION |
| `tier_seamless_60min_with_update_shutdown` | Gap 60 min, clean_shutdown = 'update' → SEAMLESS (× 6 Bonus) |
| `tier_abort_no_snapshot_60s` | flight_resumed-Event + kein Snapshot in 60 s → ABORT |
| `tier_abort_aircraft_type_changed` | A321 vorher, B738 jetzt → ABORT |
| `tier_abort_gap_8h` | Gap 8 h → ABORT |
| `seamless_banner_auto_confirms_after_5s` (frontend) | SEAMLESS-Banner → 5 s Countdown → auto-confirm |
| `seamless_banner_cancel_button_works` (frontend) | Click "Cancel" → flight stays paused |
| `guided_modal_diff_table_renders_traffic_lights` (frontend) | GUIDED-Modal zeigt Diff mit korrekten Ampel-Farben |
| `reposition_modal_shows_target_state` (frontend) | REPOSITION-Modal zeigt Ziel-Position, Altitude, Phase |
| `reposition_re_evaluation_loops_until_acceptable` | Pilot klickt "Bereit", State ist immer noch off → REPOSITION-Modal bleibt |
| `resume_after_reposition_flag_persists` | Erfolgreiche REPOSITION-Recovery → RESUME_AFTER_REPOSITION-Flag im PIREP, requires_review = true |
| `wzz2271_replay_would_trigger_abort_at_06_04` | Replay des WZZ2271-flight_resumed-Events (06:04 mit 96 min Alter, kein Snapshot bis 06:09) → ABORT (mit Default-Toleranzen ohne Clean-Shutdown-Bonus) |

---

## Stream G — phpVMS-Integrity-Gate via Module (R6 pivotiert)

### G.0 Befund

R0–R4 haben einen vollständigen Integritäts-Validator + Review-Queue auf dem VPS aufgebaut, aber **die phpVMS-Wahrheit nicht mit der VPS-Wahrheit verbunden**. WZZ2271-Beispiel: VA-Admin sieht in der VPS-Review-Queue „requires_review wegen 4× SIM_STATE_RESET_SIGNATURE", aber auf `german-sky-group.eu` läuft der PIREP weiter mit „Greaser 100/100" in der Bestenliste. Piloten und Community sehen die falsche Wahrheit, VA-Admin sieht die richtige — getrennte Welten.

R5 versuchte die Brücke über eine VPS-side Filing-Architektur (VPS filed im Namen des Piloten an phpVMS, hält Pilot-Keys verschlüsselt vor). Das funktioniert, ist aber **überengineered** — wie sich durch Analyse des etablierten `FatihKoz/DisposableBasic`-Moduls (https://github.com/FatihKoz/DisposableBasic) gezeigt hat. R6 pivotiert auf den DisposableBasic-Ansatz: **eigenes phpVMS-Module als Listener**.

### G.1 — phpVMS-Hook-Mechanismus (verifiziert im phpvms/dev-Source)

**Drei Fakten verifiziert:**

1. **`PirepFiled`-Event feuert BEFORE Auto-Approve.** Aus `app/Services/PirepService.php`:

   ```php
   // Line 326–327: Event-Dispatch
   Log::info('New PIREP filed, pirep_id: '.$pirep->id);
   event(new PirepFiled($pirep));
   
   // Line 341–357: Auto-Approve-Logik läuft DANACH
   if ($pirep->state === PirepState::PENDING || $pirep->state === PirepState::DRAFT) {
       if ($pirep->source === PirepSource::ACARS 
           && $pirep->user->rank->auto_approve_acars) {
           $default_state = PirepState::ACCEPTED;
       }
       // ...
   }
   ```

   → Ein Listener auf `PirepFiled` läuft mit PIREP noch in PENDING/DRAFT, kann den State auf REJECTED oder PENDING (explizit-held) setzen **bevor** Auto-Approve den State auf ACCEPTED drehen würde.

2. **PirepState-Enum** (`app/Enums/PirepState.php`):

   ```
   IN_PROGRESS = 0
   PENDING     = 1
   ACCEPTED    = 2
   CANCELLED   = 3
   DELETED     = 4
   DRAFT       = 5
   REJECTED    = 6
   PAUSED      = 7
   ```

   → Wir nutzen `PENDING` (1) für held-by-AeroACARS-Gate und `REJECTED` (6) für admin-rejected.

3. **Event-Payload:** `app/Events/PirepFiled.php` hat `public Pirep $pirep` — direkter Eloquent-Model-Zugriff im Listener.

**Plus aus DisposableBasic-Codebase (`Listeners/Gen_AutoReject.php`) bewährt:**

```php
public function handle(PirepFiled $event)
{
    $pirep = $event->pirep;
    // ... Validierungs-Logik ...
    if (isset($pirep_state)) {
        $pirep->state = $pirep_state;        // ← direkter Eloquent-Save
        $pirep->save();                       // ← geht durch, umgeht checkReadOnly()
    }
    DB::table('pirep_comments')->insert($pirep_comments);  // ← strukturierte Reasons
}
```

Der `checkReadOnly()`-Gate aus `PirepController` blockt nur API-Updates, nicht direkte Eloquent-Manipulation aus einem Listener — DisposableBasic nutzt das seit Jahren produktiv mit phpVMS.

### G.2 — Architektur-Entscheidung (R8: Module-Listener + GSG-Config-Prereq)

#### G.2.0 — KRITISCHE Voraussetzung (R8 neu)

**`auto_approve_acars` MUSS auf phpVMS für alle Ranks deaktiviert sein.**

Begründung: phpVMS' `PirepService::submit()`-Code (Lines 326–370) macht nach dem `PirepFiled`-Event:
- Wenn `state == PENDING || DRAFT` UND `rank.auto_approve_acars=true` → setzt `$default_state = ACCEPTED`
- Wenn `$default_state === REJECTED` → ruft `$this->reject()` (mit allen Side-Effects: Hours-Reverse, Pay-Reverse, Notification, Stats-Update)
- Wenn `$default_state === ACCEPTED` → ruft `$this->accept()` (mit allen Side-Effects)

Daraus folgt: 
- Module kann KEINEN State setzen der „auf Review wartet" ohne Side-Effects, ES SEI DENN `auto_approve_acars` ist deaktiviert
- Wenn `auto_approve_acars=true`: alle Hold-Workarounds (REJECTED, PAUSED, etc.) triggern unerwünschte Side-Effects
- Wenn `auto_approve_acars=false`: PENDING bleibt PENDING, kein `accept()`/`reject()` läuft → sauberer Hold-State

**Installation-Prozess (R10 mit Auto-Repair):**

1. VA-Owner läuft `php artisan aeroacars-gate:install`
2. Installation-Command:
   a. Liest alle Ranks
   b. **Setzt automatisch `auto_approve_acars = FALSE` auf ALLEN Ranks** (mit Audit-Log: „Module disabled auto_approve_acars on Rank X during installation")
   c. Verifiziert das Ergebnis (Read-back-Check)
   d. Aktiviert Module
3. Plus klare Doku im Module-README: **„This Module disables `auto_approve_acars` on all Ranks during installation and keeps it disabled. The Integrity Gate requires this Setting to function. If you absolutely need ACARS Auto-Approval back, uninstall the Module."**

**Laufender Schutz — Auto-Repair statt Emergency-Mode (R10 korrigiert):**

R9's „Emergency-Mode mit PENDING-Hold bei Drift" hatte einen **fundamentalen Widerspruch**: bei `auto_approve_acars=true` auf einem Rank würde PENDING ja gerade wieder zu ACCEPTED — der ganze Emergency-Mode war nutzlos für den Use-Case den er lösen sollte. R10 löst das anders: **das Module repariert die Config selbst**, statt zu warten bis VA-Owner reagiert.

| Mechanismus | Frequenz | Aktion |
|---|---|---|
| **Boot-Time-Check** | bei jedem Module-Boot / phpVMS-Restart | Auto-Repair: alle Ranks mit `auto_approve_acars=true` → `false`. Log + Discord-Notification. |
| **Daily-Cron-Job** `aeroacars-gate:auto-repair-ranks` | täglich (z. B. 04:00 UTC) | Wie Boot — find drift + repair + notify |
| **Per-PIREP-Defense-in-Depth (im Listener)** | bei jedem `PirepFiled`-Event | Listener ruft als **ERSTEN** Schritt `AutoRepairService::repairForPirep($pirep)` (siehe LE30 normativ). Service prüft `$pirep->user->rank->auto_approve_acars`, repariert wenn nötig (DB-Update + Cache-Invalidation + Relation-Refresh am `$pirep`-Objekt), loggt + Discord. Erst dann läuft der normale Verdict-Flow. |

**`Services/AutoRepairService.php` (R11 normativ — Cache-Invalidation + Relation-Refresh):**

```php
public function __construct(
    private \Illuminate\Database\ConnectionInterface $db,
    private \Illuminate\Cache\Repository $cache,
    private DiscordNotifier $discord,
) {}

/**
 * R11: defense-in-depth Auto-Repair für genau einen PIREP-Kontext.
 * MUSS aus dem PirepFiled-Listener als ERSTER Schritt vor jeder State-Manipulation
 * aufgerufen werden, sonst kann phpVMS' Submit-Flow im selben Request den
 * cached/loaded Rank-Wert auto_approve_acars=true lesen und unseren PENDING-Hold
 * auf ACCEPTED umdrehen (R11-Bug-Verhinderung).
 */
public function repairForPirep(Pirep $pirep): bool
{
    $rank = $pirep->user->rank;
    if (!$rank->auto_approve_acars) {
        return false;   // alles korrekt, kein Repair nötig
    }
    
    $rankId = $rank->id;
    
    // 1. DB-Update (authoritative)
    $this->db->table('ranks')
        ->where('id', $rankId)
        ->update(['auto_approve_acars' => false]);
    
    // 2. Cache-Invalidation (phpVMS cached Ranks oft via repository.cache.ranks.*)
    $this->cache->forget("ranks.{$rankId}");
    $this->cache->forget('ranks.all');                    // List-Cache
    $this->cache->tags(['ranks'])->flush();               // wenn Tagged-Cache verwendet
    
    // 3. WICHTIG: Relation am übergebenen $pirep refreshen
    // Sonst liest phpVMS' Submit-Code (Lines 341-360) den in-memory cached Wert true
    // → wäre der Bypass den wir verhindern wollen
    $pirep->user->setRelation('rank', $rank->fresh());
    // Plus explicit re-load to be safe
    $pirep->load('user.rank');
    
    // 4. Audit-Log
    Log::warning('AeroACARS-Gate: auto-repaired auto_approve_acars on Rank', [
        'rank_id' => $rankId,
        'rank_name' => $rank->name,
        'triggered_by' => 'per_pirep_defense_in_depth',
        'pirep_id' => $pirep->id,
    ]);
    
    // 5. Audit-Trail in module_gate_audit-Tabelle
    $this->db->table('module_gate_audit')->insert([
        'phpvms_pirep_id' => $pirep->id,
        'direction' => 'auto_repair',
        'ts' => now()->getTimestampMs(),
        'verdict_json' => json_encode([
            'action' => 'auto_repair_rank',
            'rank_id' => $rankId, 'rank_name' => $rank->name,
            'trigger' => 'per_pirep_defense_in_depth',
        ]),
    ]);
    
    // 6. Discord-Notification (async via queue, nicht-blockierend)
    dispatch(function () use ($rank, $pirep) {
        $this->discord->send(sprintf(
            "⚠ AeroACARS Auto-Repaired Rank **%s** (id=%d) — was reset to "
          . "auto_approve_acars=false. Triggered by PIREP %s. Investigate cause "
          . "(was setting changed manually? Plugin/migration drift?).",
            $rank->name, $rank->id, $pirep->id
        ));
    })->afterResponse();
    
    return true;   // repaired
}

// Wird auch von Boot-Check + Daily-Cron mit anderer Trigger-Quelle aufgerufen
public function repairAllDriftedRanks(string $triggerSource): int { /* ... */ }
```

**Garantie:** nach Aufruf von `repairForPirep()` ist `$pirep->user->rank->auto_approve_acars == false` in-memory garantiert. Damit liest phpVMS' nachfolgender Submit-Code `if (state === PENDING && auto_approve_acars)` immer `false` und macht nicht den Auto-Approve-Override.

**Warum Auto-Repair statt manueller Wiederherstellung:**

- Ein Integrity-Gate muss sich selbst absichern können — kein VA-Owner-Eingriff darf zwischen „Drift erkannt" und „Schutz wiederhergestellt" stehen
- Auto-Repair ist Standard-Pattern (Letsencrypt-Auto-Renew, fail2ban-Auto-Recovery, etc.)
- Defense-in-Depth: selbst wenn Boot-Check und Daily-Cron beide ausfallen, fängt der Per-PIREP-Check im Listener jeden einzelnen PIREP ab
- Drift wird trotzdem vollständig protokolliert (Audit-Log + Discord-Notification + phpVMS-Admin-Banner für 24h) — VA-Owner weiß was passierte

**Sichtbarkeit bei Auto-Repair:**

1. **phpVMS-Admin-Dashboard:** gelbes Info-Banner „⚠ AeroACARS Auto-Repair: `auto_approve_acars` was re-enabled on Rank X (auto-corrected to false)" — bleibt 24h sichtbar
2. **Discord-Webhook** an VA-Owner: „AeroACARS Auto-Repaired Rank X — was reset to auto_approve_acars=false. Investigate cause."
3. **Audit-Log-Eintrag** in `module_gate_audit` mit `action='auto_repair_rank'`, betroffener Rank, alter/neuer Wert, Trigger-Quelle (boot/cron/per-pirep)

**Was das für die fail-closed-Policy bedeutet:**

Auto-Repair schließt den fail-open-Vector — selbst wenn ein Rank misconfig'd ist, ist der State binnen Sekunden korrigiert (spätestens beim nächsten PIREP von einem Pilot mit diesem Rank). PENDING-Hold-Funktionalität (LE33) bleibt für alle anderen Fail-Cases bestehen (VPS-Timeout, 404, Throwable). Aber kein separater „Emergency-Mode für Config-Drift" mehr — das ist redundant geworden.

**VA-Owner-Akzeptanz dokumentiert:**

> Das Module ändert phpVMS-Rank-Settings automatisch (`auto_approve_acars=false`). Das ist Voraussetzung für das Integrity-Gate. Wenn du das nicht willst, installiere das Module nicht. Alle Auto-Repair-Aktionen werden geloggt und per Discord/Banner sichtbar gemacht — keine stillen Änderungen.

#### G.2.1 — Architektur-Diagramm

#### Pivot-Historie

| Stand | Hold-State | Auto-Approve-Voraussetzung | Probleme |
|---|---|---|---|
| R5 (verworfen) | VPS-Filing (kein phpVMS-State-Eingriff) | irrelevant | Überengineered: Pilot-Keys auf VPS, Client-Refactor, Migration-Pfad |
| R6 (verworfen) | `PENDING` direkt | nicht thematisiert | Auto-Approve hebt PENDING sofort auf ACCEPTED → Bypass |
| R7 (verworfen) | `REJECTED` | nicht thematisiert | Triggert `reject()` mit allen Side-Effects → falsche Reject-Mails, Hours-Reversal, kaputte Reverse-Path-Logik |
| R8 (überholt) | `PENDING` | `auto_approve_acars=false` Pflicht | Architektur richtig, aber Listener rief PirepService direkt → Double-Lifecycle. Misconfig war fail-open (silent disable). |
| R9 (überholt) | `PENDING` | `auto_approve_acars=false` Pflicht | Listener-vs-Webhook-Boundary OK. Aber Emergency-Mode-Logik widersprüchlich (PENDING bei Drift = wird wieder accepted bei auto_approve=true). reopen-from-ACCEPTED-Verweis auf v0.13.1. |
| R10 (überholt) | `PENDING` | `auto_approve_acars=false` durch Auto-Repair garantiert | Auto-Repair-Pattern korrekt beschrieben, aber im LE30-Listener-Code nicht normativ verankert → Same-Request-Cache-Bypass-Risiko. Validation-Rule für `reason` inkonsistent. Stale R7/R9-Texte im Security-Block. |
| R11 (überholt) | `PENDING` | `auto_approve_acars=false` durch Auto-Repair als erster Listener-Schritt garantiert | Auto-Repair-Code normativ in LE30, Cache-Invalidation, Relation-Refresh — aber Fallback bei Auto-Repair-Failure setzte PENDING-Hold, das wäre wieder via Auto-Approve zu ACCEPTED gedreht worden. `dismissed_fp → open` widersprach LE34. |
| R12 (überholt) | `PENDING` (normal) / `REJECTED` (Auto-Repair-Failure) | `auto_approve_acars=false` durch Auto-Repair garantiert | Re-Check + Emergency-Fallback fail-closed, aber `repairForPirep()` selbst nicht im try/catch — Exception-Pfad fiel am Fallback vorbei. `applyConfigDriftFallback()` noch als unsicherer PENDING-Pfad im Code. |
| **R13 (aktuell)** | `PENDING` (normal) / `REJECTED` (Auto-Repair-Failure-Emergency, alle Pfade) | `auto_approve_acars=false` durch **Auto-Repair im try/catch als erster Listener-Schritt** garantiert | try/catch um repairForPirep() — sowohl Verification-Failure als auch Repair-Exception landen im REJECTED-Emergency-Fallback. `applyConfigDriftFallback()` hart entfernt aus Code (Audit-Test verhindert Wieder-Einführung). Retry-Queue-Schema-Konsistenz für `dismissed_fp`. Header sieben Streams. Historische v0.13.1/Backlog-Texte sanitisiert. |

**R12-Architektur (PENDING-als-Hold + Auto-Repair + Listener-vs-Webhook-Lifecycle-Split):**

```
VORAUSSETZUNG (durch Module selbst gesichert via Auto-Repair — siehe G.2.0):
phpVMS Ranks: auto_approve_acars = FALSE
  → Module setzt das automatisch bei Install + Daily-Cron + Per-PIREP-Defense-in-Depth
  → Bei Drift: Auto-Repair in Sekunden, Discord + Admin-Banner

Pilot Client → phpVMS prefile + file (UNVERÄNDERT, wie heute)
                       ↓
              phpVMS feuert PirepFiled-Event (vor State-Resolution-Logik)
                       ↓
              Unser phpVMS-Module: AeroACARSIntegrityGate
              Listener: PirepFiledGate.php
                       ↓
              SCHRITT 1 (R11/R12): AutoRepairService::repairForPirep($pirep)
                → DB + Cache + Relation-Refresh
                → Defensiver Re-Check
                → Bei Repair-Failure: REJECTED-Emergency-Hold + Discord-Alarm (return)
                       ↓
              SCHRITT 2: HTTP-Call mit HMAC-Auth an VPS für Verdict
                       ↓
              VPS antwortet mit dem bereits-berechneten Integrity-Ergebnis:
              {
                trust_level:   "trusted" | "review" | "untrusted",
                requires_review: bool,
                flags:         [...],
                admin_decision: null | "accepted" | "rejected" | "dismissed_fp",
                reject_reason: string?
              }
                       ↓
              SCHRITT 3 (R9/R10/R11/R12): Listener setzt NUR State+save+Field+Comment.
              phpVMS-Submit-Flow ruft danach accept()/reject() EINMAL je nach State.
              KEIN expliziter PirepService-Call im Listener!
                       
       ┌─ clean (trust_level=trusted, no admin_decision) ─────────────────┐
       │ Listener: $pirep->state = ACCEPTED + save + Field='auto_accepted'│
       │ phpVMS-Submit → $this->accept() läuft einmal                     │
       │ → Hours/Pay/Stats/Aircraft-State/Notifications laufen normal      │
       │                                                                  │
       ├─ requires_review (suspect, kein Admin-Verdict bisher) ──────────┐
       │ Listener: $pirep->state = PENDING + save + Field='held'          │
       │ phpVMS-Submit → state===PENDING, auto_approve=false → else-Zweig │
       │ → State bleibt PENDING, KEIN accept()/reject()-Aufruf            │
       │ → KEINE reject()-Side-Effects, keine false Reject-Mail           │
       │                                                                  │
       ├─ admin_decision=rejected ────────────────────────────────────────┐
       │ Listener: $pirep->state = REJECTED + save + Field='rejected_final'│
       │ phpVMS-Submit → $this->reject() läuft einmal                     │
       │ → Hours-Reverse/Pay-Reverse/Reject-Notification laufen normal    │
       │                                                                  │
       ├─ admin_decision=accepted ────────────────────────────────────────┐
       │ Listener: $pirep->state = ACCEPTED + save + Field='accepted_with_warning'│
       │ phpVMS-Submit → $this->accept() läuft einmal                     │
       │                                                                  │
       ├─ admin_decision=dismissed_fp ────────────────────────────────────┐
       │ Listener: $pirep->state = ACCEPTED + save + Field='dismissed_as_false_positive'│
       │ phpVMS-Submit → $this->accept() läuft einmal                     │
       │                                                                  │
       └─ VPS timeout / Network / 404 (kein telem.) / \Throwable ─────────┐
         Listener: $pirep->state = PENDING + save + Field='held_*'         │
         (Begründung je nach Fail-Grund: held_vps_timeout, held_internal_  │
         error, held_no_telemetry)                                         │
         phpVMS-Submit → state bleibt PENDING, kein Lifecycle              │
         Für 404 + Legacy-Allowlist: state=ACCEPTED + Field='legacy_allowlisted'│
```

**Konsequenz:** Listener-Kontext setzt NUR State (phpVMS macht Lifecycle einmal). Sicher für jede Fail-Case, kein Double-Trigger, kein Bypass.

**Plus Webhook-Endpoint im Module** für nachträgliche Admin-Entscheidungen (VPS-Review-Queue → phpVMS):

```
POST https://german-sky-group.eu/api/aeroacars-gate/pirep-decision
  Auth: HMAC (siehe LE31a)
  Body: {
    phpvms_pirep_id: string,
    target_state: "accepted_with_warning" | "rejected" | "dismissed_fp" | "reopen",
    decision_reason: string,
    decided_by: string,       # VA-Admin-Identifier aus VPS
    decided_at: int           # unix-ms
  }
  Wirkung (R9/R10/R11/R12 — off-flow, nutzt PirepService explizit):
    - Lookup PIREP via Eloquent
    - target_state=accepted_with_warning → $pirepSvc->accept($pirep) (proper Lifecycle)
    - target_state=rejected               → $pirepSvc->reject($pirep) (proper Reverse)
    - target_state=dismissed_fp           → $pirepSvc->accept($pirep) (proper Lifecycle)
    - target_state=reopen                 → wenn PENDING/REJECTED: state=PENDING+Field='held'
                                             wenn ACCEPTED (incl. accepted_with_warning,
                                             dismissed_as_false_positive, auto_accepted): 400
    - PirepFieldValue + Comment für jede Decision
    - Antwort: 200 OK { phpvms_state: <new>, aeroacars_integrity_state: <new>, ... }
```

VPS ruft diesen Endpoint immer wenn Admin in der Review-Queue eine Entscheidung trifft. Webhook nutzt `PirepService::accept()/->reject()` explizit weil **off-flow Kontext** — kein umgebender phpVMS-Submit-Flow läuft. Genauso wie der Async-Retry-Worker (LE31).

### LE29 — phpVMS-Module: `AeroACARSIntegrityGate`

Neues phpVMS-Module, deployed auf `german-sky-group.eu` neben den bestehenden Modules. Struktur orientiert sich an DisposableBasic:

```
modules/AeroACARSIntegrityGate/
├── module.json                      # Module-Manifest mit phpVMS-Min-Version
├── composer.json
├── Config/
│   └── config.php                   # vps_base_url, shared_secret_env, http_timeout_s
├── Listeners/
│   ├── PirepFiledGate.php           # primärer Listener auf PirepFiled-Event
│   └── PirepPrefiledGate.php        # optional: Prefile-Event für Audit (NOOP für State)
├── Http/
│   ├── Controllers/
│   │   └── DecisionWebhookController.php   # POST /api/aeroacars-gate/pirep-decision
│   └── Middleware/
│       └── VerifyModuleSecret.php   # X-AeroACARS-Module-Secret-Check
├── Services/
│   ├── VpsClient.php                # HTTP-Client zum VPS (Retry-Backoff)
│   └── PirepStateManipulator.php    # $pirep->state-Setter + Comment-Insert (gemeinsame Logik)
├── Providers/
│   └── AeroACARSIntegrityGateServiceProvider.php   # Event-Registrierung
├── Resources/
│   └── lang/
│       ├── en/messages.php          # bilinguale Comment-Templates (Memory-Regel)
│       └── de/messages.php
└── Database/
    └── Migrations/
        └── 2026_06_01_000000_create_aeroacars_gate_audit_table.php
```

**Module-Installation:** VA-Owner kopiert das Module-Verzeichnis nach `modules/` auf `german-sky-group.eu`, läuft `php artisan migrate`, setzt `AEROACARS_GATE_SHARED_SECRET` in `.env`, aktiviert Module via phpVMS-Admin-Panel. Detailliertes Setup-Doc liegt als `Resources/SETUP.md` im Module.

### LE30 — Listener auf `PirepFiled`-Event (R11 mit Auto-Repair-First-Step)

**R11-Korrektur:** R10 beschrieb Per-PIREP-Defense-in-Depth in G.2.0, aber der Listener-Code rief das nicht auf. Das hätte den Same-Request-Bypass offen gelassen: Listener setzt PENDING → phpVMS-Submit liest aus dem **bereits geladenen** `$pirep->user->rank`-Relation den drift-Wert `auto_approve_acars=true` → setzt auf ACCEPTED. R11 macht Auto-Repair zum **ersten Schritt** im Listener und garantiert Relation-Refresh.

`Listeners/PirepFiledGate.php` (R11):

```php
public function __construct(
    private VpsClient $vpsClient,
    private AutoRepairService $autoRepairService,                     // R11 NEU
    private PirepStateManipulator $pirepStateManipulator,
    private AsyncVerdictRetryQueue $asyncRetryQueue,
) {}

public function handle(PirepFiled $event): void
{
    $pirep = $event->pirep;
    
    // ============================================================
    // R13 SCHRITT 1 (normativ): Auto-Repair-Defense-in-Depth
    // mit try/catch — Exceptions MÜSSEN in den Emergency-Fallback fallen,
    // sonst propagiert eine Repair-Exception unkontrolliert in phpVMS' 
    // Submit-Flow und PIREP landet in undefiniertem State.
    // ============================================================
    try {
        // autoRepairForPirep() prüft, repariert DB + cache + Relation
        // (siehe AutoRepairService::repairForPirep — Details in G.2.0).
        $this->autoRepairService->repairForPirep($pirep);
        
        // Defensive: Falls Rank trotz Repair noch drift zeigt (sollte unmöglich sein,
        // aber wir glauben dem in-memory-Wert nicht ohne Re-Check), refresh erzwingen.
        $pirep->load('user.rank');
        
        if ($pirep->user->rank->auto_approve_acars) {
            // R12+: Repair hat nicht gegriffen (DB-Update durch, aber Wert immer noch true).
            // Das ist „should never happen" — möglicherweise externer Plugin-Hook der
            // den Wert direkt wieder setzt, oder Read-Replica-Lag. Fail-closed via
            // REJECTED-Emergency-Hold (einziger State der den Submit-Flow überlebt).
            throw new AutoRepairVerificationFailedException(sprintf(
                'Rank %d (%s) still has auto_approve_acars=true after repair attempt',
                $pirep->user->rank->id, $pirep->user->rank->name
            ));
        }
        
    } catch (\Throwable $repairException) {
        // R13: jeder Auto-Repair-Failure → REJECTED-Emergency-Hold via fail-closed Pfad
        // (sowohl Verification-Failure als auch echte Repair-Exception wie DB-Lock,
        // Cache-Plugin-Error, etc.)
        Log::critical('AeroACARS-Gate: AUTO-REPAIR FAILED — falling back to REJECTED emergency-hold', [
            'pirep_id' => $pirep->id,
            'rank_id' => $pirep->user->rank->id ?? null,
            'rank_name' => $pirep->user->rank->name ?? null,
            'exception_class' => get_class($repairException),
            'exception_message' => $repairException->getMessage(),
        ]);
        $this->pirepStateManipulator->applyAutoRepairFailureFallback($pirep);
        $this->discord->sendAlarm(sprintf(
            "🚨 AeroACARS-Gate AUTO-REPAIR FAILED on PIREP %s: %s. Module in degraded state — investigate immediately (DB-Lock? Plugin-Conflict? Cache-Plugin verhindert Invalidation?).",
            $pirep->id, $repairException->getMessage()
        ));
        return;
    }
    
    // ============================================================
    // R11 SCHRITT 2: Skip wenn schon final-state
    // (defensive Programmierung, sollte nach Auto-Repair gleich sein)
    // ============================================================
    if (in_array($pirep->state, [
        PirepState::ACCEPTED, PirepState::REJECTED,
        PirepState::CANCELLED, PirepState::DELETED
    ], true)) {
        Log::info('AeroACARS-Gate: skipping PIREP in final state', [
            'pirep_id' => $pirep->id, 'state' => $pirep->state
        ]);
        return;
    }
    
    // ============================================================
    // R11 SCHRITT 3: VPS-Verdict einholen und State setzen
    // ============================================================
    try {
        $verdict = $this->vpsClient->getIntegrityVerdict($pirep->id);
        $this->pirepStateManipulator->applyVerdict($pirep, $verdict);
    } catch (VpsUnreachableException $e) {
        Log::warning('AeroACARS-Gate: VPS unreachable, holding PIREP (PENDING-Hold)', [
            'pirep_id' => $pirep->id, 'error' => $e->getMessage()
        ]);
        $this->pirepStateManipulator->applyTimeoutFallback($pirep);
        $this->asyncRetryQueue->enqueue($pirep->id);   // async Retry-Worker holt später
    } catch (VpsNoTelemetryException $e) {
        // R7: 404 vom VPS = keine MQTT-Telemetrie für diesen PIREP — fail-closed (für v0.13.0+-Clients)
        Log::warning('AeroACARS-Gate: VPS has no telemetry for PIREP, holding (unless legacy allowlist)', [
            'pirep_id' => $pirep->id, 'user_id' => $pirep->user_id
        ]);
        $this->pirepStateManipulator->applyNoTelemetryFallback($pirep);
    } catch (\Throwable $t) {
        // R7: catch \Throwable (nicht nur \Exception, PHP-Error-Hierarchie!) und fail-closed
        Log::error('AeroACARS-Gate: internal error, holding PIREP (fail-closed)', [
            'pirep_id' => $pirep->id,
            'throwable_class' => get_class($t),
            'message' => $t->getMessage(),
            'trace' => $t->getTraceAsString(),
        ]);
        $this->pirepStateManipulator->applyInternalErrorFallback($pirep, $t);
    }
}
```

**Wichtige Eigenschaft:** der Listener ist **synchron** (Laravel-Default für Listeners ohne `ShouldQueue`). Das ist nötig damit die State-Manipulation vor der Auto-Approve-Logik in `PirepService::file()` greift.

**Sync-Timeout-Budget (R7 korrigiert):** Der gesamte synchrone Pfad ist auf **max 3,5 s** beschränkt:
- 1 einmaliger HTTP-Call mit 3s-Timeout (kein In-Sync-Retry mehr — R6 hatte 3 Attempts × 5s + Backoff = ~16,5s, das blockierte das Pilot-Filing zu lange)
- 0,5 s Pufferzeit für State-Manipulation + Comment-Insert
- Bei jedem Failure: fail-closed Hold + async Retry-Worker übernimmt

**`R8/R9-Fail-Closed-Policy`:** für JEDEN Failure-Pfad (VPS timeout/network, VPS 404, internal Throwable, Config-Drift) wird der PIREP auf **PENDING-Hold** gesetzt (`integrity_state='held_*'`). Pilot bekommt **niemals** ACCEPTED ohne dass der Validator entweder „clean" gesagt oder Admin entschieden hat. Das ist die zentrale Sicherheitsgarantie gegen Bypass-Probleme. (R7 nutzte hier REJECTED-Hold, wurde in R8 auf PENDING-Hold mit `auto_approve_acars=false`-Voraussetzung umgestellt — siehe LE33 Lifecycle-Tabelle.)

### LE31 — VPS-Client im Module (R7: single-attempt sync + HMAC + async retry)

`Services/VpsClient.php`:

```php
public function getIntegrityVerdict(string $phpvmsPirepId): array
{
    $url = config('aeroacars_gate.vps_base_url')
         . '/api/integrity-check/' . urlencode($phpvmsPirepId);
    
    // R7: HMAC-Signatur statt nackt Shared-Secret
    $timestamp = time();
    $nonce = bin2hex(random_bytes(16));
    $bodyHash = hash('sha256', '');  // GET-Request hat leeren Body
    $signaturePayload = "$timestamp\n$nonce\n$bodyHash\nGET\n/api/integrity-check/$phpvmsPirepId";
    $secret = env('AEROACARS_GATE_SHARED_SECRET');
    $signature = hash_hmac('sha256', $signaturePayload, $secret);
    
    $headers = [
        'X-AeroACARS-Timestamp' => $timestamp,
        'X-AeroACARS-Nonce'     => $nonce,
        'X-AeroACARS-Signature' => $signature,
    ];
    
    // R7: EIN synchroner Versuch mit kurzem Timeout (statt 3× × 5s + Backoff = 16,5s)
    try {
        $response = Http::withHeaders($headers)
            ->timeout(3)  // R7: hart 3s, fail-closed wenn überschritten
            ->get($url);
        
        if ($response->successful()) {
            $this->auditService->logCall($phpvmsPirepId, 'verdict_received', $response->json());
            return $response->json();
        }
        
        if ($response->status() === 404) {
            // R7: 404 = VPS kennt PIREP nicht → SEPARATE Exception, kein silent fail-open mehr
            $this->auditService->logCall($phpvmsPirepId, 'vps_no_telemetry');
            throw new VpsNoTelemetryException("VPS has no telemetry for PIREP $phpvmsPirepId");
        }
        
        if ($response->status() === 401) {
            // Bad HMAC → kritischer Fehler
            $this->auditService->logCall($phpvmsPirepId, 'vps_auth_failed', null, $response->status());
            throw new VpsAuthException("Invalid HMAC signature — secret rotation may be needed");
        }
        
        // Anderer Status (4xx/5xx) → behandeln als unreachable
        throw new VpsUnreachableException("VPS returned status {$response->status()}");
        
    } catch (ConnectException | RequestException $e) {
        throw new VpsUnreachableException("VPS connection error: " . $e->getMessage());
    }
}
```

**Async-Retry-Worker (`Services/AsyncVerdictRetryWorker.php`) — R10 mit PirepService:**

Wenn `applyTimeoutFallback`-Pfad gegriffen hat (PENDING-Hold mit `integrity_state='held_vps_timeout'`), wird ein Background-Job enqueued der später erneut VPS fragt. **Der Worker läuft Stunden nach dem PirepFiled-Event, also OFF-FLOW** — kein umgebender phpVMS-Submit-Flow mehr aktiv. Damit ist die R9-Lifecycle-Boundary klar: **Worker muss `PirepService::accept()`/`->reject()` explizit aufrufen** (genauso wie der Webhook-Controller).

```php
class AsyncVerdictRetryWorker {
    public function __construct(
        private VpsClient $vpsClient,
        private PirepService $pirepSvc,   // ← off-flow Kontext, PirepService OK (analog Webhook)
        private \Illuminate\Database\ConnectionInterface $db,
    ) {}
    
    public function handle(string $pirepId): void {
        $pirep = Pirep::find($pirepId);
        if (!$pirep || $pirep->state !== PirepState::PENDING) {
            return;  // PIREP weg oder schon entschieden (z. B. Admin im Webhook) → nichts zu tun
        }
        
        try {
            $verdict = $this->vpsClient->getIntegrityVerdict($pirepId);
        } catch (\Throwable $e) {
            $this->enqueueNextRetry($pirepId, $e);
            return;
        }
        
        // R10: nutze PirepService (off-flow Kontext, kein Double-Trigger)
        if ($verdict['admin_decision'] === 'rejected') {
            $this->markFieldAndComment($pirep, 'rejected_final', '[AeroACARS] LATE REJECT', ...);
            $this->pirepSvc->reject($pirep);
        } elseif ($verdict['admin_decision'] === 'accepted'
               || $verdict['admin_decision'] === 'dismissed_fp'
               || (!$verdict['requires_review'] && $verdict['trust_level'] === 'trusted')) {
            $this->markFieldAndComment($pirep, 'auto_recovered', '[AeroACARS] LATE ACCEPT', ...);
            $this->pirepSvc->accept($pirep);
        }
        // requires_review → bleibt im PENDING-Hold, integrity_state wird auf 'held' aktualisiert
        else {
            $this->updateField($pirep, 'held');
        }
    }
}
```

Retry-Schedule: 30 s, 2 min, 10 min, 1 h, 4 h, 24 h, max 6 Attempts. Nach max-Attempts → finaler PENDING-Hold-Zustand, Admin muss manuell via Webhook (LE34) entscheiden.

**Konsistenz mit R9-Boundary:** Listener (im Submit-Flow) → kein PirepService. Webhook UND Worker (beide off-flow) → PirepService. Audit-Regel ist eindeutig: nur **eine einzige Code-Stelle** darf im Submit-Flow-Kontext State manipulieren — und das ist der PirepFiledGate-Listener via `setStateAndMark()`.

### LE31a — HMAC-Authentication-Schema (R7 neu, ersetzt R6 plain shared-secret)

**Webhook-Authentication zwischen Module und VPS** (beide Richtungen):

```
Headers auf JEDEM Request:
  X-AeroACARS-Timestamp: <unix-seconds>
  X-AeroACARS-Nonce:     <hex 16 bytes random>
  X-AeroACARS-Signature: <hex sha256 hmac>

Signature-Payload (in dieser exakten Reihenfolge):
  <timestamp>\n
  <nonce>\n
  <sha256-hash of request body, hex>\n  (empty body → hash of empty string)
  <http method (GET/POST)>\n
  <request path>

HMAC-Key: AEROACARS_GATE_SHARED_SECRET (env-Variable, beide Seiten)
```

**Server-Side-Verifikation:**

1. Parse Header-Triple. Wenn fehlt → 401.
2. Wenn `|now - timestamp| > 300` (5 min TTL) → 401 (replay-Schutz).
3. Wenn `nonce` schon in `nonce_replay_cache` für die letzten 5 min → 401 (replay-Schutz).
4. Re-compute Signature aus Body + Path + Method. Wenn `hash_equals()` false → 401.
5. Speichere `nonce` in Cache mit TTL 5 min.
6. Akzeptiere Request, processe normal.

**`nonce_replay_cache`-Tabelle (auf VPS UND in Module):**

```sql
CREATE TABLE hmac_nonce_replay_cache (
    nonce         TEXT PRIMARY KEY,
    seen_at       INTEGER NOT NULL,
    expires_at    INTEGER NOT NULL
);
CREATE INDEX idx_hmac_nonce_expires ON hmac_nonce_replay_cache(expires_at);
-- Cleanup-Job alle 10 min: DELETE WHERE expires_at < now
```

**Konsequenz für R6's Bedrohungs-Modell:** ein abgefangenes Secret allein reicht nicht mehr — Angreifer muss auch Replay-Cache-State kennen und valide HMACs in TTL-Window erzeugen. Standard-Webhook-Security-Pattern (vergleichbar mit Stripe-, GitHub-, Slack-Webhook-Signaturen).

### LE32 — VPS-Endpoint `/api/integrity-check/<pirep_id>` für Module-Lookup

Neuer Recorder-Endpoint (Stream D ergänzt):

```
GET /api/integrity-check/<phpvms_pirep_id>
  Auth: Header X-AeroACARS-Module-Secret: <shared-secret>
  Response 200:
  {
    "phpvms_pirep_id": "abc123",
    "found": true,
    "trust_level": "trusted" | "review" | "untrusted",
    "requires_review": bool,
    "flags": [
      { "type": "FUEL_RATE_IMPOSSIBLE", "severity": "critical", ... }
    ],
    "admin_decision": null | "accepted" | "rejected" | "dismissed_fp",
    "admin_decision_at": int?,
    "admin_decision_by": string?,
    "reject_reason": string?
  }
  Response 404:
  { "phpvms_pirep_id": "abc123", "found": false }
    → wenn VPS-Recorder den PIREP nicht kennt (alter v0.12.x-Client der ohne 
      MQTT-Telemetrie filed, oder Test-PIREP ohne Validator-Lauf)
  Response 401:
  { "error": "invalid_module_secret" }
```

**Auth-Validierung:** Recorder vergleicht Header gegen `AEROACARS_GATE_SHARED_SECRET` aus `/etc/aeroacars-recorder.env`. Constant-time-Vergleich (timing-attack-safe).

**Implementierung:** Lookup über bestehende `pireps`-Tabelle (R4-Datenmodell hat schon `integrity_flags`, `score_trust_level`, `requires_review`, `review_state`, `review_decision`). Endpoint ist ein simpler READ — keine neuen Spalten nötig.

**Wichtig:** der PIREP wird im Recorder über die `phpvms_pirep_id` identifiziert — die ist während des Flugs schon im Recorder vorhanden (via MQTT-Telemetrie). Die Spalte heißt aktuell evtl. nur `pirep_id` — wenn nicht eindeutig, ggf. rename auf `phpvms_pirep_id` für Klarheit.

### LE33 — Verhalten je VPS-Response (R9 final — Listener-vs-Webhook-Lifecycle-Split)

**R9-Kernumstellung gegenüber R8:** R8 ließ den Listener `PirepService::accept()`/`->reject()` direkt rufen — **das war double-trigger-anfällig**. Der phpVMS-`submit()`-Flow läuft NACH unserem Listener weiter und ruft basierend auf dem State **selbst** `accept()`/`reject()`. Wenn unser Listener das ZUSÄTZLICH macht, laufen alle Side-Effects (Hours, Pay, Stats, Notifications, Audit) **doppelt**.

**R9-Lifecycle-Split (normativ):**

| Kontext | Mechanismus | Begründung |
|---|---|---|
| **Listener** (`PirepFiledGate.php`, im Submit-Flow) | **Nur** `$pirep->state = X; $pirep->save();` — KEINE PirepService-Aufrufe | phpVMS' Submit-Flow läuft nach unserem Listener weiter und ruft `accept()`/`reject()` automatisch je nach State (siehe `PirepService.php` L341-360). Wir setzen nur den State, phpVMS macht den Lifecycle EINMAL. |
| **Webhook** (`DecisionWebhookController.php`, separate HTTP-Request) | **PirepService::accept()/->reject()** explizit aufrufen | Kein umgebender Submit-Flow — wir sind der einzige Lifecycle-Trigger, also müssen wir es explizit machen sonst läuft gar nichts. |

**phpVMS-Submit-Flow-Verständnis (R9 nochmal verifiziert):**

```php
event(new PirepFiled($pirep));   // ← unser Listener läuft hier
$pirep->refresh();                // ← lädt unsere Listener-Änderungen aus DB

if (state != ACCEPTED && != REJECTED) → $default_state = PENDING
else → $default_state = $pirep->state

if (state === PENDING || DRAFT) {
    if (auto_approve_acars) → $default_state = ACCEPTED  // bei uns false → skip
}

if ($default_state === ACCEPTED) $this->accept($pirep);   // ← phpVMS macht es
elseif ($default_state === REJECTED) $this->reject($pirep); // ← phpVMS macht es
else $pirep->state = $default_state;                       // ← PENDING bleibt PENDING
$pirep->save();
```

**Listener-Verhalten je VPS-Verdict (R9 korrekt):**

| Verdict | Listener-Aktion | Was phpVMS-Submit dann macht |
|---|---|---|
| clean | `$pirep->state = ACCEPTED; save();` + Field + Comment | `$default_state = ACCEPTED` → `accept()` läuft EINMAL durch phpVMS |
| requires_review | `$pirep->state = PENDING; save();` + Field='held' + Comment | `$default_state = PENDING` → `else`-Zweig → state bleibt PENDING (kein Lifecycle), `save()` |
| admin_decision=rejected | `$pirep->state = REJECTED; save();` + Field + Comment | `$default_state = REJECTED` → `reject()` läuft EINMAL durch phpVMS |
| admin_decision=accepted | `$pirep->state = ACCEPTED; save();` + Field + Comment | `$default_state = ACCEPTED` → `accept()` läuft EINMAL durch phpVMS |
| admin_decision=dismissed_fp | `$pirep->state = ACCEPTED; save();` + Field='dismissed_fp' + Comment | gleich wie accepted, andere Markierung |
| timeout / 404 / Throwable | `$pirep->state = PENDING; save();` + Field + Comment | PENDING bleibt PENDING, kein Lifecycle |

**Webhook-Verhalten je Admin-Decision (R9 — PirepService nutzen weil kein Submit-Flow):**

| Decision | Webhook-Aktion |
|---|---|
| accepted_with_warning | `$this->pirepSvc->accept($pirep)` (proper Hours+Pay+Stats+Notification) |
| dismissed_fp | `$this->pirepSvc->accept($pirep)` + distinct Comment |
| rejected | `$this->pirepSvc->reject($pirep)` (proper Reverse-Pfad) |
| reopen | siehe LE34, separate Sektion |

**Keine doppelten Side-Effects mehr.** Im Listener läuft phpVMS' Submit-Lifecycle einmal. Im Webhook läuft unser expliziter PirepService-Call einmal. Beide Kontexte separat und sauber.

**Hold-vs-Final-Reject-Unterscheidung (R8 — PENDING-basiert):**

| Zustand | phpVMS-State | PirepFieldValue `aeroacars_integrity_state` | PirepService-Call | Comment-Präfix |
|---|---|---|---|---|
| Clean (kein Review nötig) | ACCEPTED | `'auto_accepted'` | `accept()` | `[AeroACARS] AUTO-ACCEPTED: trust_level=trusted` |
| Held for review | PENDING | `'held'` | keiner (direkt PENDING) | `[AeroACARS] HELD FOR REVIEW: <details>` |
| Final reject by admin | REJECTED | `'rejected_final'` | `reject()` | `[AeroACARS] REJECTED: <reason>` |
| Accepted with warning | ACCEPTED | `'accepted_with_warning'` | `accept()` | `[AeroACARS] ACCEPTED WITH WARNING: <by>` |
| Dismissed as FP | ACCEPTED | `'dismissed_as_false_positive'` | `accept()` | `[AeroACARS] DISMISSED FP: <by>` |
| Held due to VPS timeout | PENDING | `'held_vps_timeout'` | keiner | `[AeroACARS] HELD - VPS unreachable: ...` |
| Held due to internal error | PENDING | `'held_internal_error'` | keiner | `[AeroACARS] HELD - internal gate error: ...` |
| Held due to no telemetry | PENDING | `'held_no_telemetry'` | keiner | `[AeroACARS] HELD - no telemetry received: ...` |

**Pilot-UX:** Pilot sieht PIREP als PENDING (gelb/orange, neutraler Zustand) während der Review. Keine false REJECTED-Mail mehr. Wenn Admin später akzeptiert → normaler accept()-Flow inkl. „PIREP accepted"-Mail. Wenn Admin rejected → normaler reject()-Flow inkl. „PIREP rejected"-Mail mit Reason. Konsistente phpVMS-Standard-Lifecycle, kein UX-Disaster.

**`Services/PirepStateManipulator.php` (R9 — kein PirepService-Call im Listener-Kontext):**

```php
public function __construct(
    private \Illuminate\Database\ConnectionInterface $db,
) {}
// NOTE R9: PirepService wird hier NICHT injected — Listener-Kontext darf accept/reject 
// NICHT direkt rufen (würde phpVMS-Submit-Flow-Lifecycle doppelt triggern).
// Für Webhook-Kontext gibt es einen separaten Service (siehe LE34).

public function applyVerdict(Pirep $pirep, array $verdict): void
{
    // R9: Listener setzt NUR State + Field + Comment.
    // phpVMS' Submit-Flow ruft accept()/reject() EINMAL je nach State (siehe LE33 Tabelle).
    
    // 1. Admin schon entschieden
    if ($verdict['admin_decision'] === 'rejected') {
        $this->setStateAndMark($pirep, PirepState::REJECTED, 'rejected_final',
            '[AeroACARS] REJECTED',
            __('aeroacars_gate::messages.rejected_by_admin', [
                'reason'     => $verdict['reject_reason'] ?? 'no reason given',
                'decided_by' => $verdict['admin_decision_by'],
            ]));
        // phpVMS-Submit-Flow ruft jetzt $this->reject() einmal — KEIN expliziter Call hier
        return;
    }
    if ($verdict['admin_decision'] === 'accepted') {
        $this->setStateAndMark($pirep, PirepState::ACCEPTED, 'accepted_with_warning',
            '[AeroACARS] ACCEPTED WITH WARNING',
            __('aeroacars_gate::messages.accepted_by_admin_with_warning', [
                'flag_count' => count($verdict['flags']),
                'decided_by' => $verdict['admin_decision_by'],
            ]));
        // phpVMS ruft $this->accept() einmal
        return;
    }
    if ($verdict['admin_decision'] === 'dismissed_fp') {
        $this->setStateAndMark($pirep, PirepState::ACCEPTED, 'dismissed_as_false_positive',
            '[AeroACARS] DISMISSED FP',
            __('aeroacars_gate::messages.dismissed_as_false_positive', [
                'flag_count' => count($verdict['flags']),
                'decided_by' => $verdict['admin_decision_by'],
            ]));
        // phpVMS ruft $this->accept() einmal
        return;
    }
    
    // 2. Kein Admin-Verdict
    if ($verdict['requires_review']) {
        $this->setStateAndMark($pirep, PirepState::PENDING, 'held',
            '[AeroACARS] HELD FOR REVIEW',
            __('aeroacars_gate::messages.held_for_review', [
                'flag_count'     => count($verdict['flags']),
                'critical_count' => count(array_filter($verdict['flags'],
                    fn($f) => $f['severity'] === 'critical')),
                'review_url'     => $this->buildReviewUrl($pirep->id),
            ]));
        // phpVMS sieht PENDING + auto_approve=false → else-Zweig → state bleibt PENDING
        // KEIN accept(), KEIN reject(), kein Side-Effect — exakt was wir wollen
        return;
    }
    
    // 3. Clean
    $this->setStateAndMark($pirep, PirepState::ACCEPTED, 'auto_accepted',
        '[AeroACARS] AUTO-ACCEPTED',
        __('aeroacars_gate::messages.clean_auto_accepted', [
            'trust_level' => $verdict['trust_level']
        ]));
    // phpVMS ruft $this->accept() einmal — proper Hours+Pay+Stats+Notification
}

public function applyTimeoutFallback(Pirep $pirep): void
{
    $this->setStateAndMark($pirep, PirepState::PENDING, 'held_vps_timeout',
        '[AeroACARS] HELD - VPS unreachable',
        __('aeroacars_gate::messages.vps_unreachable_held'));
}

public function applyInternalErrorFallback(Pirep $pirep, \Throwable $e): void
{
    $this->setStateAndMark($pirep, PirepState::PENDING, 'held_internal_error',
        '[AeroACARS] HELD - internal gate error',
        __('aeroacars_gate::messages.internal_error_held', [
            'error_class' => get_class($e)
        ]));
}

public function applyNoTelemetryFallback(Pirep $pirep): void
{
    if ($this->isLegacyAllowlistedPilot($pirep->user_id)) {
        $this->setStateAndMark($pirep, PirepState::ACCEPTED, 'legacy_allowlisted',
            '[AeroACARS] LEGACY ALLOWLISTED',
            __('aeroacars_gate::messages.legacy_allowlisted'));
        // phpVMS ruft accept() — Legacy-Pilot bekommt normalen Accept-Flow
        return;
    }
    $this->setStateAndMark($pirep, PirepState::PENDING, 'held_no_telemetry',
        '[AeroACARS] HELD - no telemetry received',
        __('aeroacars_gate::messages.no_telemetry_held'));
}

// R13 ENTFERNT: applyConfigDriftFallback() existiert nicht mehr.
//
// Begründung: die Methode setzte PENDING-Hold bei Config-Drift, was unsicher
// war — wenn auto_approve_acars=true UND state=PENDING, dreht phpVMS-Submit
// das wieder auf ACCEPTED. R12-Befund und R13-Härtung ersetzen diese Methode
// durch applyAutoRepairFailureFallback() (REJECTED-Hold, fail-closed).
//
// Im Listener-Code (LE30) wird applyAutoRepairFailureFallback() aus dem
// try/catch um repairForPirep() aufgerufen — kein Code-Pfad ruft mehr
// applyConfigDriftFallback().
//
// Test-Audit-Regel: grep durch Module-Codebase darf KEINE Referenz mehr
// auf applyConfigDriftFallback finden — sonst fail-open Risiko.

public function applyAutoRepairFailureFallback(Pirep $pirep): void
{
    // R12 NEU: Auto-Repair lief, aber Rank zeigt IMMER NOCH auto_approve_acars=true.
    // Hier kann KEIN PENDING-Hold mehr sicher sein (phpVMS-Submit würde es wieder
    // auf ACCEPTED dreht wegen auto_approve_acars=true). 
    // EINZIGE fail-closed Option: REJECTED.
    //
    // Konsequenz: phpVMS' submit() ruft danach $this->reject($pirep) automatisch.
    // - Reject-Notification feuert (Pilot wird informiert "PIREP rejected")
    // - Hours-Reverse läuft (no-op da PIREP nie ACCEPTED war)
    // - PIREP landet als REJECTED auf der GSG-Website mit erklärendem Comment
    //
    // Dieser Pfad ist „should never happen". Wenn er ausgelöst wird, ist das ein
    // echter Operations-Incident: DB-Lock, Plugin-Conflict, oder phpVMS-Cache-Bug
    // verhindert Auto-Repair. Der Pilot bekommt Reject-Notification mit klarem
    // Text, VA-Owner bekommt 🚨-Alarm via Discord (siehe LE30).
    $this->setStateAndMark($pirep, PirepState::REJECTED, 'emergency_repair_failure',
        '[AeroACARS] EMERGENCY HOLD - Auto-Repair failed',
        __('aeroacars_gate::messages.auto_repair_failure_held', [
            'support_contact' => config('aeroacars_gate.va_admin_contact', 'VA admin')
        ]));
}

private function setStateAndMark(Pirep $pirep, int $newState, string $integrityState,
                                  string $commentPrefix, string $comment): void
{
    $pirep->state = $newState;
    $pirep->save();   // ← einziger DB-Write, phpVMS-Submit übernimmt Lifecycle-Calls

    PirepFieldValue::updateOrCreate(
        ['pirep_id' => $pirep->id, 'slug' => 'aeroacars_integrity_state'],
        ['value' => $integrityState, 'name' => 'AeroACARS Integrity State']
    );
    $this->db->table('pirep_comments')->insert([
        'pirep_id'   => $pirep->id,
        'user_id'    => null,
        'comment'    => "$commentPrefix: $comment",
        'created_at' => now(),
        'updated_at' => now(),
    ]);
}
```

**Code-Audit-Hinweis (R9 verbindlich):** Code-Review prüft dass `PirepStateManipulator` **niemals** `PirepService::accept()` oder `->reject()` direkt aufruft. Wer das addiert, riskiert doppelte Lifecycle-Side-Effects. Einziger erlaubter Direct-Call-Kontext ist der Webhook-Controller (siehe LE34).

**Konsequenz:** kein doppeltes accept()/reject() mehr (R8-Bug behoben). Kein false-Reject-Mail (kein reject() für Holds). Korrekte Notifications für clean Accepts. Proper Reverse-Pfade für Admin-Rejects. Alles über genau einen Lifecycle-Trigger pro Submit-Call.

### LE34 — Webhook-Endpoint im Module für nachträgliche Admin-Entscheidungen (R8 — PirepService-basiert)

**R8-Kernkorrektur ggü. R7:** R7's Webhook nutzte direktes `$pirep->state = X; $pirep->save()` was die proper Lifecycle-Methoden umging — Hours/Pay/Stats/Rank wurden nicht aktualisiert. R8 nutzt zwingend `PirepService::accept()` und `PirepService::reject()` für State-Wechsel.

`Http/Controllers/DecisionWebhookController.php` (R8):

```php
public function __construct(
    private PirepService $pirepSvc,
    private \Illuminate\Database\ConnectionInterface $db,
) {}

public function pirepDecision(Request $request)
{
    // Auth: HMAC via Middleware (siehe LE31a)
    $body = $request->validate([
        'phpvms_pirep_id'  => 'required|string',
        'target_state'     => 'required|in:accepted_with_warning,rejected,dismissed_fp,reopen',
        // R11: reason REQUIRED für rejected/dismissed_fp/reopen (konsistent mit LE21b State-Machine)
        'decision_reason'  => 'required_if:target_state,rejected,dismissed_fp,reopen|string|max:500',
        'decided_by'       => 'required|string',
        'decided_at'       => 'required|integer',
    ]);
    
    $pirep = Pirep::find($body['phpvms_pirep_id']);
    if (!$pirep) {
        return response()->json(['error' => 'pirep_not_found'], 404);
    }
    
    $currentState = $pirep->state;
    
    switch ($body['target_state']) {
        
        case 'accepted_with_warning':
            // R8: proper PirepService::accept() — funktioniert von PENDING (Held → Accepted)
            // oder bei R7-Legacy auch von REJECTED (Re-Aktivierung)
            $this->markFieldAndComment($pirep, 'accepted_with_warning',
                sprintf('[AeroACARS] ACCEPTED WITH WARNING by %s: %s',
                    $body['decided_by'], $body['decision_reason'] ?? '(no reason)'));
            $this->pirepSvc->accept($pirep);
            break;
            
        case 'dismissed_fp':
            // R8: gleicher Lifecycle-Effekt wie accept_with_warning, separate Markierung
            $this->markFieldAndComment($pirep, 'dismissed_as_false_positive',
                sprintf('[AeroACARS] DISMISSED FP by %s: %s',
                    $body['decided_by'], $body['decision_reason'] ?? '(false positive in detector)'));
            $this->pirepSvc->accept($pirep);
            break;
            
        case 'rejected':
            // R8: proper PirepService::reject() für Hours-Reverse/Pay-Reverse/Notification
            $this->markFieldAndComment($pirep, 'rejected_final',
                sprintf('[AeroACARS] REJECTED by %s: %s',
                    $body['decided_by'], $body['decision_reason']));
            $this->pirepSvc->reject($pirep);
            break;
            
        case 'reopen':
            // R12: reopen ist NUR von PENDING oder REJECTED erlaubt.
            // ACCEPTED ist hart NICHT supported — kein side-effect-freier Reverse-Pfad
            // ohne phpVMS-Core-Änderung. Das gilt für ALLE PIREPs mit phpVMS-State=ACCEPTED,
            // unabhängig vom integrity_state:
            // - accepted_with_warning → phpVMS-State=ACCEPTED → reopen NICHT erlaubt
            // - dismissed_as_false_positive → phpVMS-State=ACCEPTED → reopen NICHT erlaubt
            // - auto_accepted → phpVMS-State=ACCEPTED → reopen NICHT erlaubt
            //
            // VA-Admin muss für seltene Reverse-Cases das phpVMS-Admin-Panel direkt
            // nutzen (phpVMS-Native-Funktionalität, kein Module-Feature).
            if ($currentState === PirepState::ACCEPTED) {
                return response()->json([
                    'error' => 'reopen_not_allowed_from_accepted',
                    'message' => 'Reopen of an ACCEPTED PIREP is not supported. '
                               . 'phpVMS does not provide a side-effect-free reverse pipeline '
                               . 'from ACCEPTED. This applies to all integrity_state values '
                               . 'including accepted_with_warning, dismissed_as_false_positive '
                               . 'and auto_accepted. Use phpVMS-Admin-UI to manually edit the '
                               . 'PIREP if absolutely necessary.',
                    'current_state' => $currentState,
                    'allowed_from'  => ['PENDING', 'REJECTED'],
                ], 400);
            }
            // Trivial für PENDING/REJECTED — kein Lifecycle-Eingriff
            $pirep->state = PirepState::PENDING;
            $pirep->save();
            $this->markFieldAndComment($pirep, 'held',
                sprintf('[AeroACARS] REOPENED to HELD by %s: %s',
                    $body['decided_by'], $body['decision_reason']));
            break;
    }
    
    $pirep->refresh();
    return response()->json([
        'phpvms_pirep_id'           => $pirep->id,
        'phpvms_state'              => $pirep->state,
        'aeroacars_integrity_state' => PirepFieldValue::where('pirep_id', $pirep->id)
            ->where('slug', 'aeroacars_integrity_state')->value('value'),
        'updated_at'                => now()->getTimestampMs(),
    ]);
}

private function markFieldAndComment(Pirep $pirep, string $integrityState, string $comment): void
{
    PirepFieldValue::updateOrCreate(
        ['pirep_id' => $pirep->id, 'slug' => 'aeroacars_integrity_state'],
        ['value' => $integrityState, 'name' => 'AeroACARS Integrity State']
    );
    $this->db->table('pirep_comments')->insert([
        'pirep_id'   => $pirep->id, 'user_id' => null,
        'comment'    => $comment,
        'created_at' => now(), 'updated_at' => now(),
    ]);
}
```

**reopen-Semantik (R10 final — von-ACCEPTED hart entfernt):**
- **reopen-from-PENDING-held:** trivial, state bleibt PENDING, nur `integrity_state` bleibt auf `'held'` (Re-Review-Marker via Comment).
- **reopen-from-REJECTED-final:** state → PENDING, `integrity_state` → `'held'`. Stats sind durch reject() bereits revert, kein Lifecycle-Eingriff nötig.
- **reopen-from-ACCEPTED: NICHT erlaubt** (Webhook returns 400). phpVMS bietet keinen side-effect-freien Reverse-Pfad aus ACCEPTED. Wenn VA-Admin wirklich einen akzeptierten PIREP zurückrollen muss: direkt im phpVMS-Admin-Panel editieren (das ist phpVMS-Native-Funktionalität, kein Module-Feature). Kein Verschieben auf späteres Release — dieses Feature kommt explizit nicht in AeroACARS.

VPS ruft diesen Endpoint synchron auf wenn Admin in der Review-Queue eine Entscheidung trifft. Wenn der Webhook fehlschlägt (Network/Auth/Validation), schreibt VPS in die Retry-Queue (siehe LE36).

### LE35 — Shared-Secret-Auth zwischen Module und VPS

**Setup-Schritte (einmalig durch VA-Owner):**

1. Generiere random Secret: `openssl rand -base64 32` → z. B. `K3v8X9...AbCdEf`
2. Auf VPS in `/etc/aeroacars-recorder.env`:
   ```
   AEROACARS_GATE_SHARED_SECRET=K3v8X9...AbCdEf
   ```
   (chmod 0400, owned by aeroacars-User)
3. Auf phpVMS in `.env`:
   ```
   AEROACARS_GATE_SHARED_SECRET=K3v8X9...AbCdEf
   ```
4. Restart beides
5. Test: VA-Admin triggert manuellen Health-Check über Webapp-Settings-Tab — Recorder ruft Module-Health-Endpoint, Module ruft Recorder-Health-Endpoint, beide bestätigen wechselseitig

**Rotation:** Wenn Secret kompromittiert: neues generieren, gleichzeitig auf Module und VPS deployen, restart beides. Während der Rotation: kurzer Window in dem PIREP-Filings entweder doppelt-PENDING gehen (alter Module + neuer VPS) oder fehlschlagen — operativ kalkulierbar (Wartungsfenster).

**Vergleich zum R5-Sicherheits-Modell:**

| R5 (VPS-Filing) | R6 (Module) |
|---|---|
| Alle Pilot-API-Keys auf VPS (encrypted) | Ein Shared-Secret zwischen 2 Servern |
| Bei VPS-Kompromittierung: alle Piloten exponiert | Bei VPS-Kompromittierung: nur das Secret, Pilot-Keys unberührt |
| Pilot-Revoke: muss neuen Key generieren, neu provisionieren | Pilot-Revoke: betrifft nur sein eigenes Filing, nichts an Module zu rotieren |

R6 hat **eine Größenordnung kleineren Sicherheits-Footprint**.

### LE36 — Retry-Queue für VPS↔Module-Webhook (bei Decision-Sync-Failure)

Wenn der Admin in der Review-Queue entscheidet und VPS den Module-Webhook nicht erreicht (Network, Auth-Error, 5xx):

```sql
CREATE TABLE module_webhook_retry_queue (
    id                    INTEGER PRIMARY KEY AUTOINCREMENT,
    phpvms_pirep_id       TEXT NOT NULL,
    target_state          TEXT NOT NULL,
    decision_reason       TEXT,
    decided_by            TEXT NOT NULL,
    decided_at            INTEGER NOT NULL,
    attempts              INTEGER DEFAULT 0,
    next_attempt_at       INTEGER NOT NULL,
    last_error            TEXT,
    status                TEXT DEFAULT 'pending',  -- 'pending' | 'delivered' | 'failed_hard'
    UNIQUE(phpvms_pirep_id, decided_at)
);
CREATE INDEX idx_mwrq_next_attempt 
    ON module_webhook_retry_queue(status, next_attempt_at) 
    WHERE status = 'pending';
```

Background-Worker im Recorder retried mit Backoff: 30s, 2min, 10min, 1h, 4h, 24h, max 6 Versuche. Bei `status='failed_hard'`: VA-Admin sieht Eintrag in der „Failed Module Webhooks"-Sub-View der Review-Queue, kann manuell retriggern oder direkt im phpVMS-Admin-Panel den PIREP-State setzen.

### LE37 — Migration & Kompatibilität (R7 korrigiert — Hard-Cut auf v0.13.0+)

**R6's Aussage „alte v0.12.x-Clients funktionieren automatisch" widersprach dem Datenmodell** (das von Stream A/B gefordert wird: jeder Snapshot braucht `session_id` + `sequence_number`, sonst wird er vom Recorder rejected). R7 löst den Widerspruch durch Hard-Cut auf v0.13.0+.

**Hard-Cut-Policy:**

| Aspekt | Verhalten |
|---|---|
| **v0.13.0+-Client** | Sendet MQTT-Telemetrie mit session_id+sequence_number → VPS hat Daten → `/api/integrity-check` liefert Verdict → Module entscheidet je nach Verdict |
| **v0.12.x-Client filed direkt** | Sendet keine v0.13.0-Telemetrie → VPS hat keine Daten → 404 von `/api/integrity-check` → Module **fail-closed PENDING-Hold** mit `integrity_state='held_no_telemetry'` (LE33). Pilot sieht PIREP als PENDING auf der GSG-Website mit klarem Comment. |
| **v0.12.x-Client möchte aktiv weiterfliegen** | Pilot muss auf v0.13.0+ updaten — Module hält jeden seiner PIREPs |

**Migrations-Plan (Pilot-Kommunikation):**

| Phase | Dauer | Aktion |
|---|---|---|
| **Phase 0** | 4 Wochen VOR Release v0.13.0 | Discord/Email-Ankündigung: „v0.13.0 kommt, neue Integrity-Validierung. Bitte updaten sobald released." |
| **Phase 1** | Release-Tag bis +1 Woche | In-App-Update-Banner bei Login für v0.12.x-Clients: „v0.13.0 verfügbar — bitte updaten innerhalb 7 Tage, sonst werden PIREPs ab Tag 8 zur Review gehalten." Auto-Update-Push via Tauri-Updater. |
| **Phase 2** | Ab +1 Woche nach Release | Hard-Cut aktiv. v0.12.x-PIREPs landen automatisch in **PENDING-Hold** mit `integrity_state='held_no_telemetry'`. VA-Admin sieht in Review-Queue „PILOT_USING_OUTDATED_CLIENT", kann manuell accept_with_warning entscheiden für Einzelfälle. |

**Legacy-Allowlist-Mechanismus** (Notfall-Override):

```sql
CREATE TABLE legacy_pilot_allowlist (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    va_prefix       TEXT NOT NULL,
    pilot_id        TEXT NOT NULL,
    reason          TEXT NOT NULL,         -- z. B. "Pilot can't update due to Win7 OS"
    added_by        TEXT NOT NULL,         -- VA-Admin-Identifier
    added_at        INTEGER NOT NULL,
    expires_at      INTEGER,               -- optional Auto-Expire (z. B. nach 30 Tagen)
    UNIQUE(va_prefix, pilot_id)
);
```

VA-Admin kann einzelne Piloten via Web-Monitor zur Allowlist hinzufügen — diese Piloten bekommen bei 404-from-VPS **keinen** Hold-Eintrag sondern werden durchgelassen (Module ruft `applyNoTelemetryFallback` → `isLegacyAllowlistedPilot()` returnt true → kein State-Change). Mit expires_at-Feld kann VA-Admin Allowlist-Einträge zeitlich begrenzen.

**Begründung Hard-Cut statt Soft-Cut:**
- Soft-Cut (R6's Vorschlag) öffnete den Bypass: Pilot installiert nie v0.13.0, sein PIREP filed weiterhin clean, weil 404 = trusted return
- Integrity-Gate ist nur so stark wie sein schwächster Pfad
- 4 Wochen Vorlauf + Auto-Update + Allowlist für Härtefälle = ausreichend großzügig

**Module-Versions-Kompatibilität:**

| AeroACARSIntegrityGate-Version | phpVMS-Min-Version | VPS-Recorder-Version |
|---|---|---|
| v1.0 (Initial) | phpVMS 7.0.0+ | v0.13.0+ |

Wenn phpVMS in einer späteren Version den `PirepFiled`-Event ändert oder das `PirepState`-Enum erweitert, müssen wir das Module nachziehen. Risiko bewertbar: DisposableBasic hat seit Jahren denselben Hook ohne Bruch genutzt.

**Module-Update-Pfad bei phpVMS-Major-Update:** VA-Owner pullt neue Module-Version, läuft `php artisan migrate`, kein Restart nötig (Listener-Reload geschieht beim nächsten Request).

### G.4 Sicherheits-Sektion (R6 — drastisch vereinfacht)

**Was sich ändert ggü. R5:** statt aller Pilot-API-Keys auf VPS halten wir nur **ein einziges Shared-Secret** zwischen Module und VPS. Pilot-Keys bleiben ausschließlich am Client.

**Threat-Model + Mitigationen (R7 verschärft):**

| Threat | Mitigation |
|---|---|
| Angreifer kompromittiert VPS, exfiltriert `AEROACARS_GATE_SHARED_SECRET` | **R7:** HMAC-Signatur über body+timestamp+nonce, 5min-TTL, Replay-Cache. Allein das Secret reicht nicht mehr — Angreifer braucht auch valide Timestamps + Nonce-Tracking. Bei Verdacht: sofortige Secret-Rotation, alle alten HMACs werden ungültig. |
| Angreifer kompromittiert phpVMS-Server | War schon vor v0.13.0 möglich (PIREPs editieren, Bestenliste manipulieren). v0.13.0 ändert das Risiko nicht. |
| Angreifer fängt Module ↔ VPS HTTPS-Traffic ab | TLS (Let's Encrypt auf live.kant.ovh, eigenes Zert auf german-sky-group.eu). **R7:** HMAC zusätzlich schützt selbst bei TLS-Bruch (z. B. MitM-CA). |
| **R10/R11 — Module-Listener crasht** | Listener catcht `\Throwable`, fail-closed **PENDING-Hold** mit `integrity_state='held_internal_error'`. Kein PIREP geht clean durch ohne Verdict. (Historie: R7 hatte REJECTED-Hold, R8 auf PENDING umgestellt mit `auto_approve_acars=false`-Voraussetzung — bleibt R11 so.) |
| **R10/R11 — VPS unreachable** | 1× 3s-Sync, dann fail-closed **PENDING-Hold** + async Retry-Worker (auto-recovery wenn VPS wieder da). |
| **R10/R11 — Bypass via PIREP-ohne-Telemetrie** | 404 vom VPS → fail-closed **PENDING-Hold** mit `integrity_state='held_no_telemetry'`. Nur über explizite `legacy_pilot_allowlist` durchlassen. |
| **R10/R11 — PENDING-Auto-Approve-Bypass** | Hold-State ist PENDING + **`auto_approve_acars=FALSE`** auf allen Ranks. R10 erweiterte: Auto-Repair-Pattern (Module sichert die Config selbst, Boot + Daily-Cron). **R11 erweiterte: Auto-Repair ist erster Schritt im Listener (LE30) mit DB-Update + Cache-Invalidation + Relation-Refresh** — verhindert Same-Request-Bypass durch in-memory cached Rank-Relation. |
| Recorder-Code loggt Verdict mit Pilot-PII | Verdict enthält keine PII (nur Flags + State). Audit-Log capturt Action-Typ + pirep_id, kein Secret. |
| VPS-Admin manipuliert PIREP-States | Kein Mitigations-Code — VPS-Admin ist trust root. Audit-Log macht es auditierbar. |
| Pilot bekommt fälschliche Reject-Notification bei held-Status | **Nicht-Problem in R8+**: PENDING-Hold triggert keinen `reject()`-Aufruf → keine false Reject-Notification. VPS sendet bei held-State eine eigene „Held for Review"-Notification an Pilot (Discord/E-Mail). |
| **R10/R11 — VA-Owner ändert auto_approve_acars zurück auf true** | Auto-Repair-Pattern korrigiert das automatisch: Boot, Daily-Cron, oder spätestens beim nächsten PIREP des betroffenen Piloten via Per-PIREP-Defense-in-Depth (LE30 + AutoRepairService). Drift wird in Sekunden korrigiert, vollständig protokolliert + Discord-Notification + Admin-Banner 24h. **Kein Emergency-Mode mehr** (R9's Konzept verworfen — siehe Pivot-Historie G.2). |
| **R10/R11 — Neuer Rank wird ohne auto_approve_acars=false angelegt** | Auto-Repair erkennt + repariert beim Boot, Daily-Cron, oder beim ersten PIREP-Filing eines Piloten dieses Rank. Discord-Notification dass Drift erkannt + auto-corrected wurde. |
| **R11 NEU — Same-Request-Cache-Bypass via Eloquent-Relation** | Wenn Auto-Repair nur die DB updated, aber phpVMS' Submit-Code im selben Request den bereits geladenen `$pirep->user->rank`-Relation-Cache mit `auto_approve_acars=true` liest, würde PENDING auf ACCEPTED gedreht. **R11-Mitigation:** `AutoRepairService::repairForPirep()` invalidiert Laravel-Cache + ruft `$pirep->user->setRelation('rank', $rank->fresh())` + `$pirep->load('user.rank')` — garantiert dass die nachfolgende Submit-Code-Auto-Approve-Prüfung den frischen `false`-Wert sieht. Test `same_request_relation_cache_invalidated_after_autorepair`. |

**Pilot-Communication:** Keine Modal mehr nötig (Pilot-Workflow ist unverändert — er filed wie immer). Optional: nach v0.13.0-Release ein Discord-/Forum-Posting „Wir haben eine Integrity-Validierung eingeführt — manche PIREPs landen jetzt in PENDING bis VA-Admin sie geprüft hat. Das ist normal."

### Stream G — Tests

| Test | Prüft |
|---|---|
| `install_auto_disables_auto_approve_acars_on_all_ranks` (R10) | Module-Install setzt auto_approve_acars=false auf allen Ranks + Audit-Log |
| `boot_auto_repair_finds_and_repairs_drift` (R10) | Beim Boot ein Rank mit auto_approve_acars=true → auto-repair + Discord-Notification + Audit-Log |
| `daily_cron_auto_repair_finds_and_repairs_drift` (R10) | Daily-Cron erkennt neu hinzugefügten Rank mit auto_approve_acars=true → repair + notify |
| `per_pirep_defense_in_depth_repairs_rank_on_demand` (R10) | PirepFiled-Event von Pilot mit drift-Rank → sofortiger auto-repair + Discord + normaler Verdict-Flow |
| `same_request_relation_cache_invalidated_after_autorepair` (R11 KRITISCH) | Setup: Rank wurde mit `auto_approve_acars=true` in DB gesetzt, dann ein PIREP gefiled. Test prüft: nach Listener (mit Auto-Repair als ersten Schritt) liest `$pirep->user->rank->auto_approve_acars` in-memory `false`, UND der nachfolgende phpVMS-Submit-Code-Pfad triggert NICHT die Auto-Approve-Übersteuerung. End-State des PIREP = PENDING (Held), nicht ACCEPTED. |
| `autorepair_invalidates_laravel_cache_for_rank` (R11) | Mock Cache: AutoRepairService::repairForPirep() ruft `Cache::forget("ranks.X")` und `Cache::tags(['ranks'])->flush()` |
| `autorepair_throws_falls_into_emergency_fallback` (R13 KRITISCH) | Setup: AutoRepairService throw'd `\Throwable` (z.B. DB-Lock-Exception) → Listener catcht via try/catch + ruft `applyAutoRepairFailureFallback()` → PIREP landet in **REJECTED-Hold mit integrity_state='emergency_repair_failure'** (NICHT PENDING). Plus 🚨-Discord-Alarm gesendet. |
| `autorepair_verification_fails_falls_into_emergency_fallback` (R13) | Setup: AutoRepairService rennt durch, aber rank.auto_approve_acars ist nach load() IMMER NOCH true → `AutoRepairVerificationFailedException` aus try/catch → applyAutoRepairFailureFallback() → **REJECTED + integrity_state='emergency_repair_failure'**. |
| `listener_calls_autorepair_within_try_catch` (R13 audit) | Code-Audit: `$this->autoRepairService->repairForPirep($pirep)` ist im LE30-Listener-Code zwingend von `try { ... } catch (\Throwable $t) { applyAutoRepairFailureFallback(); return; }` umgeben. KEIN Aufruf außerhalb try/catch erlaubt. |
| `listener_calls_autorepair_before_any_state_manipulation` (R11 audit) | Code-Audit: erste Operation im PirepFiledGate::handle() ist `$this->autoRepairService->repairForPirep($pirep)`, KEIN vorheriger VPS-Call, KEIN vorheriger State-Set |
| `module_code_does_not_reference_applyconfigdriftfallback` (R13 audit) | Code-Audit: grep durch komplette Module-Codebase findet KEINEN Aufruf von `applyConfigDriftFallback()` mehr (Methode in R13 entfernt — siehe LE33). Der Audit fail'd wenn jemand die Methode wieder einführt — bewusst-Schutz gegen Wieder-Einführen des fail-open-Pfads. |
| `module_webhook_reason_required_for_dismissed_fp` (R11) | Webhook mit target_state=dismissed_fp ohne reason → 422 (Validation-Fail), kein State-Change |
| `module_webhook_reason_required_for_reopen` (R11) | Webhook mit target_state=reopen ohne reason → 422, kein State-Change |
| `module_listener_registered_on_pirep_filed_event` | phpVMS-Module Service-Provider registriert Listener korrekt |
| `module_listener_does_not_inject_pirep_service` (R9 audit) | Code-Audit: `PirepStateManipulator`-Constructor injected NICHT `PirepService` |
| `module_skips_already_final_pirep` | PIREP-State schon ACCEPTED/REJECTED → Listener tut nichts |
| `module_clean_verdict_sets_accepted_state_only` (R10) | VPS antwortet trust_level=trusted → Listener setzt $pirep->state=ACCEPTED+save. KEIN expliziter PirepService::accept-Call (phpVMS macht den Lifecycle-Aufruf). |
| `module_requires_review_sets_pending_held_no_lifecycle` (R10) | VPS antwortet requires_review=true → state=PENDING + integrity_state='held'. KEIN accept/reject-Call (Listener oder phpVMS), kein Side-Effect. |
| `module_admin_rejected_sets_rejected_state_only` (R10) | VPS antwortet admin_decision=rejected → Listener setzt state=REJECTED+save. phpVMS-Submit ruft danach reject() EINMAL. Kein Double-Trigger. |
| `module_admin_accepted_sets_accepted_state_only` (R10) | VPS antwortet admin_decision=accepted → Listener setzt state=ACCEPTED+save. phpVMS ruft accept() EINMAL. |
| `module_admin_dismissed_fp_sets_accepted_with_distinct_field` (R10) | VPS antwortet admin_decision=dismissed_fp → state=ACCEPTED + integrity_state='dismissed_as_false_positive' |
| `module_vps_timeout_sets_pending_held_via_state_only` (R10) | VPS Timeout 3s → state=PENDING + integrity_state='held_vps_timeout'. KEIN PirepService-Call. Kein Lifecycle-Side-Effect. |
| `module_vps_404_fail_closed_sets_pending_held` (R10) | VPS antwortet 404 → state=PENDING + integrity_state='held_no_telemetry'. KEIN pass-through-Bypass. |
| `module_vps_404_legacy_allowlist_sets_accepted_state_only` (R10) | Pilot in legacy_pilot_allowlist → 404 vom VPS → state=ACCEPTED + integrity_state='legacy_allowlisted' (phpVMS-Submit macht accept-Lifecycle) |
| `module_throwable_fail_closed_sets_pending_held` | Listener wirft jeden \Throwable → state=PENDING + integrity_state='held_internal_error' |
| `module_hmac_invalid_returns_401` | VPS rejected Call mit falscher HMAC-Signatur → 401 |
| `module_hmac_replay_returns_401` | Selber Nonce 2× verwendet → 401 (replay-Schutz) |
| `module_hmac_stale_timestamp_returns_401` | Timestamp > 5 min alt → 401 (TTL) |
| `module_webhook_decision_rejected_calls_pirepsvc_reject` (R10) | Webhook (off-flow) ruft PirepService::reject() explizit → Hours-Reverse + Notification |
| `module_webhook_decision_accepted_with_warning_calls_pirepsvc_accept` (R10) | Webhook (off-flow) ruft PirepService::accept() explizit → Hours+Pay+Stats korrekt |
| `module_webhook_decision_dismissed_fp_calls_accept_with_distinct_comment` (R10) | Webhook dismissed_fp → PirepService::accept() + distinct integrity_state |
| `module_webhook_decision_reopen_from_pending_keeps_pending` | reopen wenn vorher PENDING-held → bleibt PENDING, integrity_state='held' |
| `module_webhook_decision_reopen_from_rejected_to_pending` | reopen wenn vorher REJECTED → state=PENDING, integrity_state='held', kein Re-reject |
| `module_webhook_decision_reopen_from_accepted_returns_400` (R10) | reopen wenn vorher ACCEPTED → 400 mit error='reopen_not_allowed_from_accepted'. KEIN State-Change. |
| `module_webhook_invalid_hmac_401` | Webhook ohne valide HMAC → 401, kein State-Change |
| `module_webhook_unknown_pirep_id_404` | Webhook für nicht-existenten pirep_id → 404 |
| `module_webhook_reason_required_for_rejected_dismissed_reopen` (R10 LE21b-Konsistenz) | Webhook ohne reason bei rejected/dismissed_fp/reopen → 400 (Konsistenz mit LE21b-State-Machine) |
| `vps_integrity_check_endpoint_returns_verdict` | GET /api/integrity-check/<id> mit valider HMAC → JSON-Verdict |
| `vps_module_webhook_retries_on_5xx` | Module-Webhook 503 → Retry nach 30s, 2min, 10min, ... |
| `vps_module_webhook_failed_hard_after_max_retries` | Nach 6 Retries → status=failed_hard in Queue |
| `async_retry_worker_uses_pirep_service_off_flow` (R10) | AsyncVerdictRetryWorker läuft Stunden nach PirepFiled → ruft PirepService::accept/reject explizit (off-flow Kontext) |
| `async_retry_worker_skips_if_pirep_already_decided` (R10) | Worker findet PIREP nicht mehr PENDING (Admin hat schon entschieden) → no-op |
| `wzz2271_full_pipeline_held_then_admin_rejects` | E2E: WZZ2271-Flug komplett → Stream A/B/C flagged → Listener setzt state=PENDING+integrity_state='held' → Admin entscheidet rejected → Webhook ruft PirepService::reject() → phpVMS-State=REJECTED, Hours/Pay revert, Reject-Notification (genau einmal) |
| `legacy_v012_client_pirep_fail_closed_unless_allowlisted` | v0.12.x-Client ohne Telemetrie + nicht in Allowlist → state=PENDING + integrity_state='held_no_telemetry' |
| `bilingual_comments_de_en_present` | Comments im phpVMS sind je nach Pilot-Locale DE oder EN |
| `no_false_reject_notification_for_held_pirep` | Pilot bekommt KEINE Reject-Notification wenn integrity_state='held' (weil kein reject() läuft) |
| `proper_accept_notification_for_clean_pirep` | Pilot bekommt normale Accept-Notification wenn clean verdict + auto_accepted (über phpVMS-Submit-Lifecycle) |
| `no_double_lifecycle_trigger_on_listener_state_change` (R10 R9-Boundary-Audit) | Listener setzt state=ACCEPTED → exakt 1× accept() läuft (durch phpVMS-Submit-Flow), KEIN expliziter Listener-Aufruf zusätzlich |



---

## Cross-Surface-Matrix

Pflicht-Tabelle für die Spec: für **jeden** Flag-Typ wird festgehalten wo er detektiert wird, was Recorder/Client/Web damit machen, **und welche Admin-Actions der VA-Admin im Web-Monitor durchführen kann** (R1-Erweiterung).

**Admin-Action-Vokabular** (gültig für alle Flag-Typen die einen Review-Queue-Eintrag erzeugen):

| Action | Effekt |
|---|---|
| `acknowledge` | Flag verstanden, kein Effekt auf PIREP-Score oder Status. Markiert nur als „gesehen". |
| `accept_with_warning` | PIREP-Score bleibt, Flag bleibt sichtbar, PIREP zählt in der Bestenliste **mit Warn-Badge** (z. B. legit Sim-Crash → ehrlicher Pilot soll Score behalten). |
| `reject` | PIREP wird in Bestenliste/Statistik explizit ausgeschlossen (`review_decision='rejected'`). |
| `dismiss_as_false_positive` | Flag wird als FP markiert und aus Statistik-Aggregationen entfernt. PIREP geht zurück zu `trusted` wenn keine anderen Flags. |
| `attach_note` | Freitext-Notiz an Flag/PIREP (z. B. „Pilot hat per Discord Sim-Crash bestätigt"). |

| Flag-Typ | Detection | Recorder | Client | Web-Monitor | Admin-Actions |
|---|---|---|---|---|---|
| `POSITION_DELTA_EXCESSIVE` | Stream A | persist + quarantine | Toast bei CRITICAL | rotes Badge, Review-Queue | reject / acknowledge / dismiss_FP / attach_note |
| `ALTITUDE_DELTA_EXCESSIVE` | Stream A | persist + quarantine | Toast bei CRITICAL | rotes Badge, Review-Queue | reject / acknowledge / dismiss_FP / attach_note |
| `FUEL_RATE_IMPOSSIBLE` | Stream A | persist + quarantine | Toast bei CRITICAL | rotes Badge, Review-Queue | reject / accept_with_warning / dismiss_FP / attach_note |
| `WEIGHT_DISCONTINUITY` | Stream A | persist (CRITICAL/ANOMALY) | Toast | Badge + Review-Queue | reject / accept_with_warning / dismiss_FP |
| `GROUND_ELEVATION_MISMATCH` | Stream A | persist (CRITICAL/ANOMALY) | Toast bei CRITICAL | Badge + Review-Queue | accept_with_warning (Helipad/Bush-Strip) / reject / dismiss_FP |
| `AIR_TO_GROUND_TELEPORT` | Stream A | persist + quarantine | Toast | rotes Badge, Review-Queue | reject (Default) / accept_with_warning (Sim-Crash bestätigt) |
| `SIM_STATE_RESET_SIGNATURE` | Stream A | persist (CRITICAL) | Toast „Sim-Reload erkannt" | rotes Badge, Review-Queue | reject / accept_with_warning (Sim-Crash) / attach_note |
| `TELEMETRY_GAP_SHORT` | Stream B | persist (INFO) | — | Hover-Detail im Forensik | acknowledge / attach_note |
| `TELEMETRY_GAP_LONG` | Stream B | persist (ANOMALY) | Activity-Log | gelbes Badge im Forensik | accept_with_warning (häufig legit Pause) / attach_note |
| `TELEMETRY_GAP_CRITICAL` | Stream B | persist (CRITICAL) | Toast bei Resume-Detection | rotes Badge, Review-Queue | reject / accept_with_warning / attach_note |
| `GAP_EDGE_STATE_CONFLICT` | Stream B | persist + quarantine | Toast | rotes Badge, Review-Queue | reject / accept_with_warning |
| `FUEL_TELEMETRY_UNAVAILABLE` | Stream B | persist (ANOMALY base; eskaliert) | — | gelbes Badge | accept_with_warning (Sim-Adapter-Lücke OK) / dismiss_FP |
| `AIRCRAFT_IDENTITY_VARIANT_SHIFT` (R1) | Stream B | persist (INFO) | — | INFO-Eintrag im Forensik | acknowledge |
| `AIRCRAFT_IDENTITY_FAMILY_SHIFT` (R1) | Stream B | persist (ANOMALY) | Toast | Badge im Forensik | accept_with_warning / reject |
| `AIRCRAFT_IDENTITY_MANUFACTURER_SHIFT` (R2 herabgestuft) | Stream B | persist (ANOMALY base; eskaliert auf CRITICAL via Operational-Conflict, siehe LE12) | Toast bei effective CRITICAL | gelb/rot je nach effective_severity, Review-Queue bei CRITICAL | reject / accept_with_warning (z. B. Sim-Adapter-Restart am Gate) / attach_note |
| `AIRCRAFT_IDENTITY_SIZE_CLASS_SHIFT` (R2 herabgestuft) | Stream B | persist (ANOMALY base; eskaliert auf CRITICAL via Operational-Conflict) | Toast bei effective CRITICAL | gelb/rot je nach effective_severity, Review-Queue bei CRITICAL | reject (Default bei airborne) / accept_with_warning (Sim-Adapter-Bug am Boden) |
| `AIRCRAFT_TYPE_UNKNOWN` (R2 neu) | Stream B | persist (ANOMALY) | — | gelbes Badge | acknowledge / attach_note (ICAO in keiner Familie) |
| `AIRCRAFT_IDENTITY_NULL_DOMINANT` (R1) | Stream B | persist (ANOMALY) | — | gelbes Badge | accept_with_warning (Sim-Adapter-Bug) / attach_note |
| `AIRCRAFT_REGISTRATION_INCONSISTENT` (R1) | Stream B | persist (INFO) | — | INFO-Eintrag im Forensik | acknowledge |
| `PHASE_TRANSITION_REJECTED` (LE16b, R1) | FSM-Gating (Stream C) | persist (ANOMALY base; eskaliert auf CRITICAL bei späterem Arrived) | — | Badge im Forensik, Review-Queue wenn eskaliert | accept_with_warning / reject / attach_note |
| `POST_BLOCKS_ANOMALY` | Post-Freeze (Stream C) | in `post_flight_events`, NICHT in integrity_flags | — | separater Block im Forensik | acknowledge / attach_note |
| `SCORE_UNTRUSTED` (computed, LE15) | Score-Trust-Compute (Stream C) | `score_trust_level='untrusted'`, `landing_score_trusted=false` | rotes Trust-Badge im PIREP-View | Bestenliste filtert raus, roter Badge | reject (Bestenliste-Ausschluss) / accept_with_warning (Score sichtbar mit Note) |
| `RESUME_SEAMLESS` | Stream F | resume_history-Eintrag | 5-s-Banner mit Cancel | — (zu häufig für Surfacing) | — |
| `RESUME_WITH_DRIFT` | Stream F | resume_history + flag (INFO) | GUIDED-Modal | INFO-Badge im PIREP | acknowledge |
| `RESUME_AFTER_REPOSITION` | Stream F | resume_history + flag (ANOMALY) + requires_review=true | REPOSITION-Modal | gelbes Badge, Review-Queue mit Diff vor/nach | reject (Cheat-Verdacht) / accept_with_warning (Sim-Crash bestätigt) / attach_note |
| `RESUME_AFTER_CLEAN_SHUTDOWN` | Stream F | resume_history + flag (INFO) | Toleranzen-Bonus | INFO-Eintrag im Forensik | acknowledge |
| `RESUME_AFTER_CRASH` | Stream F | flag (INFO), Crash-Report → GlitchTip | — | INFO-Eintrag | acknowledge / attach_note |
| `RESUME_TIMEOUT_ABORTED` | Stream F | session beendet | ABORT-Modal | Eintrag im Forensik | — (Session schon weg) |

---

## Datenmodell / Wire-Format

### Erweiterte `flight_sessions`-Spalten

```sql
integrity_flags                       TEXT DEFAULT '[]'
clean_shutdown_reason                 TEXT
resume_history                        TEXT DEFAULT '[]'
-- R4: Ingestion-Sequencing
last_acknowledged_sequence_number     INTEGER DEFAULT 0
last_seen_sequence_number             INTEGER DEFAULT 0
last_seen_snapshot_ts                 INTEGER
-- R4: Live-Aggregation + Recompute
session_effective_severity            TEXT DEFAULT 'info'
last_recompute_at                     INTEGER
recompute_count                       INTEGER DEFAULT 0
status                                TEXT DEFAULT 'active'
                                      -- 'active' | 'completed' | 'partial' | 'aborted'
```

### Erweiterte `pireps`-Spalten

```sql
integrity_flags                       TEXT DEFAULT '[]'
score_trust_level                     TEXT DEFAULT 'trusted'
score_trust_reasons                   TEXT DEFAULT '[]'
landing_score_trusted                 INTEGER DEFAULT 1
requires_review                       INTEGER DEFAULT 0
post_flight_events                    TEXT DEFAULT '[]'
status                                TEXT DEFAULT 'completed'
                                      -- 'completed' | 'partial' | 'aborted'
review_state                          TEXT DEFAULT 'open'
                                      -- 'open' | 'acknowledged' | 'accepted_with_warning'
                                      -- | 'rejected' | 'dismissed_false_positive'
review_state_changed_at               INTEGER
review_state_changed_by               TEXT
review_decision                       TEXT     -- Backwards-Compat
review_notes                          TEXT
reviewed_at                           INTEGER
reviewed_by                           TEXT
```

### Neue Tabellen (R4 — für Ingestion + Recompute)

```sql
-- Replay-Cache für Idempotency-Key (LE C-Sub Ingestion-Sequencing)
CREATE TABLE ingestion_replay_cache (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id        TEXT NOT NULL,
    sequence_number   INTEGER NOT NULL,
    received_at       INTEGER NOT NULL,
    UNIQUE(session_id, sequence_number)
);
CREATE INDEX idx_ingestion_replay_session_seq
    ON ingestion_replay_cache(session_id, sequence_number);
-- LRU-Cleanup: behalte letzte 1.000 Einträge pro session_id

-- FSM-Checkpoints für Recompute-Replay (Live-Aggregation R3, alle 60 s)
CREATE TABLE flight_session_checkpoints (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id        TEXT NOT NULL,
    checkpoint_ts     INTEGER NOT NULL,
    sequence_number   INTEGER NOT NULL,
    fsm_phase         TEXT NOT NULL,
    flight_stats_json TEXT NOT NULL,    -- vollständiger FlightStats-Snapshot zur Rekonstruktion
    integrity_flags_snapshot TEXT NOT NULL,  -- alle Flags bis zu diesem Zeitpunkt
    UNIQUE(session_id, sequence_number)
);
CREATE INDEX idx_fsc_session_ts
    ON flight_session_checkpoints(session_id, checkpoint_ts DESC);

-- Recompute-Log (R3 — wer hat wann was re-validiert)
CREATE TABLE recompute_log (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id        TEXT NOT NULL,
    triggered_at      INTEGER NOT NULL,
    trigger_flag_type TEXT NOT NULL,
    trigger_flag_ts   INTEGER NOT NULL,
    segment_start_ts  INTEGER NOT NULL,
    segment_end_ts    INTEGER NOT NULL,
    checkpoint_used   INTEGER,          -- FK -> flight_session_checkpoints.id
    recompute_count   INTEGER NOT NULL, -- 1, 2, 3 — bei 4. Trigger: Backstop
    from_severity     TEXT,
    to_severity       TEXT,
    fsm_changes_json  TEXT,             -- Diff der Phase-Transitions
    scorer_changes_json TEXT,           -- Diff der Score-Werte
    FOREIGN KEY (checkpoint_used) REFERENCES flight_session_checkpoints(id)
);
CREATE INDEX idx_recompute_log_session
    ON recompute_log(session_id, triggered_at DESC);

-- Admin-Review-Audit (LE21b R3)
CREATE TABLE pirep_review_audit (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    pirep_id          INTEGER NOT NULL,
    from_state        TEXT,
    to_state          TEXT NOT NULL,
    actor             TEXT NOT NULL,
    ts                INTEGER NOT NULL,
    reason            TEXT,
    note              TEXT,
    FOREIGN KEY (pirep_id) REFERENCES pireps(id)
);
CREATE INDEX idx_pirep_review_audit_pirep
    ON pirep_review_audit(pirep_id, ts DESC);
```

### Snapshot-Schema-Erweiterung (Client → Recorder)

Jeder Position-Snapshot vom Client trägt ab v0.13.0 ein zusätzliches Pflichtfeld:

```typescript
interface PositionSnapshot {
  // ... existing fields ...
  
  // R4 NEU — Ingestion-Sequencing
  session_id: string;          // stabil über die ganze Session, Client-generiert (UUID)
  sequence_number: number;     // streng monoton steigend, Client-zählt ab 0
}
```

Recorder rejected Snapshots ohne `session_id` oder `sequence_number` mit 400. Client v0.12.x ohne diese Felder ist nicht mehr kompatibel — Migrations-Hinweis bei Login.

### Indizes (R4 Performance-Pflicht)

```sql
CREATE INDEX idx_pireps_review_state
    ON pireps(review_state, ts DESC);
CREATE INDEX idx_pireps_status_review
    ON pireps(status, review_state, ts DESC);
CREATE INDEX idx_pireps_requires_review_open
    ON pireps(requires_review, review_state)
    WHERE requires_review = 1;
CREATE INDEX idx_pireps_score_trust
    ON pireps(score_trust_level, ts DESC);
```

### Stream G Tabellen (R6 — phpVMS-Module-Gate)

R5-Tabellen `pilot_phpvms_keys`, `pirep_held_queue`, `pirep_filing_audit` sind **entfallen** — werden nicht mehr gebraucht (Begründung in Stream G G.2). Stattdessen nur:

```sql
-- Audit aller integrity-check-Lookups vom Module (LE32) + Webhook-Decisions (LE34)
CREATE TABLE module_gate_audit (
    id                       INTEGER PRIMARY KEY AUTOINCREMENT,
    phpvms_pirep_id          TEXT NOT NULL,
    direction                TEXT NOT NULL,         -- 'incoming_verdict_lookup' | 'outgoing_webhook'
    ts                       INTEGER NOT NULL,
    verdict_json             TEXT,                  -- Snapshot des gelieferten Verdicts (incoming)
    webhook_target_state     TEXT,                  -- Stream G LE34 Decision (outgoing)
    webhook_decided_by       TEXT,
    webhook_decision_reason  TEXT,
    response_status          INTEGER,
    response_error           TEXT,
    duration_ms              INTEGER
);
CREATE INDEX idx_module_gate_audit_pirep ON module_gate_audit(phpvms_pirep_id, ts DESC);

-- Retry-Queue für VPS → Module Webhook-Failures (LE36)
CREATE TABLE module_webhook_retry_queue (
    id                    INTEGER PRIMARY KEY AUTOINCREMENT,
    phpvms_pirep_id       TEXT NOT NULL,
    target_state          TEXT NOT NULL,           -- 'accepted_with_warning' | 'rejected' | 'dismissed_fp' | 'reopen' (R13: dismissed_fp ergänzt, konsistent mit LE34 Webhook-Body-Validation)
    decision_reason       TEXT,
    decided_by            TEXT NOT NULL,
    decided_at            INTEGER NOT NULL,
    attempts              INTEGER DEFAULT 0,
    next_attempt_at       INTEGER NOT NULL,
    last_error            TEXT,
    status                TEXT DEFAULT 'pending',   -- 'pending' | 'delivered' | 'failed_hard'
    UNIQUE(phpvms_pirep_id, decided_at)
);
CREATE INDEX idx_mwrq_next_attempt
    ON module_webhook_retry_queue(status, next_attempt_at)
    WHERE status = 'pending';
```

Die R6-Architektur nutzt **keine MQTT-Topics für PIREP-Filing**. Module ↔ VPS kommuniziert ausschließlich synchron via HTTPS (Module → VPS für Verdict-Lookup, VPS → Module für Decision-Webhook). Telemetrie-MQTT-Topics (Position/Phase/Touchdown) bleiben unverändert wie in v0.12.x.

### `integrity_flag`-Objekt-Schema (R3 mit Versions-Bindung)

```typescript
interface IntegrityFlag {
  type: string;                       // z. B. "FUEL_RATE_IMPOSSIBLE"
  base_severity: 'info' | 'anomaly' | 'critical';
  effective_severity: 'info' | 'anomaly' | 'critical';   // R2 — kann nach Aggregation eskaliert sein
  ts: number;                         // unix-ms
  phase: string;                      // z. B. "Descent"
  detail: Record<string, any>;
  user_message_de?: string;
  user_message_en?: string;
  
  // R3 — Versions-Bindung pro Befund
  ruleset_version: string;            // z. B. "v0.13.0-rc1" — Version der Schwellwerte/Regeln die diesen Flag erzeugt haben
  aircraft_families_version: number;  // z. B. 1 — Version der aircraft_families.json zum Zeitpunkt der Detection
  detector: string;                   // z. B. "stream_a_le3_continuous" oder "stream_b_le9_gap_edge" — welcher Code-Pfad
  detected_at: number;                // unix-ms — wann der Recorder den Flag erzeugt hat (vs. ts = wann das Ereignis im Flug war)
  
  // R2 — Mode-Tag
  mode?: 'continuous' | 'gap_edge';
  
  // R3 — Recompute-Tracking
  recompute_count?: number;           // wie oft wurde dieser Flag durch Recompute aktualisiert
  effective_severity_history?: Array<{ ts: number; severity: string; reason: string }>;
}
```

**Warum Versions-Bindung pro Flag (R3-Befund P2-3):** Wenn später die `aircraft_families.json` von Version 1 auf 2 aktualisiert wird (z. B. neue Hersteller-Gruppierung), wäre ohne Versions-Bindung die historische Interpretation eines Flags nicht mehr reproduzierbar. Mit `ruleset_version` + `aircraft_families_version` am Flag selbst:

- Backfill kann mit exakter Historie reproduziert werden
- Webapp-Forensik-View zeigt „Dieser Flag wurde mit Regelversion X und Aircraft-Families v1 erzeugt"
- Reopen einer alten Entscheidung lädt den Original-Kontext nach

### `resume_history`-Eintrag

```typescript
interface ResumeHistoryEntry {
  ts: number;
  tier: 'seamless' | 'guided' | 'reposition' | 'abort';
  gap_duration_s: number;
  clean_shutdown_reason: string | null;
  state_diff: {
    position_delta_m: number | null;
    altitude_delta_ft: number | null;
    fuel_delta_kg: number | null;
    air_ground_match: boolean | null;
    icao_match: boolean | null;
  };
  accepted_by_pilot: boolean;
  abort_reason?: string;
}
```

### MQTT-Topic-Erweiterung

```
Topic: aeroacars/<vaPrefix>/<pilotId>/integrity_event
Retained: false (transient)
Payload: IntegrityFlag (siehe oben) + flight_id-Feld
```

### API-Antwort-Erweiterungen

`GET /api/pireps/:id`-Response bekommt:

```json
{
  "...existing fields...": "...",
  "integrity_flags": [...],
  "score_trust_level": "trusted" | "review" | "untrusted",
  "score_trust_reasons": ["..."],
  "requires_review": false,
  "post_flight_events": [...],
  "review_decision": null | "accepted" | "rejected",
  "review_notes": null | "..."
}
```

---

## Aufwand-Schätzung (R3 — ehrlich nach oben revidiert)

**Scope-Klarstellung verbindlich (siehe oben):** v0.13.0 wird als zusammenhängendes Integritäts-Release umgesetzt; alle sechs Streams sind in scope für v0.13.0. Eine **interne Sequenzierung der Code-Slices** ist erlaubt und sinnvoll, ändert aber nicht den Release-Scope.

**R3-Befund P3:** R2-Schätzung von 122 h war zu sportlich gegeben die R3-Ergänzungen (Recompute-Mechanismus mit Checkpoints, per-Flight-Serialisierung mit Idempotency, Post-Resume-Status-Pill, Admin-Reopen, Ruleset-Versionierung pro Flag, erweiterte reproducibility_pin, Phase-Continuity-Erweiterungen mit Operating-State-Check, Statistik-Sperre für Partial-PIREPs, zusätzliche Negative-Controls). Ehrliche Neuschätzung:

### Gesamt-Aufwand für v0.13.0

| Stream | Code | QS | Σ | Δ vs R2 |
|---|---:|---:|---:|---|
| Stream A — Physik-Validator (Recorder + Client; Continuous + Envelope-Gap-Edge; Phase-Continuity mit Go-Around/Bounce + Operating-State-Check) | 20 h | 7 h | 27 h | +3 h (Operating-State, Bounce-Logic) |
| Stream B — Gap-Handler & Data-Quality (data-driven Aircraft-Families-JSON + UNKNOWN + Versions-Bindung pro Flag) | 12 h | 5 h | 17 h | +2 h (Versions-Bindung) |
| Stream C — Downstream-Schutz (Pipeline, Quarantäne, FSM-Gating, BlocksOn-Reject, Safe Termination + Statistik-Sperre, Live-Aggregation **mit Recompute + Checkpoints**) | 20 h | 8 h | 28 h | +8 h (Recompute-Mechanismus ist substanziell) |
| Stream C-Sub — Ingestion-Serialisierung & Idempotency (per-session Lock, Replay-Cache, Monotonic-Checks) | 6 h | 3 h | 9 h | NEU |
| Stream D — Persistence & Surfacing (DB-Migration inkl. `pirep_review_audit` + Checkpoint-Tabelle + Recompute-Log, API, MQTT inkl. `integrity_segment_recomputed`, Webapp Review-Queue + Admin-State-Machine **mit reopen**) | 19 h | 7 h | 26 h | +4 h (Reopen-UI, Audit-Diff-Anzeige) |
| Stream E — Backfill (Dry-Run + Apply mit erweiterter reproducibility_pin: pirep_ids + per-source-hashes) | 10 h | 4 h | 14 h | +3 h (per-source-Hashing aller Source-JSONLs) |
| Stream F — Resume-Policy (3-Tier-Klassifikation, SEAMLESS-Banner + **Post-Resume-Status-Pill**, GUIDED-Modal, REPOSITION-Modal, Clean-Shutdown-Detection Win+macOS) | 14 h | 5 h | 19 h | +3 h (Post-Resume-Pill mit Live-Update) |
| **Stream G — phpVMS-Integrity-Gate via Module** (R12 final): phpVMS-Module `AeroACARSIntegrityGate` (PHP, ~20 h inkl. Auto-Repair-Service + Listener-vs-Webhook-Lifecycle-Split + Cache-Invalidation + Auto-Repair-Failure-Emergency-Fallback), VPS-Endpoint `/api/integrity-check` (8 h), Module-Webhook mit PirepService-basierter Lifecycle (6 h), HMAC-Authentication beide Seiten + Nonce-Replay-Cache (4 h), Async-Retry-Worker mit PirepService off-flow (2 h), Legacy-Allowlist-Tabelle + UI (2 h). | 32 h | 12 h | 44 h | R12: +4 h ggü. R8 (Auto-Repair-Pattern + Listener-Boundary + Cache-Invalidation + Emergency-Fallback) |
| Negative-Control-Fixtures (7 synthetische JSONLs + CI-Integration) | 4 h | 2 h | 6 h | NEU |
| Spec-Pflege (R0 → R1 → ... → R12 → ACCEPTED, mit Code-QS-Runden) | — | — | ~20 h | +4 h (R7–R12-Runden) |
| Release-Pflege (Version-Bump, bilinguale Release-Notes DE/EN, Tag, GitHub-Action-Build, VPS-Deploy, phpVMS-Module-Deploy auf GSG) | — | — | ~5 h | +1 h (Module-Deploy) |
| **Gesamt v0.13.0** | **141 h** | **55 h** | **~216 h** | **+94 h ggü. R2 (R3 +40, R5/R6 +32, R7 +8, R8 +3, R10 +2, R11 +1, R12 +8)** |

Realistisch verteilt über **8–10 Wochen** mit Code-QS-Runden zwischen den Slices. Honest range: **200–235 h** abhängig von Code-QS-Findings und phpVMS-Module-Edge-Cases (DisposableBasic-Pattern ist nachweislich produktionsstabil, unser Module mit Auto-Repair-Pattern ist Standard-Pattern, HMAC-Implementation gegen phpVMS-Laravel-Stack braucht sorgfältige Code-QS).

### Empfohlene interne Slice-Reihenfolge (nicht-bindend)

Implementierungs-Reihenfolge die Risiko minimiert und schnelles Feedback erlaubt — aber alle Slices sind Teil desselben v0.13.0-Releases, kein einzelner geht ohne die anderen live:

| Slice | Inhalt | Output für interne Validierung |
|---|---|---|
| Slice 1 | DB-Migration (LE17) + minimaler Stream-A-Validator (LE1, LE3) + Persistence in `integrity_flags`-Spalte | erste Flags landen in DB, sichtbar via SQL |
| Slice 2 | Stream A komplett (LE2, LE4–LE7) + Stream B komplett (LE8–LE12) + Live-Aggregation | Flag-Detection vollständig, aber nur backend-sichtbar |
| Slice 3 | Stream C (Pipeline + LE13–LE16c) | FSM- und Scorer-Schutz aktiv |
| Slice 4 | Stream D Surfacing (LE18–LE21b) — Webapp-Badges, Review-Queue, Admin-Audit-State-Machine | komplette Web-Sichtbarkeit für VA-Admin |
| Slice 5 | Stream E Backfill (Dry-Run + Apply) | historischer Audit möglich |
| Slice 6 | Stream F Client-Resume-Policy (LE22–LE26) + Client-Side-Validator-Replikation | volle Pilot-UX |
| Release | E2E-Tests inkl. WZZ2271-Master + Negative Controls + bilinguale Release-Notes + Tag + Deploy | v0.13.0 produktiv |

Während Slice 1–5 ist v0.13.0 nicht release-fähig — alle sechs Streams müssen umgesetzt sein bevor das Release-Tag fällt.

---

## Migration & Rollout

1. **Migration v0.13.0** läuft in einem Schritt; alle ALTER TABLE sind idempotent (DEFAULT-Werte verhindern Datenkorruption).
2. **Bestehende PIREPs** bleiben unverändert (`score_trust_level = 'trusted'` per Default).
3. **Client-Update** verteilt sich über die normale Auto-Update-Logik. Pilot kann während Übergangsperiode mit altem oder neuem Client fliegen — Recorder verarbeitet beide.
4. **Webapp-Review-Queue-Tab** ist sofort sichtbar nach VPS-Deploy, ist initial leer (per A3 kein Backfill), füllt sich mit neuen Flügen.
5. **Roll-Forward-Strategie:** v0.13.0 ist additiv. Wenn ein kritischer Bug auftaucht, kann Validator über Recorder-Feature-Flag deaktiviert werden (zukünftiger Stream G, nicht in v0.13.0).
6. **Roll-Back:** DB-Migration ist non-destructive — Spalten ignorieren ist OK, Migration zurücknehmen unkompliziert (DROP COLUMN für SQLite via Table-Rebuild — nur bei Notfall).

---

## Offene QS-Fragen (R4 — bereinigt, alle in scope für v0.13.0)

**Sammelübersicht für Thomas:**

| ID | Punkt | Default-Vorschlag | Status |
|---|---|---|---|
| Q1 | Stream A LE1 Cruise-Schwelle: 5 NM in 5 s ausreichend? Concorde-Edge-Case oder eher zu locker? | 5 NM / 5 s = 3.600 kt — ausreichend für jedes ziviles Flugzeug | offen |
| Q2 | Stream A LE3 Fuel-Rate: nur A321-Schwellen für v0.13.0 oder weitere Familien (A330/A350) sofort mitnehmen? | Default A321 für v0.13.0 reicht für GSG-Fleet. Weitere Familien wenn relevant in folgendem Patch-Release (kein Phase-Split, sondern reine Schwellwert-Tuning-Frage). | offen |
| Q3 | Stream A LE5 Ground-Elevation: Toleranz für Hubschrauber-Helipads / Off-Airport-Landungen (z. B. Buschpilot in Alaska)? | Skip-on-Unknown-Airport macht das automatisch sicher — Edge-Case ist abgedeckt | offen |
| Q4 | Stream B LE8 Gap-Schwellen: 300 s als CRITICAL-Grenze realistisch? Pilot pausiert legal mal länger als 5 min am Gate. | Phase-Awareness in LE8: Schwellen verdoppeln in Boarding/Pushback + skip wenn `simulator.paused == true` | offen |
| Q5 | Stream C LE15 Score-Trust: ist `groundspeed_kt < 30` beim Touchdown die richtige untrusted-Bedingung, oder zu hart (Pilatus PC-12 landet mit ~50 kt)? | < 30 kt ist conservative für GSG-Fleet (Airbus/Boeing). PC-12 ist Edge-Case — kann separat als Aircraft-Type-spezifische Override in `aircraft_families.json` ergänzt werden, in derselben v0.13.0. | offen |
| Q6 | Stream D LE17 Migration: SQLite ALTER TABLE ADD COLUMN ist idempotent — aber sicher gegen DB-Lock während aktiver Flüge? | Migration läuft beim Recorder-Start vor DB-Open für andere Connections | offen |
| Q7 | Stream D LE19 MQTT-Topic: pro-Pilot oder per-VA? Pilot subscribt nur sein eigenes Subtopic — VA-Admin braucht aggregated topic? | Per-Pilot reicht; VA-Admin nutzt Web-Monitor Push-Updates statt MQTT direkt | offen |
| Q8 | Stream F LE22 Windows-WM_QUERYENDSESSION: garantiert Tauri einen Flush-to-Disk in der Zeit zwischen QUERY und tatsächlichem Shutdown? | Tauri muss TRUE zurückgeben und hat dann normalerweise ~30 s — sollte reichen | offen |
| Q9 | Stream F LE24 Clean-Shutdown-Bonus × 6: realistisch oder zu großzügig? Windows-Update braucht in Realität meist 5–30 min, selten > 1 h. | 6× = bis 3 h für GUIDED ist Komfort-Marge, kann auf × 3 reduziert werden | offen |
| Q10 | Stream F LE25 REPOSITION-Modal: zeigt Ziel-Position absolut (lat/lon), aber kein Sim-Compass-Direction-Hint. Sollte „45 NM NE von EHEH" o. Ä. dargestellt werden? | Wäre Komfort-Feature — Vorschlag: in v0.13.0 mitnehmen (geringer Mehraufwand, viel UX-Gewinn) | offen |

**Erwartete Reviewer-Aktion vor ACCEPTED:** Q1–Q10 entscheiden, Schwellwerte ggf. tunen. Alle Antworten sind in v0.13.0-Scope — kein Q-Punkt verschiebt sich auf ein späteres Release.

---

## Referenz-Akzeptanz: WZZ2271 (R1)

Die R1-Spec ist erst **ACCEPTED**, wenn ein E2E-Test gegen den WZZ2271-Rohflug (`flight-7epBoG5lANKgZ16d.jsonl.gz`) folgende Ergebnisse produziert:

### Pflicht-Erwartungen

| # | Erwartung | Pflicht |
|---|---|---|
| 1 | PIREP `requires_review = true` | ✓ |
| 2 | PIREP `score_trust_level = "untrusted"` | ✓ |
| 3 | PIREP `landing_score_trusted = false` (Alias) | ✓ |
| 4 | `integrity_flags[]` enthält mindestens: `TELEMETRY_GAP_CRITICAL` (4×), `GAP_EDGE_STATE_CONFLICT` (≥3×), `FUEL_RATE_IMPOSSIBLE` (≥4×), `WEIGHT_DISCONTINUITY` (≥2×), `SIM_STATE_RESET_SIGNATURE` (≥4×), `AIR_TO_GROUND_TELEPORT` (1×), `GROUND_ELEVATION_MISMATCH` (≥1×), `FUEL_TELEMETRY_UNAVAILABLE` (1×), `PHASE_TRANSITION_REJECTED` (≥1×), `POST_BLOCKS_ANOMALY` (≥1×) | ✓ |
| 5 | `AIRCRAFT_IDENTITY_VARIANT_SHIFT` (INFO, base) eskaliert zu ANOMALY (effective) durch Aggregation mit `WEIGHT_DISCONTINUITY` | ✓ |
| 6 | `FUEL_TELEMETRY_UNAVAILABLE` (ANOMALY, base) eskaliert zu CRITICAL (effective) durch Aggregation mit `FUEL_RATE_IMPOSSIBLE` | ✓ |
| 7 | Kein quarantänisierter Snapshot hat eine Phase-Transition zu {Approach, Final, Landing, TaxiIn, BlocksOn, Arrived} ausgelöst (= LE16b funktioniert) | ✓ |
| 8 | Touchdown-Detection ist nicht gefeuert ODER gefeuert + sofort als untrusted markiert (LE15 Hard-Triggers) | ✓ |
| 9 | Landing-Score sichtbar mit rotem "DATA INTEGRITY: REVIEW"-Badge in Webapp | ✓ |
| 10 | Eintrag in Review-Queue mit allen effective-CRITICAL-Flags und Diff vor/nach Teleport | ✓ |
| 11 | WZZ2271-PIREP wird NICHT in der Greaser-Bestenliste angezeigt (Default-Filter `score_trust_level = 'trusted'`) | ✓ |
| 12 | Aggregations-Pass produziert mindestens 3 distinkte ANOMALY-Typen → `requires_review = true` auch ohne CRITICAL nötig (Combo-Regel) | ✓ |

### Wenn ein Punkt fehlt

Spec ist **NICHT** ACCEPTED. Weitere R-Runde nötig. Forensik-Test `wzz2271_full_replay_produces_expected_flags` ist der Master-Test der die ganze Akzeptanz fest verbacken hat — wenn der grün ist, sind 1–12 erfüllt.

### Negative Controls (R3 präzisiert — Pflicht)

Master-Test allein ist nicht genug — er kann nur sicherstellen dass WZZ2271 korrekt geflagt wird, nicht dass legitime Flüge nicht **review-impacting** geflagt werden. Ohne Negative-Controls droht Overfitting auf den einen bekannten Fall.

**Definition „review-impacting" (R3 normativ):** Ein Flag/Effekt ist review-impacting genau dann wenn mindestens eines zutrifft:

- `effective_severity == "critical"` (ein einzelner CRITICAL reicht)
- PIREP-Resultat `requires_review == true`
- PIREP `score_trust_level != "trusted"` (also `review` oder `untrusted`)
- Ein Snapshot wurde `quarantined` (FSM/Scorer hat ihn ignoriert)
- PIREP-Status `partial` (LE16c Safe-Termination)

**Nicht review-impacting (= erlaubt in Negative Controls):** `base_severity` INFO oder ANOMALY ohne Combo-Eskalation; reine Activity-Log-Einträge; informative Flags wie `RESUME_AFTER_CLEAN_SHUTDOWN`, `TELEMETRY_GAP_SHORT`, `AIRCRAFT_IDENTITY_VARIANT_SHIFT`.

**Pflicht-Gegenproben (mindestens):**

| # | Test | Erwartung (R3 präzisiert) |
|---|---|---|
| NC1 | `legit_app_restart_long_pause_no_review_impact` — synthetisches Fixture: 25 min Pause mit `clean_shutdown_reason = "update"`, Sim-State beim Resume identisch (Position, Altitude, Fuel im Burn-Range, gleiches Aircraft, gleiche Phase) | **Keine review-impacting flags.** `TELEMETRY_GAP_LONG` (ANOMALY base, kein Combo) + `RESUME_AFTER_CLEAN_SHUTDOWN` (INFO) DÜRFEN gesetzt sein — beide ohne CRITICAL-Eskalation. PIREP `score_trust_level = 'trusted'`, `requires_review = false`. Keine Quarantäne. |
| NC2 | `legit_boarding_refuel_no_review_impact` — Fuel steigt während Boarding-Phase um +6.000 kg in 8 min (typisches Pre-Flight-Refueling), Weight steigt entsprechend, Aircraft stationär | Keine `FUEL_RATE_IMPOSSIBLE`, keine `WEIGHT_DISCONTINUITY`, keine `FUEL_OUTSIDE_BURN_RANGE_HIGH`. **Keine review-impacting flags.** |
| NC3 | `legit_pause_at_gate_long_no_review_impact` — Pilot pausiert Sim für 45 min am Gate in Boarding-Phase, `simulator.paused == true` über die ganze Lücke | `TELEMETRY_GAP_LONG` NICHT geflagt (LE8-Filter: skip wenn `simulator.paused == true` zum Gap-Beginn). **Keine review-impacting flags.** |
| NC4 | `legit_step_climb_no_review_impact` — Cruise bei FL340, Step-Climb auf FL380 in 6 min (3.000 fpm gemittelt) | Keine `ALTITUDE_OUTSIDE_VERTICAL_ENVELOPE` (envelope erlaubt bis 6.000 fpm). **Keine review-impacting flags.** |
| NC5 | `legit_short_app_crash_no_review_impact` — App crashed nach 90 s, Pilot startet neu, kein Sim-State-Drift | Resume-Tier = SEAMLESS. `RESUME_AFTER_CRASH` (INFO) DARF gesetzt sein. **Keine review-impacting flags.** |
| NC6 | `legit_go_around_at_500ft_no_review_impact` (R3 neu) — Pilot fliegt Final, bricht bei 500 ft AGL ab, geht in Climb mit voller Schubleistung, macht 2. Anflug | Phase-Transition Final → Climb erlaubt (Operating-State-Check passt: engines on, brake off, GS hoch). Keine `PHASE_CONTINUITY_VIOLATION`. **Keine review-impacting flags.** |
| NC7 | `legit_bounce_touch_and_go_no_review_impact` (R3 neu) — Pilot setzt auf, bouncet kurz airborne, setzt erneut auf | Landing → TakeoffRoll → Landing erlaubt (Operating-State-Check OK). **Keine review-impacting flags.** |

**Wenn eine Gegenprobe ein review-impacting flag erzeugt:** Spec ist NICHT ACCEPTED, Schwellwerte sind falsch tuned, R-Runde nötig zur Re-Kalibrierung.

**Test-Implementation:** synthetische JSONL-Fixtures unter `client/src-tauri/crates/recorder-tests/fixtures/negative_controls/`. Sind Teil der CI-Test-Suite, blockieren Release wenn fail.

**Zukünftige Erweiterungen:** weitere Negative-Controls aus realen GSG-Pilot-Flügen sammeln (sobald v0.13.0 läuft), in einer Test-Fixture-Bibliothek pflegen.

---

## Status

**SPEC R13.** Wartet auf QS-Runde 14 oder direkt ACCEPTED durch Thomas. **Sieben Streams (A–G)** + Sub-Stream C-Sub. **39+ LEs**, 145+ Tests (R13 +3 für try/catch-Audit + Exception-Path + applyConfigDriftFallback-Removal-Audit). R13 hat den letzten Exception-Path-Bypass im Auto-Repair geschlossen und alle stale Texte sowie die unsichere `applyConfigDriftFallback()`-Methode entfernt. Aufwand-Schätzung **~216 h gesamt (range 200–235 h)**.

**R13-Hauptänderungen ggü. R12** (alle 5 QS-Findings, kein Pivot):
- **P1 — try/catch um repairForPirep() in LE30:** R12 rief AutoRepair ungeschützt. Bei Exception (DB-Lock, Cache-Plugin-Crash) lief Code nie bis zum Re-Check oder Emergency-Fallback. R13: Listener wrappt repair+verify in try/catch — sowohl `AutoRepairVerificationFailedException` als auch jede Throwable landet im selben REJECTED-Emergency-Fallback.
- **P2 — `applyConfigDriftFallback()` hart entfernt:** war PENDING-Hold = unsicher bei Config-Drift. R13 entfernt die Methode aus LE33-Code-Block + neuer Audit-Test verhindert Wieder-Einführung.
- **P2 — Retry-Queue-Schema-Kommentar `dismissed_fp` ergänzt:** war inkonsistent mit Webhook-Body-Validation.
- **P2 — Historische v0.13.1/Backlog-Texte sanitisiert:** R3-Phase-Split und R9-Reverse-Pipeline umformuliert auf „verworfen, kein Folge-Release vorgesehen".
- **P3 — Header „sechs Streams" → „sieben Streams":** Trigger-Block-Kosmetik aus R0-Zeit korrigiert.
- **Aufwand:** +0 h netto (try/catch trivial, Code-Removal spart, Audit-Test minimal). Gesamt bleibt **~216 h (range 200–235 h)**.

**R12-Hauptänderungen ggü. R11** (alle 5 QS-Findings, kein Pivot):
- **P1 — Auto-Repair-Failure-Fallback fail-closed mit REJECTED-Emergency-Hold:** R11 setzte PENDING bei Failure, was via Auto-Approve zu ACCEPTED gedreht worden wäre. R12: neue `applyAutoRepairFailureFallback()` setzt REJECTED + `integrity_state='emergency_repair_failure'`. Pilot bekommt Reject-Notification mit klarem Erklär-Text, VA-Owner bekommt 🚨-Discord-Alarm. Einziger fail-closed Pfad für „should never happen"-Case.
- **P2 — `dismissed_fp → open` aus LE21b entfernt:** Konsistenz-Fix mit LE34 (das jeden phpVMS-State=ACCEPTED hart blockt). dismissed_fp setzt state=ACCEPTED via accept() → kann nicht via reopen rückgängig gemacht werden.
- **P2 — Architektur-Diagramm + Webhook-Summary auf R12:** R8-stale-Texte „Listener: pirepSvc->accept" und „Webhook: setzt $pirep->state direkt" durch R11/R12-korrekten Listener-Schritt-Pfad + Webhook-PirepService-off-flow ersetzt.
- **P2 — Hard-Cut-Text PENDING-Hold:** R7-Reste „REJECTED-Hold" in der v0.12.x-Hard-Cut-Sektion auf `held_no_telemetry`/PENDING korrigiert.
- **P3 — Aufwand-Tabelle aktualisiert:** Stream-G-Zeile auf R12-Stand mit Auto-Repair-Pattern + Listener-Boundary + Cache-Invalidation + Emergency-Fallback. +4 h ggü. R8-Schätzung der nicht reflektiert war. Gesamt ehrlich ~216 h.

**R11-Hauptänderungen ggü. R10** (alle 4 QS-Findings, kein Pivot):
- **P1 — Auto-Repair als erster Listener-Schritt normativ in LE30 verankert:** R10 beschrieb Per-PIREP-Defense-in-Depth in G.2.0, der LE30-Code hatte es aber nicht. Same-Request-Cache-Bypass war möglich: Listener setzt PENDING → phpVMS-Submit liest cached `rank.auto_approve_acars=true` aus Eloquent-Relation → ACCEPTED. **R11:** LE30 ruft `AutoRepairService::repairForPirep($pirep)` als allererstes auf. Service macht DB-Update + Laravel-Cache-Invalidation + `setRelation('rank', $rank->fresh())` + `$pirep->load('user.rank')`. Defensive Re-Check + Emergency-Hold falls Repair ausnahmsweise fehlschlägt.
- **P2 — Webhook `decision_reason` REQUIRED für `dismissed_fp` + `reopen`:** R10's `required_if:rejected` widersprach LE21b. R11: `required_if:rejected,dismissed_fp,reopen`.
- **P2 — Stale R7/R9-Texte in Security-Sektion bereinigt:** 4 Threat-Zeilen umformuliert auf R10/R11-Auto-Repair-Pattern + PENDING-Hold (statt REJECTED-Hold + Emergency-Mode). Neue Zeile für „Same-Request-Cache-Bypass" mit R11-Mitigation.
- **P2 — Backlog-Reste entfernt:** A5 (Dump-Aircraft) und Stream-A-LE9 (Aircraft-Awareness) statt „im Backlog" jetzt explizit „nicht Teil dieser Produktanforderung". R9-Block-Stelle mit „Reverse-Pipeline für v0.13.1" als historische R9-Aussage markiert (in R10 verworfen).
- **Aufwand:** +1 h netto (AutoRepairService-Definition + 6 neue Tests). Gesamt **~208 h (range 196–226 h)**.

**R10-Hauptänderungen ggü. R9** (alle 6 QS-Findings adressiert, kein Pivot):
- **P1 — Emergency-Mode → Auto-Repair-Pattern:** R9's „PENDING-Hold bei Config-Drift" war Logikbug (PENDING wird bei `auto_approve=true` wieder ACCEPTED). R10: Module repariert die Config selbst (Boot + Daily-Cron + Per-PIREP-Defense-in-Depth). Drift wird in Sekunden korrigiert, vollständig protokolliert, sichtbar via Discord+Admin-Banner.
- **P1 — Async-Retry-Worker nutzt PirepService:** R9-Boundary erweitert auf Worker. Worker läuft off-flow (Stunden nach PirepFiled), gleiche Regel wie Webhook: PirepService::accept()/->reject() explizit.
- **P1 — Kein Auslagern nach v0.13.1:** reopen-from-ACCEPTED dauerhaft entfernt (kein Backlog-Eintrag, kein v0.13.1-Verweis). VA-Admin nutzt für Reverse-Cases das phpVMS-Admin-Panel direkt. LE21b State-Machine + LE34 Webhook konsistent: reopen nur von REJECTED/dismissed_fp.
- **P2 — Test-Matrix R10-aktualisiert:** alle 35+ Stream-G-Tests auf R10-Semantik. Neue Tests für Auto-Repair (Boot/Cron/Per-PIREP), no_double_lifecycle_trigger, async_retry_worker_uses_pirep_service_off_flow, reopen-from-ACCEPTED-returns-400. Entfernt: R8/R9-Tests für Emergency-Mode, Listener-PirepService-Calls, reopen-from-ACCEPTED-revert.
- **P2 — reopen global konsistent:** LE21b und LE34 sagen jetzt dasselbe. reason konsistent REQUIRED für rejected/dismissed_fp/reopen.
- **P2 — Stale Status/Changelog-Texte bereinigt:** R8-Hauptänderungen-Block-Texte korrigiert (sagten teils noch „Listener nutzt PirepService"). Pivot-Historie zeigt R5→R6→R7→R8→R9→R10 mit „warum überholt".
- **Aufwand:** +2 h netto (−2 h Emergency-Mode entfällt, +3 h Auto-Repair-Service, +1 h Worker-Refactor). Gesamt v0.13.0 **~207 h (range 195–225 h)**.

**R9-Hauptänderungen ggü. R8** (alle 4 QS-Findings adressiert, kein Pivot mehr):
- **P1 — Listener-vs-Webhook-Lifecycle-Split verbindlich:** R8's Listener rief `PirepService::accept()/->reject()` direkt → Double-Trigger (phpVMS' Submit-Flow rief sie nochmal). R9 macht die Boundary normativ: Listener setzt NUR State+save, phpVMS macht Lifecycle einmal. Webhook ruft PirepService explizit (kein Submit-Flow drumherum). Code-Audit-Regel: PirepStateManipulator injected nicht mehr PirepService.
- **P1 — Misconfig fail-closed statt fail-open:** R8's „Module deaktiviert sich" war Bypass-Vektor (PIREPs gingen auf phpVMS-Default = Auto-Approve). R9 wechselt bei Config-Drift in Emergency-Mode (alle PIREPs → PENDING-held), 5 parallele Sichtbarkeits-Kanäle. Keine stille Bypass-Lücke mehr.
- **P2 — Reopen-from-ACCEPTED in v0.13.0 verboten:** Webhook returns 400. *(R9-Aussage „Reverse-Pipeline kommt in v0.13.1" wurde in R10 verworfen — dauerhaft nicht supported, kein späteres Release.)*
- **P2 — Stale R7-REJECTED-Hold-Texte bereinigt:** LE30 Fail-Closed-Policy, G.4 Security-Tabelle, Changelog-Historie-Einträge alle auf R9-PENDING-Hold gebracht.
- **Aufwand:** ±0 h netto (Code-Aufwand für Listener-Refactor wird durch eingesparten PirepService-Inject + reopen-from-ACCEPTED-Removal kompensiert).

**R8-Hauptänderungen ggü. R7** (architektonischer Pivot — Historie, R8-Annahmen wurden in R9/R10 weiter präzisiert):
- **P1 — REJECTED-als-Hold-Architektur verworfen.** R7's Hold-State via REJECTED triggerte phpVMS' `reject()` mit allen Side-Effects (Hours-Reverse, Pay-Reverse, falsche Reject-Mail). R8 pivotiert auf **PENDING-als-Hold mit Voraussetzung `auto_approve_acars=FALSE` auf allen Ranks**. Mit deaktiviertem Auto-Approve hält PENDING tatsächlich, kein Lifecycle-Method läuft für Hold-Cases. *(R10-Anmerkung: Voraussetzung wird jetzt durch Auto-Repair-Pattern garantiert, nicht durch manuelle VA-Owner-Config.)*
- **P1 — PirepService::accept()/reject() statt direkt-State-Flip:** R8 ließ Module für ALLE Lifecycle-Wechsel die `PirepService`-Methoden nutzen. *(R9-Korrektur: das war zu pauschal — im Listener-Kontext erzeugt das Double-Trigger durch phpVMS-Submit-Flow. R9 zog die Listener-vs-Webhook-Boundary. R10 erweiterte die Boundary auf den Async-Retry-Worker (off-flow → PirepService OK).)*
- **P1 — Webhook reopen-Semantik klar:** reopen-from-PENDING/REJECTED/ACCEPTED jeweils sauberer Pfad. reopen-from-ACCEPTED nutzt reject() für Stats-Revert, dann PENDING-Set. Akzeptanz-Beschränkung dokumentiert.
- **P2 — Notification-Suppression-Listener entfällt komplett.** Begründung: keine ungewollten reject()-Aufrufe mehr → keine false Reject-Mails → kein Suppression-Mechanismus nötig. Eine ganze problematische Mechanik weg.
- **P2 — Config-Verify-Service + Daily-Health-Check:** Module-Boot verifiziert `auto_approve_acars=false` auf allen Ranks. Bei Misconfig: Module deaktiviert sich + VA-Admin-Notification. Daily-Cron erkennt Drift (z. B. neue Ranks).
- **P2 — Stream G Tests komplett umgeschrieben:** 30 Tests jetzt, alle auf R8-PENDING-Semantik. Stale R6/R7-Tests entfernt.
- **P2 — G.2 Architektur-Diagramm korrigiert:** zeigt jetzt durchgängig PENDING-Flow + PirepService-Calls statt direkt-State-Manipulation.
- **P2 — Pivot-Historie-Tabelle in G.2.0:** dokumentiert alle vier Versuche (R5/R6/R7/R8) und warum die ersten drei verworfen wurden — für Nachvollziehbarkeit in zukünftiger QS.
- **Aufwand:** R7 ~202 h → R8 ~205 h (+3 h netto: −2 h Suppression-Listener entfällt, +3 h Config-Verify+Health-Check, +1 h Webhook-Reverse-Pfade, +1 h Test-Refactor).

**R7-Hauptänderungen ggü. R6** (alle 7 QS-Findings adressiert):
- **P1 — PENDING-Auto-Approve-Bypass behoben:** Hold-State ist jetzt `PirepState::REJECTED` (überlebt phpVMS' `submit()`-Post-Event-Code). Hold vs. Final-Reject distinct via PirepFieldValue `aeroacars_integrity_state` (Werte: `held`, `rejected_final`, `held_vps_timeout`, `held_internal_error`, `held_no_telemetry`) + Comment-Pattern. Pilot-Notification-Suppression via dedizierten Listener.
- **P1 — 404-Bypass behoben:** 404 vom VPS → SEPARATE `VpsNoTelemetryException` → fail-closed REJECTED-Hold. `legacy_pilot_allowlist`-Tabelle für Notfall-Override durch VA-Admin.
- **P1 — Throwable fail-closed:** Listener catcht `\Throwable` (nicht nur `\Exception`, schließt PHP-Errors ein), fail-closed REJECTED-Hold mit `integrity_state='held_internal_error'`. Kein PIREP geht clean durch ohne Verdict.
- **P2 — Sync-Timeout reduziert:** 1× synchroner Versuch mit 3s-Timeout (statt 3× × 5s + Backoff = 16,5s). Bei Fail → fail-closed Hold + Async-Retry-Worker (30s, 2min, 10min, 1h, 4h, 24h Schedule).
- **P2 — HMAC-Authentication:** statt Plain-Shared-Secret jetzt HMAC-SHA256 über `body+timestamp+nonce`, 5min-TTL, Nonce-Replay-Cache. Standard-Webhook-Security-Pattern. Neue LE31a.
- **P2 — v0.12.x-Hard-Cut:** R6's „funktionieren automatisch mit"-Aussage widersprach dem Datenmodell. R7 spezifiziert Hard-Cut auf v0.13.0+ mit 4-Wochen-Migrations-Plan. Legacy-Allowlist als Notfall-Override.
- **P2 — `dismissed_fp` separater Pfad:** eigener Code-Pfad, eigenes Comment-Template, separater Audit-Eintrag. Keine Vermengung mehr mit `accepted_with_warning`.

**R6-Hauptänderungen ggü. R5** (Stream G fundamental pivotiert):
- **Stream G komplett umgeschrieben:** statt VPS-Filing-Architektur jetzt **phpVMS-Module `AeroACARSIntegrityGate`** im DisposableBasic-Pattern (https://github.com/FatihKoz/DisposableBasic). Module-Listener auf `PirepFiled`-Event manipuliert `$pirep->state` direkt — umgeht `checkReadOnly()` (das war nur ein API-Gate).
- **3 phpVMS-Core-Fakten verifiziert** im phpvms/dev-Source: `PirepFiled` feuert BEFORE auto_approve_acars; PirepState-Enum kennt PENDING=1, ACCEPTED=2, REJECTED=6; Event-Payload `public Pirep $pirep`.
- **Drastisch vereinfacht:**
  - Pilot-Keys bleiben am Client (kein VPS-side Encrypted-Storage)
  - Client-Refactor entfällt (filed weiter direkt an phpVMS)
  - Migration-Pfad LE37 entfällt (alte v0.12.x-Clients funktionieren automatisch mit)
  - 2 MQTT-Topics weg (`pirep_draft`, `pirep_status` nicht mehr nötig)
  - 3 DB-Tabellen weg (pilot_phpvms_keys, pirep_held_queue, pirep_filing_audit)
  - 2 neue minimale DB-Tabellen (`module_gate_audit`, `module_webhook_retry_queue`)
- **Sicherheits-Surface eine Größenordnung kleiner:** statt aller Pilot-Keys auf VPS nur noch ein Shared-Secret zwischen Module und VPS.
- **Aufwand-Schätzung leicht reduziert:** R5 ~202 h → R6 ~194 h (Stream G ~38 h → ~30 h; aber zusätzliche Module-Deploy-Aufwand und phpVMS-Wartung).

**R4-Hauptänderungen ggü. R3** (Reaktion auf GitHub-QS mit Zeilenreferenzen):
- **Aggregations-Widerspruch beseitigt:** alte Lifecycle-Stelle (`einmal pro PIREP beim Finalize-Pass`) und C.1-Pipeline-Step-12 hatten R3's Live-Aggregation widersprochen. Beides konsolidiert: Aggregation läuft live in Pipeline-Step 6, vor FSM/Scorer (Step 10–12); Finalize-Pass (Step 13) macht nur noch Konsolidierung, keine erste Severity-Berechnung.
- **Datenmodell für Ingestion + Recompute komplett:** neue Tabellen `ingestion_replay_cache`, `flight_session_checkpoints`, `recompute_log`, `pirep_review_audit`. Neue Spalten `last_acknowledged_sequence_number`, `last_seen_sequence_number`, `session_effective_severity`, `last_recompute_at`, `recompute_count`, `status`. Snapshot-Schema mit `session_id` + `sequence_number` als Pflichtfelder. Performance-Indizes ergänzt.
- **Partial-PIREP-Filter review-state-aware:** Filter ist jetzt `(status != 'partial' OR review_state = 'accepted_with_warning') AND review_state NOT IN ('open', 'acknowledged') AND review_decision != 'rejected'`. Admin-Entscheidung „akzeptiert" greift jetzt wirklich.
- **API LE21b `reopen`:** `target_state`-Enum um `reopen` erweitert. Validierung dass aktueller State terminal sein muss, reason REQUIRED.
- **Cross-Surface-Matrix Aircraft-Identity:** `MANUFACTURER_SHIFT` + `SIZE_CLASS_SHIFT` von CRITICAL auf ANOMALY-base herabgestuft (mit Operational-Conflict-Eskalation), `AIRCRAFT_TYPE_UNKNOWN` ergänzt. Sync mit LE12 R2-Klassifikation.
- **v0.13.1-/„später"-Reste entfernt:** REPOSITION-Cancel-Pfad jetzt explizit als Stream-F-Hookin zur LE16c-Safe-Termination; Q1–Q10 von „v0.13.1/Backlog"-Formulierungen befreit. Alle Q-Punkte in v0.13.0-Scope.
- **Status-Header + A3 nachgezogen:** Header von `SPEC R0` auf `SPEC R4`. A3 markiert als revidiert in R1.

**R3-Hauptänderungen ggü. R2:**
- **Recompute-Mechanismus** bei nachträglicher Live-Eskalation: Affected-Segment wird mit Checkpoint-Replay durch FSM/Scorer re-validiert. Verhindert kontaminierte Zustände.
- **Per-Flight-Ingestion-Serialisierung:** explizit normativ — per-`session_id`-Lock, Idempotency-Key `(session_id, sequence_number)`, Monotonic-Sequence/Timestamp-Checks, MQTT-Replay-Safety mit Sequence-Tracking.
- **Post-Resume-Status-Pill** im Client: bei SEAMLESS-Resume mit Recorder-Side-Flags zeigt Client persistente Pill „Datenlücke wird geprüft". Verhindert UX-Widerspruch zwischen Client-Komfort und Recorder-Realität.
- **LE16c Partial-PIREP Statistik-Sperre:** explizite Ausschluss-Tabelle für alle Aggregate (Bestenliste, Route, Fuel, Completion, Pilot, VA). Kaputter Flug zählt nirgends bis Admin entschieden hat.
- **Negative-Controls präzisiert:** „no review-impacting flags" statt „NICHT geflagt". INFO/ANOMALY-base ist OK. +2 neue NC für Go-Around und Bounce.
- **Admin-State-Machine `reopen`:** jedes terminale State kann zu `open` zurückgesetzt werden mit Pflicht-Reason. Korrigiert Fehl-Entscheidungen ohne SQL-Hack.
- **Ruleset-/Aircraft-Families-Version pro Flag:** `integrity_flag` persistiert `ruleset_version` + `aircraft_families_version` + `detector` + `detected_at` + `effective_severity_history`. Historische Reproduzierbarkeit gesichert.
- **Backfill-`reproducibility_pin` verschärft:** zusätzlich `pirep_ids_canonical[]` (sortiert + gehashed) + `per_source_hashes`. Jeder neue/entfernte/modifizierte PIREP zwischen Dry-Run und Apply wird erkannt.
- **Phase-Continuity Go-Around/Missed-Approach/Bounce:** Tabelle erlaubt Final→Climb, Approach→Climb, Landing→TakeoffRoll — aber **nur unter Operating-State-Check** (engines, brake, GS, IAS).
- **Aufwand ehrlich auf ~162 h hochkorrigiert** (R2 war 122 h — Recompute, Serialisierung, Reopen, Versionierung, Fixtures kosten echt mehr).

**R2-Hauptänderungen ggü. R1:** Live-Aggregation, Envelope-Gap-Edge, LE16c Safe Termination, Backfill `reproducibility_pin` v1, Aircraft-Families data-driven, CRITICAL nur mit Operational-Conflict, Clean-Shutdown-Bonus UX-only, LE21b Admin-State-Machine v1, Negative Controls (5), kein Phase-Split.

**R1-Hauptänderungen ggü. R0:** Severity-Aggregation, Stream-A-Gap-Edge-Mode, normative Recorder-Pipeline (C.1), Score-Trust-Härtung, LE16b BlocksOn-Reject, Backfill reaktiviert, Clean-Shutdown-Bonus eingegrenzt, Aircraft-Family-Tabelle, Admin-Action-Vokabular, WZZ2271-Akzeptanz-Sektion.

Nach QS-Runde 4 (R3 → R4 oder direkt ACCEPTED) werden offene Q1–Q10 entschieden, Schwellwerte ggf. getunt, dann Code-Slice-Plan in einem separaten Dokument (`docs/spec/v0.13.0-implementation-plan.md`).

**Referenz-Testfall durch die ganze Spec:** PIREP **WZZ2271** (24.05.2026, LHBP→EHEH, A321 HA-LVA, X-Plane, AeroACARS 0.12.12). Forensik-Report siehe `C:\Users\kantt\Downloads\WZZ2271-Forensik-Report.pdf` (extern). Akzeptanz-Kriterien aus diesem Fall sind im Abschnitt „Referenz-Akzeptanz: WZZ2271" oben verbindlich festgeschrieben, ergänzt durch 7 Negative-Controls die Overfitting verhindern.
