# v0.12.0 — Runway-Utilization-Score Refinement (Float-Toleranz)

**Status:** SPEC ACCEPTED R2 — von Thomas Kant freigegeben, umgesetzt in v0.12.0 (PR #17 = Code-Slice 1)
**Autor:** v0.1x-Session-Owner
**QS-Reviewer:** Thomas Kant (VA-Owner)
**Trigger:** Pilot-Beschwerde BTX8815 (Pilot Michel, Fenix A319, EDVE→LOWS, 2026-05-19) — exzellent gebremst (442 m Ausrollstrecke), trotzdem nur 80 PT „Bahn-Auslastung". Nachgerechnet: der Score ist nach `v0.10.0`-Spec korrekt, ABER die Formel gewichtet die **Float-Strecke** (zu spätes Aufsetzen) 1:1 wie die **Ausrollstrecke** (Bremsen). Bei Michel waren 55 % der „Bahn-Auslastung" Float, nur 45 % Bremsweg — die fehlenden 20 PT kamen vom späten Aufsetzen, nicht vom Bremsen. Score-Logik + UI-Kommunikation überarbeiten.

---

**Errata v0.20.x (Bahnauslastung-QS, 2026-08-04):** die 15 %-LDA-Float-
Toleranz und die 30/50/70/90-%-Banding-Grenzen dieser Spec sind
VERALTET. Ein weiterer Pilot-Fall (Thomas selbst, EDDH→LOWS) zeigte, dass
auch nach dieser Refinement noch eine objektiv gute, komfortable Landung
nur „ok_stop" (55 PT) erreichte. Beide Stellschrauben wurden
grosszuegiger gemacht: Toleranz → 20 % LDA, Banding → 40/60/80/95 %.
Ausserdem wurde der `landing_rate`-Sub-Score von einer monotonen
„weicher=besser"-Kurve auf einen Ziel-Korridor (90-250 fpm = fest,
positiv, volle Punktzahl; darunter leichter Abzug wegen Float-Risiko)
umgestellt — siehe `sub_landing_rate.rs`. `score_algorithm_version`
Some(5). Die Grundformel (LDA-basiert, Float-Toleranz-Mechanismus,
`long_float`-Override) aus dieser Spec bleibt gültig — nur die
konkreten Zahlen sind überholt.

---

## Changelog dieser Spec

- **R0:** initialer Draft (Float-Toleranz 15 % LDA, `long_float`-Rationale, score_algorithm_version 3, UI-Punkte)
- **R1 (jetzt):** QS-R0-Findings eingearbeitet:
  - **P1** — `long_float` triggerte zu breit (`Band::Good` umfasst auch 80 PT = nicht-top-Bremsweg). R1: zusätzliche Trigger-Bedingung `rollout / LDA < 30 %` (= Bremsweg ALLEIN wäre `excellent_margin`-Niveau) — siehe LE2.
  - **P1** — `value`-`format!` hatte deutschen Text → un-i18n-fähig. R1: `value` bleibt sprachneutral (`"983 m / 2850 m · 35 %"` wie v0.10.0), Labels macht der TS-Renderer — siehe LE4 + Algorithmus.
  - **P1** — Alttexte mit „Stopp"/„Stillstand"/„standstill" in den Locales. R1: neuer Pflicht-Akzeptanzpunkt „Locale-Audit" — siehe LE9.
  - **P1** — LE6/LE7 drifteten zur v0.11.2-UI. Es existiert bereits `RunwayUtilizationHelpModal.tsx` (389 Zeilen) + der rollout-InfoBadge ist im LandingPanel schon ausgeblendet. R1: kein neuer Inline-Block — das **vorhandene Modal wird aktualisiert** — siehe LE6+LE7 (zusammengelegt).
  - **P2** — i18n-Sektion nannte DE/EN/IT, listete nur DE/EN. R1: IT ergänzt.
  - **P2** — Michel-Rechnung rundungs-inkonsistent. R1: Rundungsregel festgelegt (LE10) + Beispielzahlen vereinheitlicht.
  - Plus: alle 6 R0-Fragen von Thomas beantwortet (siehe Sektion „Beantwortete R0-Fragen").
- **R2 (jetzt):** QS-R1-Findings eingearbeitet:
  - **P1** — LE5 nannte Feldnamen die im Pilot-Client so nicht existieren (`runway_match_icao` flach). Pilot-Client hat nested `record.runway_match.{airport_ident,runway_ident,length_ft}` + `displaced_threshold_ft`. R2: getrenntes Mapping Pilot vs. Webapp in LE5.
  - **P1** — `extra=[]` falsch beschrieben: `SubScoreEntry.extra` hat `skip_serializing_if = "Vec::is_empty"` → leeres `extra` wird im Wire-JSON **ganz weggelassen**, nicht als `[]` serialisiert. R2: Datenmodell + Test präzisiert.
  - **P2** — Aufsetzpunkt-Extra-Zeile braucht signed wording: negatives `td_distance` darf nicht „−50 m hinter der Schwelle" ergeben. R2: separater i18n-Key `before_threshold` + Formatter-Regel.
  - Bestätigt: `long_float`-Schwelle 30 % (Punkt A) ✓ · Locale-Audit per grep (Punkt B) ✓.

---

## Verhältnis zur bestehenden Spec

`docs/spec/v0.10.0-runway-utilization-score.md` ist **SPEC ACCEPTED** und released (v0.10.0). Diese Spec hier **ersetzt sie nicht** — sie verfeinert die Score-Formel (LE1) und die UI-Kommunikation (LE4–LE7). Die v0.10.0-Leitentscheidungen LE2 (LDA), LE5 (Heavy-Allowance), LE6 (6 Skip-Gates), LE7 (SSoT), LE8 (Golden-JSON), LE10 (forward-only) bleiben **unverändert gültig**.

Geändert wird ausschließlich:
- LE1 (Numerator) — neue Float-Toleranz
- LE4 (Banding) — Banding auf der toleranzbereinigten Distanz, Overrun-Gate bleibt auf der echten
- neue Rationale `long_float`
- `score_algorithm_version` 2 → 3
- UI-Render (Value-Label, getrennte Extra-Zeilen, Tooltip-Fix, „Wie berechnet?"-Block)

---

## Ziel

1. **Brems-Disziplin und Aufsetz-Disziplin fair trennen.** Ein Pilot der exzellent bremst, aber innerhalb der normalen Toleranz spät aufsetzt, soll nicht denselben Punktabzug bekommen wie einer der lang ausrollt.
2. **Transparenz** — der Pilot muss in der UI sofort sehen WAS den Score gedrückt hat (Float vs. Ausrollstrecke), nicht raten.
3. **Sicherheits-Aussage bleibt intakt** — das Overrun-Risiko (zu nah am Bahnende) wird weiterhin auf der **echten gesamten** Distanz bewertet, ungekürzt.
4. **Keine stille Score-Drift** — `score_algorithm_version` macht v0.10.0- vs v0.12.0-Scores explizit unterscheidbar; alte PIREPs behalten ihren v2-Score.

---

## Pflicht-Vorgaben

### Datenquellen (alle bereits im Touchdown-Payload, seit v0.8.x)

| Feld | Verwendung |
|---|---|
| `td_distance_from_threshold_m` | Float-Strecke (Schwelle → Aufsetzpunkt) |
| `rollout_distance_m` | Ausrollstrecke (Aufsetzpunkt → Abrollen, gemessen bis ~40 kt ODER >30° Heading-Abweichung) |
| `runway_length_m`, `runway_displaced_threshold_ft` | LDA-Berechnung (unverändert v0.10.0 LE2) |
| `pre_displaced_threshold`, `runway_geometry_trusted`, `airport_source` | Skip-Gates (unverändert v0.10.0 LE6) |
| `aircraft_icao` | Heavy-Allowance (unverändert v0.10.0 LE5) |

**Keine neuen Wire-Felder am `TouchdownPayload`.** Einziges Wire-Delta: `score_algorithm_version` bekommt den Wert `3` statt `2`.

### Terminologie-Festlegung (verbindlich für UI + i18n + Spec)

| Phase | Definition | UI-Begriff (DE) |
|---|---|---|
| Schwelle → Aufsetzpunkt | Schwebestrecke nach Schwellen-Crossing | **„Float"** / „Schwebestrecke" |
| Aufsetzpunkt → Abrollen | Verzögerung bis Abrollgeschwindigkeit | **„Ausrollstrecke"** |
| Endpunkt der Messung | Geschwindigkeit < 40 kt **ODER** Heading-Abweichung > 30° (= Abbiegen in Taxiway) | „bis zum **Abrollen** von der Bahn" |

**WICHTIG:** Die Ausrollstrecke endet NICHT beim Stillstand. Auf der Bahn wird nicht angehalten — gemessen wird bis zum Abrollpunkt (~40 kt). Texte dürfen nirgends „Stopp" / „Stillstand" für das Ende der Ausrollstrecke verwenden.

---

## Leitentscheidungen

### LE1 — Float-Toleranz: 15 % der LDA

Die v0.10.0-Formel `distance_used = td_distance + rollout` wird ergänzt um eine **Float-Toleranzzone**. Float bis 15 % der LDA gilt als „normal/akzeptabel" und belastet das Punkte-Banding nicht. Nur Float **über** der Toleranz zählt.

```rust
let float_tolerance_m = 0.15 * lda_m;
let effective_float_m = (td_dist - float_tolerance_m).max(0.0);
let effective_distance_m = rollout + effective_float_m;
```

**Begründung 15 %:**
- Die offizielle Touchdown-Zone (TDZ) reicht bis `min(900 m, LDA/3)` — bei einer typischen 2400–3000 m Bahn sind das ~800–900 m, also ~30 % der LDA. 15 % LDA liegt sicher INNERHALB der TDZ → wer in der erlaubten Zone aufsetzt wird nicht bestraft.
- 15 % ist konsistent mit der `long_float`-Rationale-Schwelle (LE2) — eine Zahl, ein mentales Modell.
- Bei kurzen Bahnen (z. B. 1500 m LDA) sind 15 % nur 225 m — angemessen eng, weil dort jeder Meter zählt. Bei langen Bahnen (3500 m) sind es 525 m — großzügiger, was passt weil dort Float weniger kritisch ist.

**Negativ-Schutz (v0.10.0 LE3 bleibt):** bei `pre_displaced_threshold` ist `td_dist` negativ → `td_dist - tolerance` ist erst recht negativ → `effective_float = 0`. `effective_distance = rollout`. Korrekt — pre-displaced wird weiterhin separat über den Cap auf 55 PT behandelt (v0.10.0 LE3).

**Rundung (R1-P2-2-Fix):** alle internen Berechnungen laufen mit `f32` **ohne Zwischenrundung** — identisch zu v0.10.0 LE4 („Float-Banding, keine Pre-Rundung"). Gerundet wird **ausschließlich fürs Display** (ganze Meter, ganze Prozent). Beispielzahlen in dieser Spec sind durchgehend mit den echten Flight-Log-Werten gerechnet (`td_dist=540,85 m · rollout=442,15 m · LDA=2849,88 m`) und auf 1 Nachkommastelle dargestellt.

### LE2 — `long_float`-Rationale

`long_float` ist explizit der Fall **„Bremsen war exzellent, nur der Aufsetzpunkt war zu spät"**. Der Rationale-Key wird auf `long_float` überschrieben wenn **alle drei** Bedingungen erfüllt sind:

```rust
let float_over_tolerance = td_dist > float_tolerance_m;          // 1. Float über 15 %-Toleranz
let rollout_alone_excellent = (rollout / lda_m) * 100.0 < 30.0;  // 2. Bremsweg ALLEIN wäre excellent
let band_is_good = matches!(final_band, Band::Good);             // 3. finaler Score-Band good/excellent
let long_float = !pre_displaced && float_over_tolerance
                  && rollout_alone_excellent && band_is_good;
```

**R1-P1-1-Fix — warum Bedingung 2 (`rollout / LDA < 30 %`):** R0 prüfte nur `Band::Good`. Aber `Band::Good` umfasst 80 PT UND 100 PT — bei 80 PT ist der Bremsweg nicht zwingend „top". Die Aussage „Bremsweg top" stimmt nur wenn die **Ausrollstrecke für sich allein** ein `excellent_margin`-Niveau erreicht hätte. 30 % LDA ist exakt die `excellent_margin`-Bandgrenze (v0.10.0 LE4). Damit triggert `long_float` nur wenn der Pilot wirklich exzellent gebremst hat und ausschließlich der Float die Punkte gekostet hat.

| Key | DE | EN |
|---|---|---|
| `long_float` | „Bremsweg top — aber spät aufgesetzt" | „Good braking — but touched down long" |

**Warum Bedingung 3 (Band good):** bei finalem Band `ok`/`bad` ist die normale Rationale (`ok_stop` / `long_rollout` / `marginal_runway`) aussagekräftiger — selbst wenn der reine Bremsweg < 30 % war, ist der Gesamt-Eindruck dann nicht mehr „top".

Analogie: v0.10.0 hat `pre_displaced_capped` für „zu früh aufgesetzt". `long_float` ist das Gegenstück „zu spät aufgesetzt". Symmetrisch.

### LE3 — Banding auf `effective_distance`, Overrun-Gate auf echter Distanz

**Kritische Trennung** (Sicherheit vs. Fairness):

```rust
// Overrun-Gate — UNVERÄNDERT auf der ECHTEN, ungekürzten Distanz (v0.10.0 LE4 Step 2)
let raw_ratio_pct = (distance_used / lda_m) * 100.0;   // distance_used = max(td+rollout, rollout)
if raw_ratio_pct > 100.0 {
    return (0, "overrun_risk", Band::Bad);
}

// Banding — NEU auf der toleranzbereinigten Distanz
let effective_ratio_pct = (effective_distance_m / lda_m) * 100.0;
let allowance_pp = if is_heavy { 5.0 } else { 0.0 };
let banding_pct = effective_ratio_pct - allowance_pp;
match banding_pct {
    e if e < 30.0 => (100, "excellent_margin", Band::Good),
    e if e < 50.0 => ( 80, "good_stop",        Band::Good),
    e if e < 70.0 => ( 55, "ok_stop",          Band::Ok),
    e if e < 90.0 => ( 25, "long_rollout",     Band::Bad),
    _             => (  5, "marginal_runway",  Band::Bad),
}
```

**Begründung:** Wer wirklich am Bahnende klebt (echte Distanz > 100 % LDA) bekommt weiterhin `overrun_risk` / 0 PT — die Float-Toleranz darf ein echtes Overrun-Risiko NICHT verstecken. Nur das „Komfort-Banding" (100/80/55/25/5) nutzt die toleranzbereinigte Distanz.

**Konsequenz-Check (BTX8815, echte Flight-Log-Werte):** `td_dist=540,85 m · rollout=442,15 m · LDA=2849,88 m` (A319 = Medium, keine Heavy-Allowance).
- `float_tolerance = 0,15 × 2849,88 = 427,5 m`
- `effective_float = (540,85 − 427,5).max(0) = 113,4 m`
- `effective_distance = 442,15 + 113,4 = 555,5 m`
- `distance_used = (540,85 + 442,15).max(442,15) = 983,0 m`
- `raw_ratio = 983,0 / 2849,88 = 34,5 %` → Overrun-Gate: 34,5 < 100 ✓ kein Overrun
- `effective_ratio = 555,5 / 2849,88 = 19,5 %` → Banding `< 30` → **`excellent_margin`, 100 PT**, `final_band = Good`
- `long_float`-Trigger (LE2): Float 540,85 > Toleranz 427,5 ✓ · `rollout/LDA = 442,15/2849,88 = 15,5 % < 30 %` ✓ · `final_band = Good` ✓ → **`long_float`**
- Ergebnis: **100 PT · Rationale „Bremsweg top — aber spät aufgesetzt"** (vorher v0.11.x: 80 PT „Solider Bremsweg")

### LE4 — Display-Value: ECHTE Bahn-Auslastung, sprachneutral

Die Card-Hauptzeile (`value`) zeigt die **echte** genutzte Distanz + echte Prozent — NICHT die toleranzbereinigte (R0-Frage 1, von Thomas bestätigt: nur echte %, score-relevant gehört in den Erklär-Block). Begründung: das RunwayDiagram zeigt physisch wo der Pilot aufgesetzt + abgerollt ist; eine kleinere Card-Zahl als im Diagramm wäre verwirrend.

**R1-P1-2-Fix — `value` bleibt sprachneutral.** R0 hatte `value` mit deutschem Text (`"35 % der Bahn genutzt · …"`) — das macht den Rust-Crate un-i18n-fähig. R1: `value` ist das **gleiche sprachneutrale Kompakt-Format wie v0.10.0**:

```
value (vom Rust-Crate)  =  "983 m / 2850 m · 35 %"
```

Die Labels (`„genutzt"`, `„von LDA"`, `„der Bahn"`) baut der **TS-Renderer** um den `value` herum — DE/EN/IT über i18n. Damit bleibt `landing-scoring` (Rust) komplett sprachneutral, konsistent mit v0.10.0.

### LE5 — UI: Value-Label + getrennte Extra-Zeilen (TS-gerendert, i18n-fähig)

Aktuelle Card (v0.10.0–v0.11.x):
```
983 m / 2850 m  ·  35 %                       ← value (sprachneutral)
Solider Bremsweg                              ← rationale
▸ davon ~541 m Float vor Aufsetzen            ← extra[0] — hardcoded DE im Rust-Crate
▸ Bahn: LOWS 15, LDA 2850 m                   ← extra[1] — hardcoded DE im Rust-Crate
```

Ziel-Card (v0.12.0):
```
983 m / 2850 m · 35 %     [Label: "35 % der Bahn · 983 m von 2850 m LDA" — TS]
Bremsweg top — aber spät aufgesetzt           ← rationale (i18n long_float)
▸ Aufsetzpunkt: 541 m hinter der Schwelle     ← TS-gerendert aus Record-Feld
▸ Ausrollstrecke ab Aufsetzen: 442 m          ← TS-gerendert aus Record-Feld
▸ Bahn: LOWS 15 · LDA 2850 m                  ← TS-gerendert aus Record-Feld
```

**R1-P1-2 / Frage-5-Fix — `extra` wird NICHT mehr vom Rust-Crate befüllt (für v3-Scores).** R0 wollte die `extra`-Strings im Rust-Crate bauen — das wären hardcoded deutsche Strings (genau das Problem das v0.10.0 schon hatte mit `"davon ~520 m Float…"`). R1-Lösung (Thomas Frage-5-Antwort, Option b):

- `sub_rollout_v2()` setzt für **v0.12.0-Scores `extra = Vec::new()` (leer)**.
- Der **TS-Renderer** baut die 3 Zeilen aus den vorhandenen Record-/Payload-Feldern + i18n-Keys `landing.rollout_extra.*` (DE/EN/IT).

**R2-P1-1-Fix — getrenntes Feld-Mapping (Pilot-Client ≠ Webapp).** Die Datenstrukturen unterscheiden sich:

| Zeile | Pilot-Client (`LandingPanel.tsx`, `LandingRecord`) | Webapp (`LandingAnalysis.tsx`, `FullTouchdownPayload`) |
|---|---|---|
| Aufsetzpunkt | `record.td_distance_from_threshold_m` | `payload.td_distance_from_threshold_m` |
| Ausrollstrecke | `record.rollout_distance_m` | `payload.rollout_distance_m` |
| Bahn-ICAO | `record.runway_match.airport_ident` (nested) | `payload.runway_match_icao` (top-level) |
| Bahn-Ident | `record.runway_match.runway_ident` (nested) | `payload.runway_match_ident` (top-level) |
| LDA-Quelle | `record.runway_match.length_ft` + `record.runway_match.displaced_threshold_ft` (beides ft, nested) | `payload.runway_length_m` + `payload.runway_displaced_threshold_ft` |

**LDA-Berechnung Pilot-Client:** `LDA_m = (length_ft − displaced_threshold_ft) × 0.3048`.
**LDA-Berechnung Webapp:** `LDA_m = runway_length_m − runway_displaced_threshold_ft × 0.3048`.

Beide Renderer kommen zum gleichen LDA-Wert, müssen aber die unterschiedlichen Feld-Pfade kennen. Wenn `runway_match` (Pilot) bzw. `runway_match_icao` (Webapp) fehlt, entfällt die Bahn-Extra-Zeile (die anderen zwei bleiben).

- **Alt-v2-PIREPs** behalten ihre hardcoded-DE `extra`-Strings — Legacy. Der TS-Renderer entscheidet per `score_algorithm_version`: `>= 3` → selbst rendern · `< 3` → gespeicherte `extra`-Strings zeigen.

Damit ist `landing-scoring` (Rust) für v3 komplett sprachneutral, und neue Extra-Zeilen sind i18n-fähig ohne Wire-Format-Änderung.

### LE6 — `RunwayUtilizationHelpModal` aktualisieren (KEIN neuer Block)

**R1-P1-4-Fix.** R0 schlug einen neuen aufklappbaren Inline-Block + einen InfoBadge vor. Beides driftet an der v0.11.2-UI vorbei:
- Der rollout-`InfoBadge` ist im `LandingPanel.tsx` **bereits ausgeblendet** (v0.11.x).
- Es existiert bereits `client/src/components/RunwayUtilizationHelpModal.tsx` (389 Zeilen) — ein vollwertiges Pilot-Hilfe-Modal mit Formel, 5 Bändern, Heavy-Bonus, Pre-Displaced-Cap, Skip-Reasons, i18n via `landing.runway_utilization_help.*`, geöffnet über einen „🛬 Wie wird das berechnet?"-Button am Boden der rollout-Card.

→ **Kein neuer Block, kein InfoBadge.** Das **vorhandene `RunwayUtilizationHelpModal` wird inhaltlich auf v0.12.0 aktualisiert.** Was rein muss:

1. **Float-Toleranz-Erklärung** — neuer Abschnitt: „Float innerhalb der ersten 15 % der LDA zählt nicht — nur der Überschuss. Ausrollstrecke zählt immer voll."
2. **Formel-Update** — die angezeigte Formel von `(Float + Ausrollstrecke) / LDA` auf die toleranzbereinigte Variante.
3. **`long_float`-Band-Erklärung** — neben den 5 Bändern + Pre-Displaced-Cap auch der `long_float`-Fall.
4. **Konkrete Beispiel-Rechnung** (BTX8815 als Anchor):
   ```
   Schwelle ─[ Float 541 m ]→ Aufsetzen ─[ Ausrollen 442 m ]→ Abrollen (~40 kt)
   |                                                                       |
   0 m                                              echte Auslastung 983 m / 35 %

   Float-Toleranz   = 15 % der LDA  = 427 m
   davon score-relevant: 541 − 427  = 114 m Float-Überschuss
   Score-Distanz    = 442 m Ausrollen + 114 m Float-Überschuss = 556 m = 20 %
   →  < 30 %  →  100 PTS „excellent_margin"

   Bremsen war top — die Punkte-Schwelle lag am Float (zu spät aufgesetzt).
   Übe: früher aufsetzen (Aim-Point), nicht härter bremsen.
   ```
5. **Terminologie** — alle „Stopp"/„Stillstand"-Formulierungen im Modal-Text raus (siehe LE9).

Das Modal hat die ASCII-/Box-Darstellung Platz — die SinkrateForensik-artige Visualisierung kommt also INS Modal, nicht als separater Inline-Block (R0-Frage 2, Thomas-Antwort: „vorhandenes Modal berücksichtigen, keine zusätzliche eigene Sektion").

**Webapp-Pendant:** `aeroacars-live` hat die Help-Erklärung als eigene Komponente/Sektion in `LandingAnalysis.tsx` (der „How is this calculated?"-Link aus Michels Screenshot). Gleiche inhaltliche Aktualisierung dort.

### LE7 — `info.rollout`-Tooltip-Text-Fix

Unabhängig vom Modal: der i18n-Key `landing.info.rollout` (= Tooltip-Text, auch wenn der Badge im Pilot-Client gerade nicht gerendert wird — die Webapp und evtl. spätere UI nutzen ihn) beschreibt das **Pre-v0.10-Verhalten**:

> **Jetzt (falsch):** „Prozent der verfügbaren Bahnlänge die du **vom Touchdown** bis zum Verlassen … genutzt hast"

> **Neu:** „Anteil der nutzbaren Bahnlänge (LDA) den deine gesamte Landung gebraucht hat — gemessen **ab der Schwelle**: erst die Float-Strecke bis zum Aufsetzen, dann die Ausrollstrecke bis zum Abrollen von der Bahn (~40 kt). Float innerhalb der ersten 15 % der LDA gilt als normal und kostet keine Punkte; darüber zählt es. < 50 % ist gut, > 90 % wird knapp — bei nasser Bahn riskant."

### LE8 — `score_algorithm_version` 2 → 3

```
- None / Some(1): pre-v0.10 (meter-only)
- Some(2): v0.10.0/v0.11.x (LDA-basiert, Float 1:1)
- Some(3): v0.12.0 (LDA-basiert mit 15 %-Float-Toleranz)
```

Forward-only (v0.10.0 LE10 bleibt): alte PIREPs behalten ihren gespeicherten Score + ihre Version. Kein Re-Score. UI rendert v3 + v2 weitgehend identisch — Unterschied: bei v3 baut der TS-Renderer die Extra-Zeilen selbst (LE5), bei v2 zeigt er die gespeicherten `extra`-Strings.

**Master-Score-Rückwirkung (R0-Frage 3, Thomas-Antwort: Release-Notes-Hinweis, kein Re-Score):** der Bahn-Auslastungs-Sub-Score hat Gewicht 1 von 13. Ein Pilot wie BTX8815 (rollout 80→100) gewinnt ~1,5 Master-Punkte. In der „Meine Flüge"-Historie entsteht ein Knick zwischen v2- und v3-PIREPs. → wird in den v0.12.0-Release-Notes transparent erwähnt. Kein Re-Score-Tool.

### LE9 — Locale-Audit: „Stopp"/„Stillstand"/„standstill" raus (NEU R1, P1-3-Fix)

QS-R0 hat aufgedeckt: die Terminologie-Festlegung (Ausrollstrecke endet bei ~40 kt, nicht beim Stillstand) widerspricht **bestehenden** Locale-Texten. Konkret gefunden:

| Datei | Zeile | Stelle | Problem |
|---|---|---|---|
| `de/common.json` | ~174 | `landing.info.runway_section` | „…Highspeed-Exit < 40 kt oder **Stopp**" |
| `de/common.json` | ~501 | `landing.runway_utilization_help.intro` | „…bis zum **Stillstand** verbraucht…" |
| `de/common.json` | ~507 | `…help.terms.rollout` | „Rollout — vom Aufsetzen bis **Stillstand**…" |
| `en/common.json` | ~501 | `…help.intro` | „…to **standstill**…" |
| `en/common.json` | ~507 | `…help.terms.rollout` | „…from touchdown to **standstill**…" |
| `it/common.json` | analog | dieselben Keys | IT-Pendants prüfen |

**Akzeptanzpunkt v0.12.0:** vor SPEC-Abschluss alle drei Locale-Dateien (`de`/`en`/`it`) nach `stopp` / `stillstand` / `standstill` / `zum stehen` / `come to a (full )?stop` / `to a halt` durchsuchen. Jede Fundstelle die das **Ende der Ausrollstrecke** beschreibt MUSS auf „Abrollen / Abrollgeschwindigkeit ~40 kt" umformuliert werden. (Fundstellen die echten Stillstand meinen — z. B. Parkposition — bleiben.) Der EN-Text in `en/common.json:~457` ist bereits korrekt formuliert („Almost nobody rolls to a full stop on the runway") — der dient als Vorlage für die Tonalität.

Code-QS prüft das per `grep` über alle Locale-Dateien als Teil der Akzeptanz.

---

## Datenmodell

| Wo | Feld | Änderung |
|---|---|---|
| `TouchdownPayload` / `PirepPayload` / `LandingRecord` | `score_algorithm_version` | Wert `2` → `3` (Type unverändert `Option<u8>`) |
| `SubScoreEntry.rationale_key` | Werte erweitert | NEU: `landing.rat.long_float` |
| `SubScoreEntry.extra` | Inhalt | für v3-Scores `Vec::new()`. **R2-P1-2:** das Feld hat `#[serde(skip_serializing_if = "Vec::is_empty")]` → ein leeres `extra` erscheint **gar nicht** im Wire-JSON (kein `"extra": []`). TS deserialisiert das fehlende Feld zum Default `[]`. Feld-Definition selbst unverändert (Alt-v2-Compat). |
| `SubScoreEntry.value` | Format | **unverändert** sprachneutral `"{m} / {m} · {pct} %"` wie v0.10.0 (R1-P1-2) |

Kein neues Struct-Feld, **kein Wire-Format-Delta außer** `score_algorithm_version`-Wert. Golden-JSON-Snapshot-Test aus v0.10.0 LE8 muss aktualisiert werden: das `extra`-Feld **fehlt** im erwarteten JSON (statt `[]`), Score-Zahl neu, ggf. `rationale_key=landing.rat.long_float`.

---

## Algorithmus (Rust, `sub_rollout_v2` → erweitert)

Der bestehende `sub_rollout_v2()` aus `landing-scoring/src/sub_rollout.rs` wird erweitert. **Kein neuer Funktionsname** — `score_algorithm_version` am Payload macht die Version explizit, die Funktion selbst bleibt die eine SSoT-Compute-Stelle.

```rust
pub fn sub_rollout_v2(input: &RolloutInput) -> SubScoreEntry {
    // Skip-Gate (v0.10.0 LE6) — UNVERÄNDERT
    if let Some(reason) = skip_reason(input) {
        return SubScoreEntry::skipped("rollout", "landing.sub.rollout", reason);
    }
    let td_dist  = input.td_distance_from_threshold_m.unwrap() as f32;
    let rollout  = input.rollout_distance_m.unwrap();
    let runway_m = input.runway_length_m.unwrap();
    let displaced_m = input.runway_displaced_threshold_ft.unwrap_or(0) as f32 * 0.3048;
    let lda_m = runway_m - displaced_m;

    // Echte genutzte Distanz (v0.10.0 LE1 + Negativ-Schutz) — UNVERÄNDERT
    let raw_used = td_dist + rollout;
    let distance_used = raw_used.max(rollout);
    let raw_ratio_pct = (distance_used / lda_m) * 100.0;

    // NEU v0.12.0 (LE1): Float-Toleranz 15 % LDA
    let float_tolerance_m = 0.15 * lda_m;
    let effective_float_m = (td_dist - float_tolerance_m).max(0.0);
    let effective_distance_m = rollout + effective_float_m;
    let effective_ratio_pct = (effective_distance_m / lda_m) * 100.0;

    // Overrun-Check ZUERST, auf der ECHTEN Distanz (v0.10.0 LE4 Step 2) — UNVERÄNDERT
    let (points, rationale, band) = if raw_ratio_pct > 100.0 {
        (0u8, "overrun_risk", Band::Bad)
    } else {
        // NEU v0.12.0 (LE3): Banding auf effective, nicht raw
        let allowance_pp: f32 = if is_heavy(input.aircraft_icao) { 5.0 } else { 0.0 };
        let banding_pct = effective_ratio_pct - allowance_pp;
        match banding_pct {
            e if e < 30.0 => (100, "excellent_margin", Band::Good),
            e if e < 50.0 => ( 80, "good_stop",        Band::Good),
            e if e < 70.0 => ( 55, "ok_stop",          Band::Ok),
            e if e < 90.0 => ( 25, "long_rollout",     Band::Bad),
            _             => (  5, "marginal_runway",  Band::Bad),
        }
    };

    // pre_displaced-Cap (v0.10.0 LE3) — UNVERÄNDERT
    let pre_displaced = input.pre_displaced_threshold.unwrap_or(false);
    let (capped_points, capped_rationale, capped_band) = if pre_displaced {
        let pts = points.min(55);
        let b = if pts >= 75 { Band::Good } else if pts >= 45 { Band::Ok } else { Band::Bad };
        (pts, "pre_displaced_capped", b)
    } else {
        (points, rationale, band)
    };

    // NEU v0.12.0 (LE2, R1-P1-1): long_float-Rationale-Override.
    // DREI Bedingungen — siehe LE2:
    //   1. Float über 15 %-Toleranz
    //   2. Ausrollstrecke ALLEIN waere excellent_margin (rollout/LDA < 30 %)
    //   3. finaler Band ist Good (good_stop / excellent_margin)
    // pre_displaced hat Vorrang (eigener Cap + eigene Rationale).
    let float_over_tolerance = td_dist > float_tolerance_m;
    let rollout_alone_excellent = (rollout / lda_m) * 100.0 < 30.0;
    let (final_points, final_rationale, final_band) = if !pre_displaced
        && float_over_tolerance
        && rollout_alone_excellent
        && matches!(capped_band, Band::Good)
    {
        (capped_points, "long_float", capped_band)
    } else {
        (capped_points, capped_rationale, capped_band)
    };

    let warning = if pre_displaced {
        Some("pre_displaced_threshold".to_string())
    } else {
        None
    };

    // Display-value: SPRACHNEUTRAL (R1-P1-2-Fix), identisches Format wie
    // v0.10.0. ECHTE Auslastung (raw, nicht effective — LE4). Labels macht
    // der TS-Renderer.
    let display_pct = raw_ratio_pct.round() as i32;
    let value = format!(
        "{} m / {} m  ·  {} %",
        distance_used.round() as i32,
        lda_m.round() as i32,
        display_pct,
    );

    // Extra-Zeilen (LE5, R1-P1-2-Fix): v3-Scores lassen `extra` LEER.
    // Der TS-Renderer baut die Float-/Ausrollstrecke-/Bahn-Zeilen aus den
    // ohnehin vorhandenen Record-Feldern + i18n. Kein hardcoded Deutsch
    // mehr im Rust-Crate.
    let extra: Vec<String> = Vec::new();

    SubScoreEntry::scored("rollout", "landing.sub.rollout",
        final_points, value, final_rationale, final_band)
        .with_extra(extra)
        .with_warning(warning)
}
```

**Anmerkung zum `extra`-Feld:** für v0.12.0-Scores ist `extra` bewusst leer (LE5). Das Feld bleibt im Wire-Format (v0.10.0 LE12) — damit Alt-v2-Records ihre gespeicherten `extra`-Strings weiter rendern können. Der TS-Renderer unterscheidet per `score_algorithm_version`.

---

## i18n (DE / EN / IT)

### Neue Rationale-/Tip-Keys (P2-1-Fix: IT ergänzt)

| Key | DE | EN | IT |
|---|---|---|---|
| `landing.rat.long_float` | „Bremsweg top — aber spät aufgesetzt" | „Good braking — but touched down long" | „Frenata ottima — ma toccato troppo lungo" |
| `landing.tip.long_float` | „Dein Bremsen war top — der Punktverlust kommt vom späten Aufsetzen. Ziel den Aim-Point an (Markierung ~400 m hinter der Schwelle), Schwellen-Crossing mit Vref+5, Flare nicht zu früh." | „Your braking was excellent — the points came off for touching down long. Aim for the aiming-point marker (~400 m past the threshold), cross the threshold at Vref+5, don't flare too early." | „La frenata era ottima — i punti persi vengono dal touchdown lungo. Mira all'aiming-point (~400 m oltre la soglia), attraversa la soglia a Vref+5, non flerare troppo presto." |

### Neue Extra-Zeilen-Keys (TS-gerendert, LE5)

Die Extra-Zeilen werden vom TS-Renderer gebaut (nicht mehr Rust, LE5). Drei neue i18n-Keys mit Platzhaltern:

| Key | DE | EN | IT |
|---|---|---|---|
| `landing.rollout_extra.touchdown_point` | „Aufsetzpunkt: {{m}} m hinter der Schwelle" | „Touchdown: {{m}} m past the threshold" | „Touchdown: {{m}} m oltre la soglia" |
| `landing.rollout_extra.touchdown_point_before` | „Aufsetzpunkt: {{m}} m vor der Schwelle" | „Touchdown: {{m}} m before the threshold" | „Touchdown: {{m}} m prima della soglia" |
| `landing.rollout_extra.rollout_distance` | „Ausrollstrecke ab Aufsetzen: {{m}} m" | „Rollout from touchdown: {{m}} m" | „Rollout dal touchdown: {{m}} m" |
| `landing.rollout_extra.runway` | „Bahn: {{icao}} {{ident}} · LDA {{lda}} m" | „Runway: {{icao}} {{ident}} · LDA {{lda}} m" | „Pista: {{icao}} {{ident}} · LDA {{lda}} m" |

**R2-P2-Fix — Aufsetzpunkt signed wording.** `td_distance_from_threshold_m` kann negativ sein (Aufsetzen VOR der Schwelle = pre-displaced). Der TS-Renderer wählt den Key nach Vorzeichen und übergibt den **Betrag**:

```ts
const m = Math.round(Math.abs(td_distance_from_threshold_m));
const key = td_distance_from_threshold_m < 0
  ? "landing.rollout_extra.touchdown_point_before"
  : "landing.rollout_extra.touchdown_point";
t(key, { m });
```

Damit erscheint nie „−50 m hinter der Schwelle", sondern „50 m vor der Schwelle". (`{{m}}` ist immer der nicht-negative Betrag.)

### Geänderte Keys

| Key | Änderung |
|---|---|
| `landing.info.rollout` | Komplett neuer Text (LE7), DE/EN/IT |
| `landing.runway_utilization_help.*` | Float-Toleranz + `long_float` + Formel-Update + Beispiel + Terminologie-Fix (LE6), DE/EN/IT |
| diverse Keys mit „Stopp"/„Stillstand"/„standstill" | Locale-Audit (LE9), DE/EN/IT |

---

## Tests

### Rust-Crate (`landing-scoring/tests/sub_rollout_v2.rs` — erweitern)

Neue Test-Cases zusätzlich zu den 21 bestehenden:

| Test | Prüft |
|---|---|
| `btx8815_real_case_long_float` | echte Flight-Log-Werte (540,85/442,15/2849,88) → 100 PT, Rationale `long_float`, `extra=[]` |
| `long_float_needs_excellent_rollout` | Float über Toleranz, aber `rollout/LDA = 35 %` (≥ 30 %) → NICHT `long_float`, normale Rationale (R1-P1-1) |
| `float_within_tolerance_no_override` | Float < 15 % LDA → normale Rationale, kein `long_float` |
| `float_over_tolerance_but_band_ok_keeps_normal_rationale` | Float über Toleranz, aber Band `ok` (schlecht gebremst) → `ok_stop`, NICHT `long_float` |
| `overrun_still_on_raw_distance` | echte Distanz > 100 %, effective < 100 % → trotzdem `overrun_risk` (Toleranz versteckt Overrun nicht) |
| `tolerance_scales_with_lda` | kurze Bahn 1500 m → Toleranz 225 m · lange Bahn 3500 m → Toleranz 525 m |
| `pre_displaced_has_priority_over_long_float` | pre_displaced + langer Float → `pre_displaced_capped` gewinnt, nicht `long_float` |
| `negative_td_with_tolerance` | negativer td_dist → effective_float = 0, kein Crash |
| `effective_vs_raw_ratio_in_value` | `value` zeigt RAW-% (echte Auslastung), nicht effective-%; sprachneutrales Format `"m / m · %"` |
| `extra_is_empty_for_v3` | v3-Score: Rust-Struct `entry.extra.is_empty()` true · serialisiertes JSON enthält KEIN `extra`-Feld (skip_serializing_if) |
| `td_distance_signed_wording` | TS-Test: negatives `td_distance` → „… vor der Schwelle", positiv → „… hinter der Schwelle" (R2-P2) |
| Golden-JSON-Snapshot | aktualisierter erwarteter Wert für `score_algorithm_version=3` |

### Webapp (`aeroacars-live`)

Mirror-Implementation in `webapp/src/components/landingScoring.ts` (`subRolloutV2`) muss die gleiche Float-Toleranz + `long_float`-Rationale bekommen. Plus Test-Cases in `landingScoring.test.ts`. Identisches Muster wie v0.10.0-Webapp-Mirror.

### TS Pilot-Client

`LandingPanel.tsx` — value-Label-Render (LE4) + TS-gerenderte extra-Zeilen aus Record-Feldern (LE5). `RunwayUtilizationHelpModal.tsx` — aktualisierter Inhalt (LE6). vitest-Tests für die extra-Zeilen-Render-Logik (v3 → TS-gebaut, v2 → gespeicherte `extra` zeigen).

---

## Aufwand-Schätzung

| Task | Schätzung |
|---|---|
| Spec QS bis ACCEPTED | 1–2 Runden |
| `sub_rollout.rs` Float-Toleranz + `long_float` + Tests | 2 h |
| Golden-JSON aktualisieren | 15 min |
| `lib.rs` `score_algorithm_version` 2→3 an 3 Sites | 30 min |
| i18n DE/EN/IT (2 neue rat/tip + 3 extra-Keys + info-Fix + Locale-Audit LE9) | 1 h |
| `LandingPanel.tsx` value-Label + TS-gerenderte extra-Zeilen | 1,5 h |
| `RunwayUtilizationHelpModal.tsx` auf v0.12.0 aktualisieren (LE6) | 1,5 h |
| aeroacars-live Mirror (`landingScoring.ts` + Tests + `LandingAnalysis.tsx` Help-Sektion + Locale-Audit) | 2,5 h |
| Code-QS-Runden | 1–2 Runden |
| Version-Bump + bilinguale Release-Notes + VPS-Deploy | 1 h |
| **Gesamt** | **~11–13 h Code + QS** |

---

## Beantwortete R0-Fragen (Thomas-QS-R0)

1. **`value`-Format** — nur echte Auslastung anzeigen; score-relevant in den Erklär-Block. ✅ LE4.
2. **Erklär-Block-Platzierung** — vorhandenes `RunwayUtilizationHelpModal` aktualisieren, keine zusätzliche eigene Sektion. ✅ LE6.
3. **Master-Score-Knick** — Release-Notes-Hinweis reicht, kein Re-Score. ✅ LE8.
4. **`value` sprachneutral** — ja, sprachneutral im Rust-Crate, Labels im TS-Renderer. ✅ LE4 + Algorithmus.
5. **Extra-Zeilen-i18n** — nicht weiter hardcoded-DE; TS-Renderer baut sie aus Record-Feldern, Alt-v2-`extra` bleibt legacy. ✅ LE5.
6. **Versions-Tag** — `v0.12.0` bestätigt (Score-Algo 2→3 ist Minor, kein Patch). ✅.

## R1-Bestätigungspunkte (von Thomas-QS-R1 bestätigt)

- **A** — `long_float`-Schwelle `rollout/LDA < 30 %`: ✅ bestätigt (sauber an die `excellent_margin`-Grenze gekoppelt).
- **B** — Locale-Audit (LE9): ✅ grep-Akzeptanz reicht; Spec muss die Ersatztexte nicht alle ausformulieren, nur Keys + Suchbegriffe + semantische Vorgabe „Abrollen/~40 kt statt Stillstand" — ist in LE9 enthalten.

---

## Status

**SPEC ACCEPTED R2.** Von Thomas Kant freigegeben. R1-Findings adressiert:

| R1-Finding | R2-Status |
|---|---|
| **P1** LE5 Feldnamen existieren im Pilot-Client so nicht | ✅ LE5 — getrenntes Mapping Pilot (nested `record.runway_match.*`) vs. Webapp (top-level Payload) + getrennte LDA-Formeln |
| **P1** `extra=[]` falsch — `skip_serializing_if` lässt leeres Feld weg | ✅ Datenmodell + Test präzisiert: Wire-JSON enthält kein `extra`-Feld, TS-Default `[]` |
| **P2** Aufsetzpunkt-Extra-Zeile signed wording | ✅ neuer Key `touchdown_point_before` + Formatter-Regel (Betrag + Vorzeichen-Key-Wahl) |

Vorgeschichte: R0 → R1 (4 P1, 2 P2) → R2 (2 P1, 1 P2). Keine offenen Fragen mehr.

**Umsetzung:**

- **Code-Slice 1** (Rust-Crate + Wire-Version `score_algorithm_version=3`) → PR #17, QS-clean.
- **Code-Slice 2** (TS-UI + `RunwayUtilizationHelpModal` + `info.rollout`-Tooltip-Fix + Locale-Audit LE9 + Webapp-Mirror) → folgt.

**Kein Code-Touch bis SPEC ACCEPTED.**
